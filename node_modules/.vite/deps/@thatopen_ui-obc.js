import {
  Ai,
  Bn,
  Ci,
  Fr,
  Ri,
  _n,
  dn,
  ke,
  na,
  sa,
  ut
} from "./chunk-W5B2UAA5.js";
import {
  Uf,
  xa,
  xc
} from "./chunk-ODJKMZZX.js";
import {
  IFCCLASSIFICATIONREFERENCE,
  IFCELEMENTQUANTITY,
  IFCMATERIAL,
  IFCMATERIALLAYER,
  IFCMATERIALLAYERSET,
  IFCMATERIALLAYERSETUSAGE,
  IFCMATERIALLIST,
  IFCPROJECT,
  IFCPROPERTYSET,
  REF
} from "./chunk-7F5QJTWE.js";
import {
  AmbientLight,
  BufferAttribute,
  BufferGeometry,
  Color,
  DirectionalLight,
  Frustum,
  Group,
  Light,
  LineBasicMaterial,
  LineSegments,
  Matrix4,
  Object3D,
  PerspectiveCamera,
  Vector2,
  Vector3
} from "./chunk-UZOV2HZI.js";
import "./chunk-5FUTL2UF.js";

// node_modules/@thatopen/ui-obc/dist/index.js
var fr = Object.defineProperty;
var mr = (i, t, e) => t in i ? fr(i, t, { enumerable: true, configurable: true, writable: true, value: e }) : i[t] = e;
var Ot = (i, t, e) => (mr(i, typeof t != "symbol" ? t + "" : t, e), e);
var Gt = Math.min;
var rt = Math.max;
var ti = Math.round;
var yt = (i) => ({
  x: i,
  y: i
});
var br = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
var gr = {
  start: "end",
  end: "start"
};
function $n(i, t, e) {
  return rt(i, Gt(t, e));
}
function Pe(i, t) {
  return typeof i == "function" ? i(t) : i;
}
function at(i) {
  return i.split("-")[0];
}
function di(i) {
  return i.split("-")[1];
}
function ws(i) {
  return i === "x" ? "y" : "x";
}
function $s(i) {
  return i === "y" ? "height" : "width";
}
function Mt(i) {
  return ["top", "bottom"].includes(at(i)) ? "y" : "x";
}
function Cs(i) {
  return ws(Mt(i));
}
function vr(i, t, e) {
  e === void 0 && (e = false);
  const s = di(i), n = Cs(i), o = $s(n);
  let r = n === "x" ? s === (e ? "end" : "start") ? "right" : "left" : s === "start" ? "bottom" : "top";
  return t.reference[o] > t.floating[o] && (r = ei(r)), [r, ei(r)];
}
function yr(i) {
  const t = ei(i);
  return [zi(i), t, zi(t)];
}
function zi(i) {
  return i.replace(/start|end/g, (t) => gr[t]);
}
function _r(i, t, e) {
  const s = ["left", "right"], n = ["right", "left"], o = ["top", "bottom"], r = ["bottom", "top"];
  switch (i) {
    case "top":
    case "bottom":
      return e ? t ? n : s : t ? s : n;
    case "left":
    case "right":
      return t ? o : r;
    default:
      return [];
  }
}
function xr(i, t, e, s) {
  const n = di(i);
  let o = _r(at(i), e === "start", s);
  return n && (o = o.map((r) => r + "-" + n), t && (o = o.concat(o.map(zi)))), o;
}
function ei(i) {
  return i.replace(/left|right|bottom|top/g, (t) => br[t]);
}
function wr(i) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...i
  };
}
function Es(i) {
  return typeof i != "number" ? wr(i) : {
    top: i,
    right: i,
    bottom: i,
    left: i
  };
}
function qt(i) {
  const {
    x: t,
    y: e,
    width: s,
    height: n
  } = i;
  return {
    width: s,
    height: n,
    top: e,
    left: t,
    right: t + s,
    bottom: e + n,
    x: t,
    y: e
  };
}
function Cn(i, t, e) {
  let {
    reference: s,
    floating: n
  } = i;
  const o = Mt(t), r = Cs(t), a = $s(r), l = at(t), u = o === "y", h = s.x + s.width / 2 - n.width / 2, d = s.y + s.height / 2 - n.height / 2, c = s[a] / 2 - n[a] / 2;
  let p;
  switch (l) {
    case "top":
      p = {
        x: h,
        y: s.y - n.height
      };
      break;
    case "bottom":
      p = {
        x: h,
        y: s.y + s.height
      };
      break;
    case "right":
      p = {
        x: s.x + s.width,
        y: d
      };
      break;
    case "left":
      p = {
        x: s.x - n.width,
        y: d
      };
      break;
    default:
      p = {
        x: s.x,
        y: s.y
      };
  }
  switch (di(t)) {
    case "start":
      p[r] -= c * (e && u ? -1 : 1);
      break;
    case "end":
      p[r] += c * (e && u ? -1 : 1);
      break;
  }
  return p;
}
var $r = async (i, t, e) => {
  const {
    placement: s = "bottom",
    strategy: n = "absolute",
    middleware: o = [],
    platform: r
  } = e, a = o.filter(Boolean), l = await (r.isRTL == null ? void 0 : r.isRTL(t));
  let u = await r.getElementRects({
    reference: i,
    floating: t,
    strategy: n
  }), {
    x: h,
    y: d
  } = Cn(u, s, l), c = s, p = {}, g = 0;
  for (let w = 0; w < a.length; w++) {
    const {
      name: _,
      fn: x
    } = a[w], {
      x: m,
      y: b,
      data: v,
      reset: $
    } = await x({
      x: h,
      y: d,
      initialPlacement: s,
      placement: c,
      strategy: n,
      middlewareData: p,
      rects: u,
      platform: r,
      elements: {
        reference: i,
        floating: t
      }
    });
    h = m ?? h, d = b ?? d, p = {
      ...p,
      [_]: {
        ...p[_],
        ...v
      }
    }, $ && g <= 50 && (g++, typeof $ == "object" && ($.placement && (c = $.placement), $.rects && (u = $.rects === true ? await r.getElementRects({
      reference: i,
      floating: t,
      strategy: n
    }) : $.rects), {
      x: h,
      y: d
    } = Cn(u, c, l)), w = -1);
  }
  return {
    x: h,
    y: d,
    placement: c,
    strategy: n,
    middlewareData: p
  };
};
async function tn(i, t) {
  var e;
  t === void 0 && (t = {});
  const {
    x: s,
    y: n,
    platform: o,
    rects: r,
    elements: a,
    strategy: l
  } = i, {
    boundary: u = "clippingAncestors",
    rootBoundary: h = "viewport",
    elementContext: d = "floating",
    altBoundary: c = false,
    padding: p = 0
  } = Pe(t, i), g = Es(p), w = a[c ? d === "floating" ? "reference" : "floating" : d], _ = qt(await o.getClippingRect({
    element: (e = await (o.isElement == null ? void 0 : o.isElement(w))) == null || e ? w : w.contextElement || await (o.getDocumentElement == null ? void 0 : o.getDocumentElement(a.floating)),
    boundary: u,
    rootBoundary: h,
    strategy: l
  })), x = d === "floating" ? {
    x: s,
    y: n,
    width: r.floating.width,
    height: r.floating.height
  } : r.reference, m = await (o.getOffsetParent == null ? void 0 : o.getOffsetParent(a.floating)), b = await (o.isElement == null ? void 0 : o.isElement(m)) ? await (o.getScale == null ? void 0 : o.getScale(m)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  }, v = qt(o.convertOffsetParentRelativeRectToViewportRelativeRect ? await o.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements: a,
    rect: x,
    offsetParent: m,
    strategy: l
  }) : x);
  return {
    top: (_.top - v.top + g.top) / b.y,
    bottom: (v.bottom - _.bottom + g.bottom) / b.y,
    left: (_.left - v.left + g.left) / b.x,
    right: (v.right - _.right + g.right) / b.x
  };
}
var Cr = function(i) {
  return i === void 0 && (i = {}), {
    name: "flip",
    options: i,
    async fn(t) {
      var e, s;
      const {
        placement: n,
        middlewareData: o,
        rects: r,
        initialPlacement: a,
        platform: l,
        elements: u
      } = t, {
        mainAxis: h = true,
        crossAxis: d = true,
        fallbackPlacements: c,
        fallbackStrategy: p = "bestFit",
        fallbackAxisSideDirection: g = "none",
        flipAlignment: w = true,
        ..._
      } = Pe(i, t);
      if ((e = o.arrow) != null && e.alignmentOffset)
        return {};
      const x = at(n), m = Mt(a), b = at(a) === a, v = await (l.isRTL == null ? void 0 : l.isRTL(u.floating)), $ = c || (b || !w ? [ei(a)] : yr(a)), C = g !== "none";
      !c && C && $.push(...xr(a, w, g, v));
      const E = [a, ...$], A = await tn(t, _), j = [];
      let O = ((s = o.flip) == null ? void 0 : s.overflows) || [];
      if (h && j.push(A[x]), d) {
        const U = vr(n, r, v);
        j.push(A[U[0]], A[U[1]]);
      }
      if (O = [...O, {
        placement: n,
        overflows: j
      }], !j.every((U) => U <= 0)) {
        var G, kt;
        const U = (((G = o.flip) == null ? void 0 : G.index) || 0) + 1, dt = E[U];
        if (dt)
          return {
            data: {
              index: U,
              overflows: O
            },
            reset: {
              placement: dt
            }
          };
        let H = (kt = O.filter((et) => et.overflows[0] <= 0).sort((et, q) => et.overflows[1] - q.overflows[1])[0]) == null ? void 0 : kt.placement;
        if (!H)
          switch (p) {
            case "bestFit": {
              var ht;
              const et = (ht = O.filter((q) => {
                if (C) {
                  const it = Mt(q.placement);
                  return it === m || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  it === "y";
                }
                return true;
              }).map((q) => [q.placement, q.overflows.filter((it) => it > 0).reduce((it, xn) => it + xn, 0)]).sort((q, it) => q[1] - it[1])[0]) == null ? void 0 : ht[0];
              et && (H = et);
              break;
            }
            case "initialPlacement":
              H = a;
              break;
          }
        if (n !== H)
          return {
            reset: {
              placement: H
            }
          };
      }
      return {};
    }
  };
};
function As(i) {
  const t = Gt(...i.map((o) => o.left)), e = Gt(...i.map((o) => o.top)), s = rt(...i.map((o) => o.right)), n = rt(...i.map((o) => o.bottom));
  return {
    x: t,
    y: e,
    width: s - t,
    height: n - e
  };
}
function Er(i) {
  const t = i.slice().sort((n, o) => n.y - o.y), e = [];
  let s = null;
  for (let n = 0; n < t.length; n++) {
    const o = t[n];
    !s || o.y - s.y > s.height / 2 ? e.push([o]) : e[e.length - 1].push(o), s = o;
  }
  return e.map((n) => qt(As(n)));
}
var Ar = function(i) {
  return i === void 0 && (i = {}), {
    name: "inline",
    options: i,
    async fn(t) {
      const {
        placement: e,
        elements: s,
        rects: n,
        platform: o,
        strategy: r
      } = t, {
        padding: a = 2,
        x: l,
        y: u
      } = Pe(i, t), h = Array.from(await (o.getClientRects == null ? void 0 : o.getClientRects(s.reference)) || []), d = Er(h), c = qt(As(h)), p = Es(a);
      function g() {
        if (d.length === 2 && d[0].left > d[1].right && l != null && u != null)
          return d.find((_) => l > _.left - p.left && l < _.right + p.right && u > _.top - p.top && u < _.bottom + p.bottom) || c;
        if (d.length >= 2) {
          if (Mt(e) === "y") {
            const O = d[0], G = d[d.length - 1], kt = at(e) === "top", ht = O.top, U = G.bottom, dt = kt ? O.left : G.left, H = kt ? O.right : G.right, et = H - dt, q = U - ht;
            return {
              top: ht,
              bottom: U,
              left: dt,
              right: H,
              width: et,
              height: q,
              x: dt,
              y: ht
            };
          }
          const _ = at(e) === "left", x = rt(...d.map((O) => O.right)), m = Gt(...d.map((O) => O.left)), b = d.filter((O) => _ ? O.left === m : O.right === x), v = b[0].top, $ = b[b.length - 1].bottom, C = m, E = x, A = E - C, j = $ - v;
          return {
            top: v,
            bottom: $,
            left: C,
            right: E,
            width: A,
            height: j,
            x: C,
            y: v
          };
        }
        return c;
      }
      const w = await o.getElementRects({
        reference: {
          getBoundingClientRect: g
        },
        floating: s.floating,
        strategy: r
      });
      return n.reference.x !== w.reference.x || n.reference.y !== w.reference.y || n.reference.width !== w.reference.width || n.reference.height !== w.reference.height ? {
        reset: {
          rects: w
        }
      } : {};
    }
  };
};
async function Sr(i, t) {
  const {
    placement: e,
    platform: s,
    elements: n
  } = i, o = await (s.isRTL == null ? void 0 : s.isRTL(n.floating)), r = at(e), a = di(e), l = Mt(e) === "y", u = ["left", "top"].includes(r) ? -1 : 1, h = o && l ? -1 : 1, d = Pe(t, i);
  let {
    mainAxis: c,
    crossAxis: p,
    alignmentAxis: g
  } = typeof d == "number" ? {
    mainAxis: d,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: 0,
    crossAxis: 0,
    alignmentAxis: null,
    ...d
  };
  return a && typeof g == "number" && (p = a === "end" ? g * -1 : g), l ? {
    x: p * h,
    y: c * u
  } : {
    x: c * u,
    y: p * h
  };
}
var Ss = function(i) {
  return {
    name: "offset",
    options: i,
    async fn(t) {
      var e, s;
      const {
        x: n,
        y: o,
        placement: r,
        middlewareData: a
      } = t, l = await Sr(t, i);
      return r === ((e = a.offset) == null ? void 0 : e.placement) && (s = a.arrow) != null && s.alignmentOffset ? {} : {
        x: n + l.x,
        y: o + l.y,
        data: {
          ...l,
          placement: r
        }
      };
    }
  };
};
var kr = function(i) {
  return i === void 0 && (i = {}), {
    name: "shift",
    options: i,
    async fn(t) {
      const {
        x: e,
        y: s,
        placement: n
      } = t, {
        mainAxis: o = true,
        crossAxis: r = false,
        limiter: a = {
          fn: (_) => {
            let {
              x,
              y: m
            } = _;
            return {
              x,
              y: m
            };
          }
        },
        ...l
      } = Pe(i, t), u = {
        x: e,
        y: s
      }, h = await tn(t, l), d = Mt(at(n)), c = ws(d);
      let p = u[c], g = u[d];
      if (o) {
        const _ = c === "y" ? "top" : "left", x = c === "y" ? "bottom" : "right", m = p + h[_], b = p - h[x];
        p = $n(m, p, b);
      }
      if (r) {
        const _ = d === "y" ? "top" : "left", x = d === "y" ? "bottom" : "right", m = g + h[_], b = g - h[x];
        g = $n(m, g, b);
      }
      const w = a.fn({
        ...t,
        [c]: p,
        [d]: g
      });
      return {
        ...w,
        data: {
          x: w.x - e,
          y: w.y - s
        }
      };
    }
  };
};
function _t(i) {
  return ks(i) ? (i.nodeName || "").toLowerCase() : "#document";
}
function V(i) {
  var t;
  return (i == null || (t = i.ownerDocument) == null ? void 0 : t.defaultView) || window;
}
function wt(i) {
  var t;
  return (t = (ks(i) ? i.ownerDocument : i.document) || window.document) == null ? void 0 : t.documentElement;
}
function ks(i) {
  return i instanceof Node || i instanceof V(i).Node;
}
function lt(i) {
  return i instanceof Element || i instanceof V(i).Element;
}
function nt(i) {
  return i instanceof HTMLElement || i instanceof V(i).HTMLElement;
}
function En(i) {
  return typeof ShadowRoot > "u" ? false : i instanceof ShadowRoot || i instanceof V(i).ShadowRoot;
}
function Re(i) {
  const {
    overflow: t,
    overflowX: e,
    overflowY: s,
    display: n
  } = X(i);
  return /auto|scroll|overlay|hidden|clip/.test(t + s + e) && !["inline", "contents"].includes(n);
}
function Or(i) {
  return ["table", "td", "th"].includes(_t(i));
}
function Tr(i) {
  return [":popover-open", ":modal"].some((t) => {
    try {
      return i.matches(t);
    } catch {
      return false;
    }
  });
}
function en(i) {
  const t = nn(), e = X(i);
  return e.transform !== "none" || e.perspective !== "none" || (e.containerType ? e.containerType !== "normal" : false) || !t && (e.backdropFilter ? e.backdropFilter !== "none" : false) || !t && (e.filter ? e.filter !== "none" : false) || ["transform", "perspective", "filter"].some((s) => (e.willChange || "").includes(s)) || ["paint", "layout", "strict", "content"].some((s) => (e.contain || "").includes(s));
}
function Ir(i) {
  let t = Yt(i);
  for (; nt(t) && !pi(t); ) {
    if (Tr(t))
      return null;
    if (en(t))
      return t;
    t = Yt(t);
  }
  return null;
}
function nn() {
  return typeof CSS > "u" || !CSS.supports ? false : CSS.supports("-webkit-backdrop-filter", "none");
}
function pi(i) {
  return ["html", "body", "#document"].includes(_t(i));
}
function X(i) {
  return V(i).getComputedStyle(i);
}
function fi(i) {
  return lt(i) ? {
    scrollLeft: i.scrollLeft,
    scrollTop: i.scrollTop
  } : {
    scrollLeft: i.pageXOffset,
    scrollTop: i.pageYOffset
  };
}
function Yt(i) {
  if (_t(i) === "html")
    return i;
  const t = (
    // Step into the shadow DOM of the parent of a slotted node.
    i.assignedSlot || // DOM Element detected.
    i.parentNode || // ShadowRoot detected.
    En(i) && i.host || // Fallback.
    wt(i)
  );
  return En(t) ? t.host : t;
}
function Os(i) {
  const t = Yt(i);
  return pi(t) ? i.ownerDocument ? i.ownerDocument.body : i.body : nt(t) && Re(t) ? t : Os(t);
}
function Di(i, t, e) {
  var s;
  t === void 0 && (t = []), e === void 0 && (e = true);
  const n = Os(i), o = n === ((s = i.ownerDocument) == null ? void 0 : s.body), r = V(n);
  return o ? t.concat(r, r.visualViewport || [], Re(n) ? n : [], r.frameElement && e ? Di(r.frameElement) : []) : t.concat(n, Di(n, [], e));
}
function Ts(i) {
  const t = X(i);
  let e = parseFloat(t.width) || 0, s = parseFloat(t.height) || 0;
  const n = nt(i), o = n ? i.offsetWidth : e, r = n ? i.offsetHeight : s, a = ti(e) !== o || ti(s) !== r;
  return a && (e = o, s = r), {
    width: e,
    height: s,
    $: a
  };
}
function Is(i) {
  return lt(i) ? i : i.contextElement;
}
function Vt(i) {
  const t = Is(i);
  if (!nt(t))
    return yt(1);
  const e = t.getBoundingClientRect(), {
    width: s,
    height: n,
    $: o
  } = Ts(t);
  let r = (o ? ti(e.width) : e.width) / s, a = (o ? ti(e.height) : e.height) / n;
  return (!r || !Number.isFinite(r)) && (r = 1), (!a || !Number.isFinite(a)) && (a = 1), {
    x: r,
    y: a
  };
}
var Pr = yt(0);
function Ps(i) {
  const t = V(i);
  return !nn() || !t.visualViewport ? Pr : {
    x: t.visualViewport.offsetLeft,
    y: t.visualViewport.offsetTop
  };
}
function Rr(i, t, e) {
  return t === void 0 && (t = false), !e || t && e !== V(i) ? false : t;
}
function we(i, t, e, s) {
  t === void 0 && (t = false), e === void 0 && (e = false);
  const n = i.getBoundingClientRect(), o = Is(i);
  let r = yt(1);
  t && (s ? lt(s) && (r = Vt(s)) : r = Vt(i));
  const a = Rr(o, e, s) ? Ps(o) : yt(0);
  let l = (n.left + a.x) / r.x, u = (n.top + a.y) / r.y, h = n.width / r.x, d = n.height / r.y;
  if (o) {
    const c = V(o), p = s && lt(s) ? V(s) : s;
    let g = c, w = g.frameElement;
    for (; w && s && p !== g; ) {
      const _ = Vt(w), x = w.getBoundingClientRect(), m = X(w), b = x.left + (w.clientLeft + parseFloat(m.paddingLeft)) * _.x, v = x.top + (w.clientTop + parseFloat(m.paddingTop)) * _.y;
      l *= _.x, u *= _.y, h *= _.x, d *= _.y, l += b, u += v, g = V(w), w = g.frameElement;
    }
  }
  return qt({
    width: h,
    height: d,
    x: l,
    y: u
  });
}
var Nr = [":popover-open", ":modal"];
function Rs(i) {
  return Nr.some((t) => {
    try {
      return i.matches(t);
    } catch {
      return false;
    }
  });
}
function Mr(i) {
  let {
    elements: t,
    rect: e,
    offsetParent: s,
    strategy: n
  } = i;
  const o = n === "fixed", r = wt(s), a = t ? Rs(t.floating) : false;
  if (s === r || a && o)
    return e;
  let l = {
    scrollLeft: 0,
    scrollTop: 0
  }, u = yt(1);
  const h = yt(0), d = nt(s);
  if ((d || !d && !o) && ((_t(s) !== "body" || Re(r)) && (l = fi(s)), nt(s))) {
    const c = we(s);
    u = Vt(s), h.x = c.x + s.clientLeft, h.y = c.y + s.clientTop;
  }
  return {
    width: e.width * u.x,
    height: e.height * u.y,
    x: e.x * u.x - l.scrollLeft * u.x + h.x,
    y: e.y * u.y - l.scrollTop * u.y + h.y
  };
}
function Lr(i) {
  return Array.from(i.getClientRects());
}
function Ns(i) {
  return we(wt(i)).left + fi(i).scrollLeft;
}
function zr(i) {
  const t = wt(i), e = fi(i), s = i.ownerDocument.body, n = rt(t.scrollWidth, t.clientWidth, s.scrollWidth, s.clientWidth), o = rt(t.scrollHeight, t.clientHeight, s.scrollHeight, s.clientHeight);
  let r = -e.scrollLeft + Ns(i);
  const a = -e.scrollTop;
  return X(s).direction === "rtl" && (r += rt(t.clientWidth, s.clientWidth) - n), {
    width: n,
    height: o,
    x: r,
    y: a
  };
}
function Dr(i, t) {
  const e = V(i), s = wt(i), n = e.visualViewport;
  let o = s.clientWidth, r = s.clientHeight, a = 0, l = 0;
  if (n) {
    o = n.width, r = n.height;
    const u = nn();
    (!u || u && t === "fixed") && (a = n.offsetLeft, l = n.offsetTop);
  }
  return {
    width: o,
    height: r,
    x: a,
    y: l
  };
}
function jr(i, t) {
  const e = we(i, true, t === "fixed"), s = e.top + i.clientTop, n = e.left + i.clientLeft, o = nt(i) ? Vt(i) : yt(1), r = i.clientWidth * o.x, a = i.clientHeight * o.y, l = n * o.x, u = s * o.y;
  return {
    width: r,
    height: a,
    x: l,
    y: u
  };
}
function An(i, t, e) {
  let s;
  if (t === "viewport")
    s = Dr(i, e);
  else if (t === "document")
    s = zr(wt(i));
  else if (lt(t))
    s = jr(t, e);
  else {
    const n = Ps(i);
    s = {
      ...t,
      x: t.x - n.x,
      y: t.y - n.y
    };
  }
  return qt(s);
}
function Ms(i, t) {
  const e = Yt(i);
  return e === t || !lt(e) || pi(e) ? false : X(e).position === "fixed" || Ms(e, t);
}
function Hr(i, t) {
  const e = t.get(i);
  if (e)
    return e;
  let s = Di(i, [], false).filter((a) => lt(a) && _t(a) !== "body"), n = null;
  const o = X(i).position === "fixed";
  let r = o ? Yt(i) : i;
  for (; lt(r) && !pi(r); ) {
    const a = X(r), l = en(r);
    !l && a.position === "fixed" && (n = null), (o ? !l && !n : !l && a.position === "static" && n && ["absolute", "fixed"].includes(n.position) || Re(r) && !l && Ms(i, r)) ? s = s.filter((u) => u !== r) : n = a, r = Yt(r);
  }
  return t.set(i, s), s;
}
function Fr2(i) {
  let {
    element: t,
    boundary: e,
    rootBoundary: s,
    strategy: n
  } = i;
  const o = [...e === "clippingAncestors" ? Hr(t, this._c) : [].concat(e), s], r = o[0], a = o.reduce((l, u) => {
    const h = An(t, u, n);
    return l.top = rt(h.top, l.top), l.right = Gt(h.right, l.right), l.bottom = Gt(h.bottom, l.bottom), l.left = rt(h.left, l.left), l;
  }, An(t, r, n));
  return {
    width: a.right - a.left,
    height: a.bottom - a.top,
    x: a.left,
    y: a.top
  };
}
function Br(i) {
  const {
    width: t,
    height: e
  } = Ts(i);
  return {
    width: t,
    height: e
  };
}
function Ur(i, t, e) {
  const s = nt(t), n = wt(t), o = e === "fixed", r = we(i, true, o, t);
  let a = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const l = yt(0);
  if (s || !s && !o)
    if ((_t(t) !== "body" || Re(n)) && (a = fi(t)), s) {
      const d = we(t, true, o, t);
      l.x = d.x + t.clientLeft, l.y = d.y + t.clientTop;
    } else
      n && (l.x = Ns(n));
  const u = r.left + a.scrollLeft - l.x, h = r.top + a.scrollTop - l.y;
  return {
    x: u,
    y: h,
    width: r.width,
    height: r.height
  };
}
function Sn(i, t) {
  return !nt(i) || X(i).position === "fixed" ? null : t ? t(i) : i.offsetParent;
}
function Ls(i, t) {
  const e = V(i);
  if (!nt(i) || Rs(i))
    return e;
  let s = Sn(i, t);
  for (; s && Or(s) && X(s).position === "static"; )
    s = Sn(s, t);
  return s && (_t(s) === "html" || _t(s) === "body" && X(s).position === "static" && !en(s)) ? e : s || Ir(i) || e;
}
var Vr = async function(i) {
  const t = this.getOffsetParent || Ls, e = this.getDimensions;
  return {
    reference: Ur(i.reference, await t(i.floating), i.strategy),
    floating: {
      x: 0,
      y: 0,
      ...await e(i.floating)
    }
  };
};
function Wr(i) {
  return X(i).direction === "rtl";
}
var Gr = {
  convertOffsetParentRelativeRectToViewportRelativeRect: Mr,
  getDocumentElement: wt,
  getClippingRect: Fr2,
  getOffsetParent: Ls,
  getElementRects: Vr,
  getClientRects: Lr,
  getDimensions: Br,
  getScale: Vt,
  isElement: lt,
  isRTL: Wr
};
var zs = kr;
var Ds = Cr;
var js = Ar;
var Hs = (i, t, e) => {
  const s = /* @__PURE__ */ new Map(), n = {
    platform: Gr,
    ...e
  }, o = {
    ...n.platform,
    _c: s
  };
  return $r(i, t, {
    ...n,
    platform: o
  });
};
var Je = globalThis;
var sn = Je.ShadowRoot && (Je.ShadyCSS === void 0 || Je.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;
var on = Symbol();
var kn = /* @__PURE__ */ new WeakMap();
var Fs = class {
  constructor(i, t, e) {
    if (this._$cssResult$ = true, e !== on)
      throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
    this.cssText = i, this.t = t;
  }
  get styleSheet() {
    let i = this.o;
    const t = this.t;
    if (sn && i === void 0) {
      const e = t !== void 0 && t.length === 1;
      e && (i = kn.get(t)), i === void 0 && ((this.o = i = new CSSStyleSheet()).replaceSync(this.cssText), e && kn.set(t, i));
    }
    return i;
  }
  toString() {
    return this.cssText;
  }
};
var qr = (i) => new Fs(typeof i == "string" ? i : i + "", void 0, on);
var T = (i, ...t) => {
  const e = i.length === 1 ? i[0] : t.reduce((s, n, o) => s + ((r) => {
    if (r._$cssResult$ === true)
      return r.cssText;
    if (typeof r == "number")
      return r;
    throw Error("Value passed to 'css' function must be a 'css' function result: " + r + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
  })(n) + i[o + 1], i[0]);
  return new Fs(e, i, on);
};
var Yr = (i, t) => {
  if (sn)
    i.adoptedStyleSheets = t.map((e) => e instanceof CSSStyleSheet ? e : e.styleSheet);
  else
    for (const e of t) {
      const s = document.createElement("style"), n = Je.litNonce;
      n !== void 0 && s.setAttribute("nonce", n), s.textContent = e.cssText, i.appendChild(s);
    }
};
var On = sn ? (i) => i : (i) => i instanceof CSSStyleSheet ? ((t) => {
  let e = "";
  for (const s of t.cssRules)
    e += s.cssText;
  return qr(e);
})(i) : i;
var { is: Xr, defineProperty: Jr, getOwnPropertyDescriptor: Qr, getOwnPropertyNames: Kr, getOwnPropertySymbols: Zr, getPrototypeOf: ta } = Object;
var Xt = globalThis;
var Tn = Xt.trustedTypes;
var ea = Tn ? Tn.emptyScript : "";
var In = Xt.reactiveElementPolyfillSupport;
var be = (i, t) => i;
var ii = { toAttribute(i, t) {
  switch (t) {
    case Boolean:
      i = i ? ea : null;
      break;
    case Object:
    case Array:
      i = i == null ? i : JSON.stringify(i);
  }
  return i;
}, fromAttribute(i, t) {
  let e = i;
  switch (t) {
    case Boolean:
      e = i !== null;
      break;
    case Number:
      e = i === null ? null : Number(i);
      break;
    case Object:
    case Array:
      try {
        e = JSON.parse(i);
      } catch {
        e = null;
      }
  }
  return e;
} };
var rn = (i, t) => !Xr(i, t);
var Pn = { attribute: true, type: String, converter: ii, reflect: false, hasChanged: rn };
Symbol.metadata ?? (Symbol.metadata = Symbol("metadata")), Xt.litPropertyMetadata ?? (Xt.litPropertyMetadata = /* @__PURE__ */ new WeakMap());
var Bt = class extends HTMLElement {
  static addInitializer(t) {
    this._$Ei(), (this.l ?? (this.l = [])).push(t);
  }
  static get observedAttributes() {
    return this.finalize(), this._$Eh && [...this._$Eh.keys()];
  }
  static createProperty(t, e = Pn) {
    if (e.state && (e.attribute = false), this._$Ei(), this.elementProperties.set(t, e), !e.noAccessor) {
      const s = Symbol(), n = this.getPropertyDescriptor(t, s, e);
      n !== void 0 && Jr(this.prototype, t, n);
    }
  }
  static getPropertyDescriptor(t, e, s) {
    const { get: n, set: o } = Qr(this.prototype, t) ?? { get() {
      return this[e];
    }, set(r) {
      this[e] = r;
    } };
    return { get() {
      return n == null ? void 0 : n.call(this);
    }, set(r) {
      const a = n == null ? void 0 : n.call(this);
      o.call(this, r), this.requestUpdate(t, a, s);
    }, configurable: true, enumerable: true };
  }
  static getPropertyOptions(t) {
    return this.elementProperties.get(t) ?? Pn;
  }
  static _$Ei() {
    if (this.hasOwnProperty(be("elementProperties")))
      return;
    const t = ta(this);
    t.finalize(), t.l !== void 0 && (this.l = [...t.l]), this.elementProperties = new Map(t.elementProperties);
  }
  static finalize() {
    if (this.hasOwnProperty(be("finalized")))
      return;
    if (this.finalized = true, this._$Ei(), this.hasOwnProperty(be("properties"))) {
      const e = this.properties, s = [...Kr(e), ...Zr(e)];
      for (const n of s)
        this.createProperty(n, e[n]);
    }
    const t = this[Symbol.metadata];
    if (t !== null) {
      const e = litPropertyMetadata.get(t);
      if (e !== void 0)
        for (const [s, n] of e)
          this.elementProperties.set(s, n);
    }
    this._$Eh = /* @__PURE__ */ new Map();
    for (const [e, s] of this.elementProperties) {
      const n = this._$Eu(e, s);
      n !== void 0 && this._$Eh.set(n, e);
    }
    this.elementStyles = this.finalizeStyles(this.styles);
  }
  static finalizeStyles(t) {
    const e = [];
    if (Array.isArray(t)) {
      const s = new Set(t.flat(1 / 0).reverse());
      for (const n of s)
        e.unshift(On(n));
    } else
      t !== void 0 && e.push(On(t));
    return e;
  }
  static _$Eu(t, e) {
    const s = e.attribute;
    return s === false ? void 0 : typeof s == "string" ? s : typeof t == "string" ? t.toLowerCase() : void 0;
  }
  constructor() {
    super(), this._$Ep = void 0, this.isUpdatePending = false, this.hasUpdated = false, this._$Em = null, this._$Ev();
  }
  _$Ev() {
    var t;
    this._$ES = new Promise((e) => this.enableUpdating = e), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), (t = this.constructor.l) == null || t.forEach((e) => e(this));
  }
  addController(t) {
    var e;
    (this._$EO ?? (this._$EO = /* @__PURE__ */ new Set())).add(t), this.renderRoot !== void 0 && this.isConnected && ((e = t.hostConnected) == null || e.call(t));
  }
  removeController(t) {
    var e;
    (e = this._$EO) == null || e.delete(t);
  }
  _$E_() {
    const t = /* @__PURE__ */ new Map(), e = this.constructor.elementProperties;
    for (const s of e.keys())
      this.hasOwnProperty(s) && (t.set(s, this[s]), delete this[s]);
    t.size > 0 && (this._$Ep = t);
  }
  createRenderRoot() {
    const t = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
    return Yr(t, this.constructor.elementStyles), t;
  }
  connectedCallback() {
    var t;
    this.renderRoot ?? (this.renderRoot = this.createRenderRoot()), this.enableUpdating(true), (t = this._$EO) == null || t.forEach((e) => {
      var s;
      return (s = e.hostConnected) == null ? void 0 : s.call(e);
    });
  }
  enableUpdating(t) {
  }
  disconnectedCallback() {
    var t;
    (t = this._$EO) == null || t.forEach((e) => {
      var s;
      return (s = e.hostDisconnected) == null ? void 0 : s.call(e);
    });
  }
  attributeChangedCallback(t, e, s) {
    this._$AK(t, s);
  }
  _$EC(t, e) {
    var s;
    const n = this.constructor.elementProperties.get(t), o = this.constructor._$Eu(t, n);
    if (o !== void 0 && n.reflect === true) {
      const r = (((s = n.converter) == null ? void 0 : s.toAttribute) !== void 0 ? n.converter : ii).toAttribute(e, n.type);
      this._$Em = t, r == null ? this.removeAttribute(o) : this.setAttribute(o, r), this._$Em = null;
    }
  }
  _$AK(t, e) {
    var s;
    const n = this.constructor, o = n._$Eh.get(t);
    if (o !== void 0 && this._$Em !== o) {
      const r = n.getPropertyOptions(o), a = typeof r.converter == "function" ? { fromAttribute: r.converter } : ((s = r.converter) == null ? void 0 : s.fromAttribute) !== void 0 ? r.converter : ii;
      this._$Em = o, this[o] = a.fromAttribute(e, r.type), this._$Em = null;
    }
  }
  requestUpdate(t, e, s) {
    if (t !== void 0) {
      if (s ?? (s = this.constructor.getPropertyOptions(t)), !(s.hasChanged ?? rn)(this[t], e))
        return;
      this.P(t, e, s);
    }
    this.isUpdatePending === false && (this._$ES = this._$ET());
  }
  P(t, e, s) {
    this._$AL.has(t) || this._$AL.set(t, e), s.reflect === true && this._$Em !== t && (this._$Ej ?? (this._$Ej = /* @__PURE__ */ new Set())).add(t);
  }
  async _$ET() {
    this.isUpdatePending = true;
    try {
      await this._$ES;
    } catch (e) {
      Promise.reject(e);
    }
    const t = this.scheduleUpdate();
    return t != null && await t, !this.isUpdatePending;
  }
  scheduleUpdate() {
    return this.performUpdate();
  }
  performUpdate() {
    var t;
    if (!this.isUpdatePending)
      return;
    if (!this.hasUpdated) {
      if (this.renderRoot ?? (this.renderRoot = this.createRenderRoot()), this._$Ep) {
        for (const [o, r] of this._$Ep)
          this[o] = r;
        this._$Ep = void 0;
      }
      const n = this.constructor.elementProperties;
      if (n.size > 0)
        for (const [o, r] of n)
          r.wrapped !== true || this._$AL.has(o) || this[o] === void 0 || this.P(o, this[o], r);
    }
    let e = false;
    const s = this._$AL;
    try {
      e = this.shouldUpdate(s), e ? (this.willUpdate(s), (t = this._$EO) == null || t.forEach((n) => {
        var o;
        return (o = n.hostUpdate) == null ? void 0 : o.call(n);
      }), this.update(s)) : this._$EU();
    } catch (n) {
      throw e = false, this._$EU(), n;
    }
    e && this._$AE(s);
  }
  willUpdate(t) {
  }
  _$AE(t) {
    var e;
    (e = this._$EO) == null || e.forEach((s) => {
      var n;
      return (n = s.hostUpdated) == null ? void 0 : n.call(s);
    }), this.hasUpdated || (this.hasUpdated = true, this.firstUpdated(t)), this.updated(t);
  }
  _$EU() {
    this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = false;
  }
  get updateComplete() {
    return this.getUpdateComplete();
  }
  getUpdateComplete() {
    return this._$ES;
  }
  shouldUpdate(t) {
    return true;
  }
  update(t) {
    this._$Ej && (this._$Ej = this._$Ej.forEach((e) => this._$EC(e, this[e]))), this._$EU();
  }
  updated(t) {
  }
  firstUpdated(t) {
  }
};
Bt.elementStyles = [], Bt.shadowRootOptions = { mode: "open" }, Bt[be("elementProperties")] = /* @__PURE__ */ new Map(), Bt[be("finalized")] = /* @__PURE__ */ new Map(), In == null || In({ ReactiveElement: Bt }), (Xt.reactiveElementVersions ?? (Xt.reactiveElementVersions = [])).push("2.0.4");
var ni = globalThis;
var si = ni.trustedTypes;
var Rn = si ? si.createPolicy("lit-html", { createHTML: (i) => i }) : void 0;
var Bs = "$lit$";
var mt = `lit$${Math.random().toFixed(9).slice(2)}$`;
var Us = "?" + mt;
var ia = `<${Us}>`;
var Lt = document;
var $e = () => Lt.createComment("");
var Ce = (i) => i === null || typeof i != "object" && typeof i != "function";
var Vs = Array.isArray;
var na2 = (i) => Vs(i) || typeof (i == null ? void 0 : i[Symbol.iterator]) == "function";
var Si = `[ 	
\f\r]`;
var pe = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
var Nn = /-->/g;
var Mn = />/g;
var Tt = RegExp(`>|${Si}(?:([^\\s"'>=/]+)(${Si}*=${Si}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
var Ln = /'/g;
var zn = /"/g;
var Ws = /^(?:script|style|textarea|title)$/i;
var sa2 = (i) => (t, ...e) => ({ _$litType$: i, strings: t, values: e });
var y = sa2(1);
var Jt = Symbol.for("lit-noChange");
var M = Symbol.for("lit-nothing");
var Dn = /* @__PURE__ */ new WeakMap();
var Pt = Lt.createTreeWalker(Lt, 129);
function Gs(i, t) {
  if (!Array.isArray(i) || !i.hasOwnProperty("raw"))
    throw Error("invalid template strings array");
  return Rn !== void 0 ? Rn.createHTML(t) : t;
}
var oa = (i, t) => {
  const e = i.length - 1, s = [];
  let n, o = t === 2 ? "<svg>" : "", r = pe;
  for (let a = 0; a < e; a++) {
    const l = i[a];
    let u, h, d = -1, c = 0;
    for (; c < l.length && (r.lastIndex = c, h = r.exec(l), h !== null); )
      c = r.lastIndex, r === pe ? h[1] === "!--" ? r = Nn : h[1] !== void 0 ? r = Mn : h[2] !== void 0 ? (Ws.test(h[2]) && (n = RegExp("</" + h[2], "g")), r = Tt) : h[3] !== void 0 && (r = Tt) : r === Tt ? h[0] === ">" ? (r = n ?? pe, d = -1) : h[1] === void 0 ? d = -2 : (d = r.lastIndex - h[2].length, u = h[1], r = h[3] === void 0 ? Tt : h[3] === '"' ? zn : Ln) : r === zn || r === Ln ? r = Tt : r === Nn || r === Mn ? r = pe : (r = Tt, n = void 0);
    const p = r === Tt && i[a + 1].startsWith("/>") ? " " : "";
    o += r === pe ? l + ia : d >= 0 ? (s.push(u), l.slice(0, d) + Bs + l.slice(d) + mt + p) : l + mt + (d === -2 ? a : p);
  }
  return [Gs(i, o + (i[e] || "<?>") + (t === 2 ? "</svg>" : "")), s];
};
var Ee = class _Ee {
  constructor({ strings: t, _$litType$: e }, s) {
    let n;
    this.parts = [];
    let o = 0, r = 0;
    const a = t.length - 1, l = this.parts, [u, h] = oa(t, e);
    if (this.el = _Ee.createElement(u, s), Pt.currentNode = this.el.content, e === 2) {
      const d = this.el.content.firstChild;
      d.replaceWith(...d.childNodes);
    }
    for (; (n = Pt.nextNode()) !== null && l.length < a; ) {
      if (n.nodeType === 1) {
        if (n.hasAttributes())
          for (const d of n.getAttributeNames())
            if (d.endsWith(Bs)) {
              const c = h[r++], p = n.getAttribute(d).split(mt), g = /([.?@])?(.*)/.exec(c);
              l.push({ type: 1, index: o, name: g[2], strings: p, ctor: g[1] === "." ? aa : g[1] === "?" ? la : g[1] === "@" ? ca : mi }), n.removeAttribute(d);
            } else
              d.startsWith(mt) && (l.push({ type: 6, index: o }), n.removeAttribute(d));
        if (Ws.test(n.tagName)) {
          const d = n.textContent.split(mt), c = d.length - 1;
          if (c > 0) {
            n.textContent = si ? si.emptyScript : "";
            for (let p = 0; p < c; p++)
              n.append(d[p], $e()), Pt.nextNode(), l.push({ type: 2, index: ++o });
            n.append(d[c], $e());
          }
        }
      } else if (n.nodeType === 8)
        if (n.data === Us)
          l.push({ type: 2, index: o });
        else {
          let d = -1;
          for (; (d = n.data.indexOf(mt, d + 1)) !== -1; )
            l.push({ type: 7, index: o }), d += mt.length - 1;
        }
      o++;
    }
  }
  static createElement(t, e) {
    const s = Lt.createElement("template");
    return s.innerHTML = t, s;
  }
};
function Qt(i, t, e = i, s) {
  var n, o;
  if (t === Jt)
    return t;
  let r = s !== void 0 ? (n = e._$Co) == null ? void 0 : n[s] : e._$Cl;
  const a = Ce(t) ? void 0 : t._$litDirective$;
  return (r == null ? void 0 : r.constructor) !== a && ((o = r == null ? void 0 : r._$AO) == null || o.call(r, false), a === void 0 ? r = void 0 : (r = new a(i), r._$AT(i, e, s)), s !== void 0 ? (e._$Co ?? (e._$Co = []))[s] = r : e._$Cl = r), r !== void 0 && (t = Qt(i, r._$AS(i, t.values), r, s)), t;
}
var ra = class {
  constructor(t, e) {
    this._$AV = [], this._$AN = void 0, this._$AD = t, this._$AM = e;
  }
  get parentNode() {
    return this._$AM.parentNode;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  u(t) {
    const { el: { content: e }, parts: s } = this._$AD, n = ((t == null ? void 0 : t.creationScope) ?? Lt).importNode(e, true);
    Pt.currentNode = n;
    let o = Pt.nextNode(), r = 0, a = 0, l = s[0];
    for (; l !== void 0; ) {
      if (r === l.index) {
        let u;
        l.type === 2 ? u = new Ne(o, o.nextSibling, this, t) : l.type === 1 ? u = new l.ctor(o, l.name, l.strings, this, t) : l.type === 6 && (u = new ua(o, this, t)), this._$AV.push(u), l = s[++a];
      }
      r !== (l == null ? void 0 : l.index) && (o = Pt.nextNode(), r++);
    }
    return Pt.currentNode = Lt, n;
  }
  p(t) {
    let e = 0;
    for (const s of this._$AV)
      s !== void 0 && (s.strings !== void 0 ? (s._$AI(t, s, e), e += s.strings.length - 2) : s._$AI(t[e])), e++;
  }
};
var Ne = class _Ne {
  get _$AU() {
    var t;
    return ((t = this._$AM) == null ? void 0 : t._$AU) ?? this._$Cv;
  }
  constructor(t, e, s, n) {
    this.type = 2, this._$AH = M, this._$AN = void 0, this._$AA = t, this._$AB = e, this._$AM = s, this.options = n, this._$Cv = (n == null ? void 0 : n.isConnected) ?? true;
  }
  get parentNode() {
    let t = this._$AA.parentNode;
    const e = this._$AM;
    return e !== void 0 && (t == null ? void 0 : t.nodeType) === 11 && (t = e.parentNode), t;
  }
  get startNode() {
    return this._$AA;
  }
  get endNode() {
    return this._$AB;
  }
  _$AI(t, e = this) {
    t = Qt(this, t, e), Ce(t) ? t === M || t == null || t === "" ? (this._$AH !== M && this._$AR(), this._$AH = M) : t !== this._$AH && t !== Jt && this._(t) : t._$litType$ !== void 0 ? this.$(t) : t.nodeType !== void 0 ? this.T(t) : na2(t) ? this.k(t) : this._(t);
  }
  S(t) {
    return this._$AA.parentNode.insertBefore(t, this._$AB);
  }
  T(t) {
    this._$AH !== t && (this._$AR(), this._$AH = this.S(t));
  }
  _(t) {
    this._$AH !== M && Ce(this._$AH) ? this._$AA.nextSibling.data = t : this.T(Lt.createTextNode(t)), this._$AH = t;
  }
  $(t) {
    var e;
    const { values: s, _$litType$: n } = t, o = typeof n == "number" ? this._$AC(t) : (n.el === void 0 && (n.el = Ee.createElement(Gs(n.h, n.h[0]), this.options)), n);
    if (((e = this._$AH) == null ? void 0 : e._$AD) === o)
      this._$AH.p(s);
    else {
      const r = new ra(o, this), a = r.u(this.options);
      r.p(s), this.T(a), this._$AH = r;
    }
  }
  _$AC(t) {
    let e = Dn.get(t.strings);
    return e === void 0 && Dn.set(t.strings, e = new Ee(t)), e;
  }
  k(t) {
    Vs(this._$AH) || (this._$AH = [], this._$AR());
    const e = this._$AH;
    let s, n = 0;
    for (const o of t)
      n === e.length ? e.push(s = new _Ne(this.S($e()), this.S($e()), this, this.options)) : s = e[n], s._$AI(o), n++;
    n < e.length && (this._$AR(s && s._$AB.nextSibling, n), e.length = n);
  }
  _$AR(t = this._$AA.nextSibling, e) {
    var s;
    for ((s = this._$AP) == null ? void 0 : s.call(this, false, true, e); t && t !== this._$AB; ) {
      const n = t.nextSibling;
      t.remove(), t = n;
    }
  }
  setConnected(t) {
    var e;
    this._$AM === void 0 && (this._$Cv = t, (e = this._$AP) == null || e.call(this, t));
  }
};
var mi = class {
  get tagName() {
    return this.element.tagName;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  constructor(t, e, s, n, o) {
    this.type = 1, this._$AH = M, this._$AN = void 0, this.element = t, this.name = e, this._$AM = n, this.options = o, s.length > 2 || s[0] !== "" || s[1] !== "" ? (this._$AH = Array(s.length - 1).fill(new String()), this.strings = s) : this._$AH = M;
  }
  _$AI(t, e = this, s, n) {
    const o = this.strings;
    let r = false;
    if (o === void 0)
      t = Qt(this, t, e, 0), r = !Ce(t) || t !== this._$AH && t !== Jt, r && (this._$AH = t);
    else {
      const a = t;
      let l, u;
      for (t = o[0], l = 0; l < o.length - 1; l++)
        u = Qt(this, a[s + l], e, l), u === Jt && (u = this._$AH[l]), r || (r = !Ce(u) || u !== this._$AH[l]), u === M ? t = M : t !== M && (t += (u ?? "") + o[l + 1]), this._$AH[l] = u;
    }
    r && !n && this.j(t);
  }
  j(t) {
    t === M ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t ?? "");
  }
};
var aa = class extends mi {
  constructor() {
    super(...arguments), this.type = 3;
  }
  j(t) {
    this.element[this.name] = t === M ? void 0 : t;
  }
};
var la = class extends mi {
  constructor() {
    super(...arguments), this.type = 4;
  }
  j(t) {
    this.element.toggleAttribute(this.name, !!t && t !== M);
  }
};
var ca = class extends mi {
  constructor(t, e, s, n, o) {
    super(t, e, s, n, o), this.type = 5;
  }
  _$AI(t, e = this) {
    if ((t = Qt(this, t, e, 0) ?? M) === Jt)
      return;
    const s = this._$AH, n = t === M && s !== M || t.capture !== s.capture || t.once !== s.once || t.passive !== s.passive, o = t !== M && (s === M || n);
    n && this.element.removeEventListener(this.name, this, s), o && this.element.addEventListener(this.name, this, t), this._$AH = t;
  }
  handleEvent(t) {
    var e;
    typeof this._$AH == "function" ? this._$AH.call(((e = this.options) == null ? void 0 : e.host) ?? this.element, t) : this._$AH.handleEvent(t);
  }
};
var ua = class {
  constructor(t, e, s) {
    this.element = t, this.type = 6, this._$AN = void 0, this._$AM = e, this.options = s;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AI(t) {
    Qt(this, t);
  }
};
var jn = ni.litHtmlPolyfillSupport;
jn == null || jn(Ee, Ne), (ni.litHtmlVersions ?? (ni.litHtmlVersions = [])).push("3.1.4");
var Kt = (i, t, e) => {
  const s = (e == null ? void 0 : e.renderBefore) ?? t;
  let n = s._$litPart$;
  if (n === void 0) {
    const o = (e == null ? void 0 : e.renderBefore) ?? null;
    s._$litPart$ = n = new Ne(t.insertBefore($e(), o), o, void 0, e ?? {});
  }
  return n._$AI(i), n;
};
var k = class extends Bt {
  constructor() {
    super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
  }
  createRenderRoot() {
    var t;
    const e = super.createRenderRoot();
    return (t = this.renderOptions).renderBefore ?? (t.renderBefore = e.firstChild), e;
  }
  update(t) {
    const e = this.render();
    this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(t), this._$Do = Kt(e, this.renderRoot, this.renderOptions);
  }
  connectedCallback() {
    var t;
    super.connectedCallback(), (t = this._$Do) == null || t.setConnected(true);
  }
  disconnectedCallback() {
    var t;
    super.disconnectedCallback(), (t = this._$Do) == null || t.setConnected(false);
  }
  render() {
    return Jt;
  }
};
var Hn;
k._$litElement$ = true, k.finalized = true, (Hn = globalThis.litElementHydrateSupport) == null || Hn.call(globalThis, { LitElement: k });
var Fn = globalThis.litElementPolyfillSupport;
Fn == null || Fn({ LitElement: k });
(globalThis.litElementVersions ?? (globalThis.litElementVersions = [])).push("4.0.6");
var ha = { attribute: true, type: String, converter: ii, reflect: false, hasChanged: rn };
var da = (i = ha, t, e) => {
  const { kind: s, metadata: n } = e;
  let o = globalThis.litPropertyMetadata.get(n);
  if (o === void 0 && globalThis.litPropertyMetadata.set(n, o = /* @__PURE__ */ new Map()), o.set(e.name, i), s === "accessor") {
    const { name: r } = e;
    return { set(a) {
      const l = t.get.call(this);
      t.set.call(this, a), this.requestUpdate(r, l, i);
    }, init(a) {
      return a !== void 0 && this.P(r, void 0, i), a;
    } };
  }
  if (s === "setter") {
    const { name: r } = e;
    return function(a) {
      const l = this[r];
      t.call(this, a), this.requestUpdate(r, l, i);
    };
  }
  throw Error("Unsupported decorator location: " + s);
};
function f(i) {
  return (t, e) => typeof e == "object" ? da(i, t, e) : ((s, n, o) => {
    const r = n.hasOwnProperty(o);
    return n.constructor.createProperty(o, r ? { ...s, wrapped: true } : s), r ? Object.getOwnPropertyDescriptor(n, o) : void 0;
  })(i, t, e);
}
function Me(i) {
  return f({ ...i, state: true, attribute: false });
}
var pa = (i) => i.strings === void 0;
var fa = { ATTRIBUTE: 1, CHILD: 2, PROPERTY: 3, BOOLEAN_ATTRIBUTE: 4, EVENT: 5, ELEMENT: 6 };
var ma = (i) => (...t) => ({ _$litDirective$: i, values: t });
var ba = class {
  constructor(i) {
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AT(i, t, e) {
    this._$Ct = i, this._$AM = t, this._$Ci = e;
  }
  _$AS(i, t) {
    return this.update(i, t);
  }
  update(i, t) {
    return this.render(...t);
  }
};
var ge = (i, t) => {
  var e;
  const s = i._$AN;
  if (s === void 0)
    return false;
  for (const n of s)
    (e = n._$AO) == null || e.call(n, t, false), ge(n, t);
  return true;
};
var oi = (i) => {
  let t, e;
  do {
    if ((t = i._$AM) === void 0)
      break;
    e = t._$AN, e.delete(i), i = t;
  } while ((e == null ? void 0 : e.size) === 0);
};
var qs = (i) => {
  for (let t; t = i._$AM; i = t) {
    let e = t._$AN;
    if (e === void 0)
      t._$AN = e = /* @__PURE__ */ new Set();
    else if (e.has(i))
      break;
    e.add(i), ya(t);
  }
};
function ga(i) {
  this._$AN !== void 0 ? (oi(this), this._$AM = i, qs(this)) : this._$AM = i;
}
function va(i, t = false, e = 0) {
  const s = this._$AH, n = this._$AN;
  if (n !== void 0 && n.size !== 0)
    if (t)
      if (Array.isArray(s))
        for (let o = e; o < s.length; o++)
          ge(s[o], false), oi(s[o]);
      else
        s != null && (ge(s, false), oi(s));
    else
      ge(this, i);
}
var ya = (i) => {
  i.type == fa.CHILD && (i._$AP ?? (i._$AP = va), i._$AQ ?? (i._$AQ = ga));
};
var _a = class extends ba {
  constructor() {
    super(...arguments), this._$AN = void 0;
  }
  _$AT(t, e, s) {
    super._$AT(t, e, s), qs(this), this.isConnected = t._$AU;
  }
  _$AO(t, e = true) {
    var s, n;
    t !== this.isConnected && (this.isConnected = t, t ? (s = this.reconnected) == null || s.call(this) : (n = this.disconnected) == null || n.call(this)), e && (ge(this, t), oi(this));
  }
  setValue(t) {
    if (pa(this._$Ct))
      this._$Ct._$AI(t, this);
    else {
      const e = [...this._$Ct._$AH];
      e[this._$Ci] = t, this._$Ct._$AI(e, this, 0);
    }
  }
  disconnected() {
  }
  reconnected() {
  }
};
var gt = () => new xa2();
var xa2 = class {
};
var ki = /* @__PURE__ */ new WeakMap();
var Q = ma(class extends _a {
  render(i) {
    return M;
  }
  update(i, [t]) {
    var e;
    const s = t !== this.Y;
    return s && this.Y !== void 0 && this.rt(void 0), (s || this.lt !== this.ct) && (this.Y = t, this.ht = (e = i.options) == null ? void 0 : e.host, this.rt(this.ct = i.element)), M;
  }
  rt(i) {
    if (this.isConnected || (i = void 0), typeof this.Y == "function") {
      const t = this.ht ?? globalThis;
      let e = ki.get(t);
      e === void 0 && (e = /* @__PURE__ */ new WeakMap(), ki.set(t, e)), e.get(this.Y) !== void 0 && this.Y.call(this.ht, void 0), e.set(this.Y, i), i !== void 0 && this.Y.call(this.ht, i);
    } else
      this.Y.value = i;
  }
  get lt() {
    var i, t;
    return typeof this.Y == "function" ? (i = ki.get(this.ht ?? globalThis)) == null ? void 0 : i.get(this.Y) : (t = this.Y) == null ? void 0 : t.value;
  }
  disconnected() {
    this.lt === this.ct && this.rt(void 0);
  }
  reconnected() {
    this.rt(this.ct);
  }
});
var Ys = Object.freeze(
  {
    left: 0,
    top: 0,
    width: 16,
    height: 16
  }
);
var ri = Object.freeze({
  rotate: 0,
  vFlip: false,
  hFlip: false
});
var Le = Object.freeze({
  ...Ys,
  ...ri
});
var ji = Object.freeze({
  ...Le,
  body: "",
  hidden: false
});
var wa = Object.freeze({
  width: null,
  height: null
});
var Xs = Object.freeze({
  // Dimensions
  ...wa,
  // Transformations
  ...ri
});
function $a(i, t = 0) {
  const e = i.replace(/^-?[0-9.]*/, "");
  function s(n) {
    for (; n < 0; )
      n += 4;
    return n % 4;
  }
  if (e === "") {
    const n = parseInt(i);
    return isNaN(n) ? 0 : s(n);
  } else if (e !== i) {
    let n = 0;
    switch (e) {
      case "%":
        n = 25;
        break;
      case "deg":
        n = 90;
    }
    if (n) {
      let o = parseFloat(i.slice(0, i.length - e.length));
      return isNaN(o) ? 0 : (o = o / n, o % 1 === 0 ? s(o) : 0);
    }
  }
  return t;
}
var Ca = /[\s,]+/;
function Ea(i, t) {
  t.split(Ca).forEach((e) => {
    switch (e.trim()) {
      case "horizontal":
        i.hFlip = true;
        break;
      case "vertical":
        i.vFlip = true;
        break;
    }
  });
}
var Js = {
  ...Xs,
  preserveAspectRatio: ""
};
function Bn2(i) {
  const t = {
    ...Js
  }, e = (s, n) => i.getAttribute(s) || n;
  return t.width = e("width", null), t.height = e("height", null), t.rotate = $a(e("rotate", "")), Ea(t, e("flip", "")), t.preserveAspectRatio = e("preserveAspectRatio", e("preserveaspectratio", "")), t;
}
function Aa(i, t) {
  for (const e in Js)
    if (i[e] !== t[e])
      return true;
  return false;
}
var ve = /^[a-z0-9]+(-[a-z0-9]+)*$/;
var ze = (i, t, e, s = "") => {
  const n = i.split(":");
  if (i.slice(0, 1) === "@") {
    if (n.length < 2 || n.length > 3)
      return null;
    s = n.shift().slice(1);
  }
  if (n.length > 3 || !n.length)
    return null;
  if (n.length > 1) {
    const a = n.pop(), l = n.pop(), u = {
      // Allow provider without '@': "provider:prefix:name"
      provider: n.length > 0 ? n[0] : s,
      prefix: l,
      name: a
    };
    return t && !Qe(u) ? null : u;
  }
  const o = n[0], r = o.split("-");
  if (r.length > 1) {
    const a = {
      provider: s,
      prefix: r.shift(),
      name: r.join("-")
    };
    return t && !Qe(a) ? null : a;
  }
  if (e && s === "") {
    const a = {
      provider: s,
      prefix: "",
      name: o
    };
    return t && !Qe(a, e) ? null : a;
  }
  return null;
};
var Qe = (i, t) => i ? !!((i.provider === "" || i.provider.match(ve)) && (t && i.prefix === "" || i.prefix.match(ve)) && i.name.match(ve)) : false;
function Sa(i, t) {
  const e = {};
  !i.hFlip != !t.hFlip && (e.hFlip = true), !i.vFlip != !t.vFlip && (e.vFlip = true);
  const s = ((i.rotate || 0) + (t.rotate || 0)) % 4;
  return s && (e.rotate = s), e;
}
function Un(i, t) {
  const e = Sa(i, t);
  for (const s in ji)
    s in ri ? s in i && !(s in e) && (e[s] = ri[s]) : s in t ? e[s] = t[s] : s in i && (e[s] = i[s]);
  return e;
}
function ka(i, t) {
  const e = i.icons, s = i.aliases || /* @__PURE__ */ Object.create(null), n = /* @__PURE__ */ Object.create(null);
  function o(r) {
    if (e[r])
      return n[r] = [];
    if (!(r in n)) {
      n[r] = null;
      const a = s[r] && s[r].parent, l = a && o(a);
      l && (n[r] = [a].concat(l));
    }
    return n[r];
  }
  return Object.keys(e).concat(Object.keys(s)).forEach(o), n;
}
function Oa(i, t, e) {
  const s = i.icons, n = i.aliases || /* @__PURE__ */ Object.create(null);
  let o = {};
  function r(a) {
    o = Un(
      s[a] || n[a],
      o
    );
  }
  return r(t), e.forEach(r), Un(i, o);
}
function Qs(i, t) {
  const e = [];
  if (typeof i != "object" || typeof i.icons != "object")
    return e;
  i.not_found instanceof Array && i.not_found.forEach((n) => {
    t(n, null), e.push(n);
  });
  const s = ka(i);
  for (const n in s) {
    const o = s[n];
    o && (t(n, Oa(i, n, o)), e.push(n));
  }
  return e;
}
var Ta = {
  provider: "",
  aliases: {},
  not_found: {},
  ...Ys
};
function Oi(i, t) {
  for (const e in t)
    if (e in i && typeof i[e] != typeof t[e])
      return false;
  return true;
}
function Ks(i) {
  if (typeof i != "object" || i === null)
    return null;
  const t = i;
  if (typeof t.prefix != "string" || !i.icons || typeof i.icons != "object" || !Oi(i, Ta))
    return null;
  const e = t.icons;
  for (const n in e) {
    const o = e[n];
    if (!n.match(ve) || typeof o.body != "string" || !Oi(
      o,
      ji
    ))
      return null;
  }
  const s = t.aliases || /* @__PURE__ */ Object.create(null);
  for (const n in s) {
    const o = s[n], r = o.parent;
    if (!n.match(ve) || typeof r != "string" || !e[r] && !s[r] || !Oi(
      o,
      ji
    ))
      return null;
  }
  return t;
}
var ai = /* @__PURE__ */ Object.create(null);
function Ia(i, t) {
  return {
    provider: i,
    prefix: t,
    icons: /* @__PURE__ */ Object.create(null),
    missing: /* @__PURE__ */ new Set()
  };
}
function xt(i, t) {
  const e = ai[i] || (ai[i] = /* @__PURE__ */ Object.create(null));
  return e[t] || (e[t] = Ia(i, t));
}
function an(i, t) {
  return Ks(t) ? Qs(t, (e, s) => {
    s ? i.icons[e] = s : i.missing.add(e);
  }) : [];
}
function Pa(i, t, e) {
  try {
    if (typeof e.body == "string")
      return i.icons[t] = { ...e }, true;
  } catch {
  }
  return false;
}
function Ra(i, t) {
  let e = [];
  return (typeof i == "string" ? [i] : Object.keys(ai)).forEach((s) => {
    (typeof s == "string" && typeof t == "string" ? [t] : Object.keys(ai[s] || {})).forEach((n) => {
      const o = xt(s, n);
      e = e.concat(
        Object.keys(o.icons).map(
          (r) => (s !== "" ? "@" + s + ":" : "") + n + ":" + r
        )
      );
    });
  }), e;
}
var Ae = false;
function Zs(i) {
  return typeof i == "boolean" && (Ae = i), Ae;
}
function Se(i) {
  const t = typeof i == "string" ? ze(i, true, Ae) : i;
  if (t) {
    const e = xt(t.provider, t.prefix), s = t.name;
    return e.icons[s] || (e.missing.has(s) ? null : void 0);
  }
}
function to(i, t) {
  const e = ze(i, true, Ae);
  if (!e)
    return false;
  const s = xt(e.provider, e.prefix);
  return Pa(s, e.name, t);
}
function Vn(i, t) {
  if (typeof i != "object")
    return false;
  if (typeof t != "string" && (t = i.provider || ""), Ae && !t && !i.prefix) {
    let n = false;
    return Ks(i) && (i.prefix = "", Qs(i, (o, r) => {
      r && to(o, r) && (n = true);
    })), n;
  }
  const e = i.prefix;
  if (!Qe({
    provider: t,
    prefix: e,
    name: "a"
  }))
    return false;
  const s = xt(t, e);
  return !!an(s, i);
}
function Wn(i) {
  return !!Se(i);
}
function Na(i) {
  const t = Se(i);
  return t ? {
    ...Le,
    ...t
  } : null;
}
function Ma(i) {
  const t = {
    loaded: [],
    missing: [],
    pending: []
  }, e = /* @__PURE__ */ Object.create(null);
  i.sort((n, o) => n.provider !== o.provider ? n.provider.localeCompare(o.provider) : n.prefix !== o.prefix ? n.prefix.localeCompare(o.prefix) : n.name.localeCompare(o.name));
  let s = {
    provider: "",
    prefix: "",
    name: ""
  };
  return i.forEach((n) => {
    if (s.name === n.name && s.prefix === n.prefix && s.provider === n.provider)
      return;
    s = n;
    const o = n.provider, r = n.prefix, a = n.name, l = e[o] || (e[o] = /* @__PURE__ */ Object.create(null)), u = l[r] || (l[r] = xt(o, r));
    let h;
    a in u.icons ? h = t.loaded : r === "" || u.missing.has(a) ? h = t.missing : h = t.pending;
    const d = {
      provider: o,
      prefix: r,
      name: a
    };
    h.push(d);
  }), t;
}
function eo(i, t) {
  i.forEach((e) => {
    const s = e.loaderCallbacks;
    s && (e.loaderCallbacks = s.filter((n) => n.id !== t));
  });
}
function La(i) {
  i.pendingCallbacksFlag || (i.pendingCallbacksFlag = true, setTimeout(() => {
    i.pendingCallbacksFlag = false;
    const t = i.loaderCallbacks ? i.loaderCallbacks.slice(0) : [];
    if (!t.length)
      return;
    let e = false;
    const s = i.provider, n = i.prefix;
    t.forEach((o) => {
      const r = o.icons, a = r.pending.length;
      r.pending = r.pending.filter((l) => {
        if (l.prefix !== n)
          return true;
        const u = l.name;
        if (i.icons[u])
          r.loaded.push({
            provider: s,
            prefix: n,
            name: u
          });
        else if (i.missing.has(u))
          r.missing.push({
            provider: s,
            prefix: n,
            name: u
          });
        else
          return e = true, true;
        return false;
      }), r.pending.length !== a && (e || eo([i], o.id), o.callback(
        r.loaded.slice(0),
        r.missing.slice(0),
        r.pending.slice(0),
        o.abort
      ));
    });
  }));
}
var za = 0;
function Da(i, t, e) {
  const s = za++, n = eo.bind(null, e, s);
  if (!t.pending.length)
    return n;
  const o = {
    id: s,
    icons: t,
    callback: i,
    abort: n
  };
  return e.forEach((r) => {
    (r.loaderCallbacks || (r.loaderCallbacks = [])).push(o);
  }), n;
}
var Hi = /* @__PURE__ */ Object.create(null);
function Gn(i, t) {
  Hi[i] = t;
}
function Fi(i) {
  return Hi[i] || Hi[""];
}
function ja(i, t = true, e = false) {
  const s = [];
  return i.forEach((n) => {
    const o = typeof n == "string" ? ze(n, t, e) : n;
    o && s.push(o);
  }), s;
}
var Ha = {
  resources: [],
  index: 0,
  timeout: 2e3,
  rotate: 750,
  random: false,
  dataAfterTimeout: false
};
function Fa(i, t, e, s) {
  const n = i.resources.length, o = i.random ? Math.floor(Math.random() * n) : i.index;
  let r;
  if (i.random) {
    let C = i.resources.slice(0);
    for (r = []; C.length > 1; ) {
      const E = Math.floor(Math.random() * C.length);
      r.push(C[E]), C = C.slice(0, E).concat(C.slice(E + 1));
    }
    r = r.concat(C);
  } else
    r = i.resources.slice(o).concat(i.resources.slice(0, o));
  const a = Date.now();
  let l = "pending", u = 0, h, d = null, c = [], p = [];
  typeof s == "function" && p.push(s);
  function g() {
    d && (clearTimeout(d), d = null);
  }
  function w() {
    l === "pending" && (l = "aborted"), g(), c.forEach((C) => {
      C.status === "pending" && (C.status = "aborted");
    }), c = [];
  }
  function _(C, E) {
    E && (p = []), typeof C == "function" && p.push(C);
  }
  function x() {
    return {
      startTime: a,
      payload: t,
      status: l,
      queriesSent: u,
      queriesPending: c.length,
      subscribe: _,
      abort: w
    };
  }
  function m() {
    l = "failed", p.forEach((C) => {
      C(void 0, h);
    });
  }
  function b() {
    c.forEach((C) => {
      C.status === "pending" && (C.status = "aborted");
    }), c = [];
  }
  function v(C, E, A) {
    const j = E !== "success";
    switch (c = c.filter((O) => O !== C), l) {
      case "pending":
        break;
      case "failed":
        if (j || !i.dataAfterTimeout)
          return;
        break;
      default:
        return;
    }
    if (E === "abort") {
      h = A, m();
      return;
    }
    if (j) {
      h = A, c.length || (r.length ? $() : m());
      return;
    }
    if (g(), b(), !i.random) {
      const O = i.resources.indexOf(C.resource);
      O !== -1 && O !== i.index && (i.index = O);
    }
    l = "completed", p.forEach((O) => {
      O(A);
    });
  }
  function $() {
    if (l !== "pending")
      return;
    g();
    const C = r.shift();
    if (C === void 0) {
      if (c.length) {
        d = setTimeout(() => {
          g(), l === "pending" && (b(), m());
        }, i.timeout);
        return;
      }
      m();
      return;
    }
    const E = {
      status: "pending",
      resource: C,
      callback: (A, j) => {
        v(E, A, j);
      }
    };
    c.push(E), u++, d = setTimeout($, i.rotate), e(C, t, E.callback);
  }
  return setTimeout($), x;
}
function io(i) {
  const t = {
    ...Ha,
    ...i
  };
  let e = [];
  function s() {
    e = e.filter((r) => r().status === "pending");
  }
  function n(r, a, l) {
    const u = Fa(
      t,
      r,
      a,
      (h, d) => {
        s(), l && l(h, d);
      }
    );
    return e.push(u), u;
  }
  function o(r) {
    return e.find((a) => r(a)) || null;
  }
  return {
    query: n,
    find: o,
    setIndex: (r) => {
      t.index = r;
    },
    getIndex: () => t.index,
    cleanup: s
  };
}
function ln(i) {
  let t;
  if (typeof i.resources == "string")
    t = [i.resources];
  else if (t = i.resources, !(t instanceof Array) || !t.length)
    return null;
  return {
    // API hosts
    resources: t,
    // Root path
    path: i.path || "/",
    // URL length limit
    maxURL: i.maxURL || 500,
    // Timeout before next host is used.
    rotate: i.rotate || 750,
    // Timeout before failing query.
    timeout: i.timeout || 5e3,
    // Randomise default API end point.
    random: i.random === true,
    // Start index
    index: i.index || 0,
    // Receive data after time out (used if time out kicks in first, then API module sends data anyway).
    dataAfterTimeout: i.dataAfterTimeout !== false
  };
}
var bi = /* @__PURE__ */ Object.create(null);
var qe = [
  "https://api.simplesvg.com",
  "https://api.unisvg.com"
];
var Bi = [];
for (; qe.length > 0; )
  qe.length === 1 || Math.random() > 0.5 ? Bi.push(qe.shift()) : Bi.push(qe.pop());
bi[""] = ln({
  resources: ["https://api.iconify.design"].concat(Bi)
});
function qn(i, t) {
  const e = ln(t);
  return e === null ? false : (bi[i] = e, true);
}
function gi(i) {
  return bi[i];
}
function Ba() {
  return Object.keys(bi);
}
function Yn() {
}
var Ti = /* @__PURE__ */ Object.create(null);
function Ua(i) {
  if (!Ti[i]) {
    const t = gi(i);
    if (!t)
      return;
    const e = io(t), s = {
      config: t,
      redundancy: e
    };
    Ti[i] = s;
  }
  return Ti[i];
}
function no(i, t, e) {
  let s, n;
  if (typeof i == "string") {
    const o = Fi(i);
    if (!o)
      return e(void 0, 424), Yn;
    n = o.send;
    const r = Ua(i);
    r && (s = r.redundancy);
  } else {
    const o = ln(i);
    if (o) {
      s = io(o);
      const r = i.resources ? i.resources[0] : "", a = Fi(r);
      a && (n = a.send);
    }
  }
  return !s || !n ? (e(void 0, 424), Yn) : s.query(t, n, e)().abort;
}
var Xn = "iconify2";
var ke2 = "iconify";
var so = ke2 + "-count";
var Jn = ke2 + "-version";
var oo = 36e5;
var Va = 168;
var Wa = 50;
function Ui(i, t) {
  try {
    return i.getItem(t);
  } catch {
  }
}
function cn(i, t, e) {
  try {
    return i.setItem(t, e), true;
  } catch {
  }
}
function Qn(i, t) {
  try {
    i.removeItem(t);
  } catch {
  }
}
function Vi(i, t) {
  return cn(i, so, t.toString());
}
function Wi(i) {
  return parseInt(Ui(i, so)) || 0;
}
var Nt = {
  local: true,
  session: true
};
var ro = {
  local: /* @__PURE__ */ new Set(),
  session: /* @__PURE__ */ new Set()
};
var un = false;
function Ga(i) {
  un = i;
}
var Ye = typeof window > "u" ? {} : window;
function ao(i) {
  const t = i + "Storage";
  try {
    if (Ye && Ye[t] && typeof Ye[t].length == "number")
      return Ye[t];
  } catch {
  }
  Nt[i] = false;
}
function lo(i, t) {
  const e = ao(i);
  if (!e)
    return;
  const s = Ui(e, Jn);
  if (s !== Xn) {
    if (s) {
      const a = Wi(e);
      for (let l = 0; l < a; l++)
        Qn(e, ke2 + l.toString());
    }
    cn(e, Jn, Xn), Vi(e, 0);
    return;
  }
  const n = Math.floor(Date.now() / oo) - Va, o = (a) => {
    const l = ke2 + a.toString(), u = Ui(e, l);
    if (typeof u == "string") {
      try {
        const h = JSON.parse(u);
        if (typeof h == "object" && typeof h.cached == "number" && h.cached > n && typeof h.provider == "string" && typeof h.data == "object" && typeof h.data.prefix == "string" && // Valid item: run callback
        t(h, a))
          return true;
      } catch {
      }
      Qn(e, l);
    }
  };
  let r = Wi(e);
  for (let a = r - 1; a >= 0; a--)
    o(a) || (a === r - 1 ? (r--, Vi(e, r)) : ro[i].add(a));
}
function co() {
  if (!un) {
    Ga(true);
    for (const i in Nt)
      lo(i, (t) => {
        const e = t.data, s = t.provider, n = e.prefix, o = xt(
          s,
          n
        );
        if (!an(o, e).length)
          return false;
        const r = e.lastModified || -1;
        return o.lastModifiedCached = o.lastModifiedCached ? Math.min(o.lastModifiedCached, r) : r, true;
      });
  }
}
function qa(i, t) {
  const e = i.lastModifiedCached;
  if (
    // Matches or newer
    e && e >= t
  )
    return e === t;
  if (i.lastModifiedCached = t, e)
    for (const s in Nt)
      lo(s, (n) => {
        const o = n.data;
        return n.provider !== i.provider || o.prefix !== i.prefix || o.lastModified === t;
      });
  return true;
}
function Ya(i, t) {
  un || co();
  function e(s) {
    let n;
    if (!Nt[s] || !(n = ao(s)))
      return;
    const o = ro[s];
    let r;
    if (o.size)
      o.delete(r = Array.from(o).shift());
    else if (r = Wi(n), r >= Wa || !Vi(n, r + 1))
      return;
    const a = {
      cached: Math.floor(Date.now() / oo),
      provider: i.provider,
      data: t
    };
    return cn(
      n,
      ke2 + r.toString(),
      JSON.stringify(a)
    );
  }
  t.lastModified && !qa(i, t.lastModified) || Object.keys(t.icons).length && (t.not_found && (t = Object.assign({}, t), delete t.not_found), e("local") || e("session"));
}
function Kn() {
}
function Xa(i) {
  i.iconsLoaderFlag || (i.iconsLoaderFlag = true, setTimeout(() => {
    i.iconsLoaderFlag = false, La(i);
  }));
}
function Ja(i, t) {
  i.iconsToLoad ? i.iconsToLoad = i.iconsToLoad.concat(t).sort() : i.iconsToLoad = t, i.iconsQueueFlag || (i.iconsQueueFlag = true, setTimeout(() => {
    i.iconsQueueFlag = false;
    const { provider: e, prefix: s } = i, n = i.iconsToLoad;
    delete i.iconsToLoad;
    let o;
    !n || !(o = Fi(e)) || o.prepare(e, s, n).forEach((r) => {
      no(e, r, (a) => {
        if (typeof a != "object")
          r.icons.forEach((l) => {
            i.missing.add(l);
          });
        else
          try {
            const l = an(
              i,
              a
            );
            if (!l.length)
              return;
            const u = i.pendingIcons;
            u && l.forEach((h) => {
              u.delete(h);
            }), Ya(i, a);
          } catch (l) {
            console.error(l);
          }
        Xa(i);
      });
    });
  }));
}
var hn = (i, t) => {
  const e = ja(i, true, Zs()), s = Ma(e);
  if (!s.pending.length) {
    let l = true;
    return t && setTimeout(() => {
      l && t(
        s.loaded,
        s.missing,
        s.pending,
        Kn
      );
    }), () => {
      l = false;
    };
  }
  const n = /* @__PURE__ */ Object.create(null), o = [];
  let r, a;
  return s.pending.forEach((l) => {
    const { provider: u, prefix: h } = l;
    if (h === a && u === r)
      return;
    r = u, a = h, o.push(xt(u, h));
    const d = n[u] || (n[u] = /* @__PURE__ */ Object.create(null));
    d[h] || (d[h] = []);
  }), s.pending.forEach((l) => {
    const { provider: u, prefix: h, name: d } = l, c = xt(u, h), p = c.pendingIcons || (c.pendingIcons = /* @__PURE__ */ new Set());
    p.has(d) || (p.add(d), n[u][h].push(d));
  }), o.forEach((l) => {
    const { provider: u, prefix: h } = l;
    n[u][h].length && Ja(l, n[u][h]);
  }), t ? Da(t, s, o) : Kn;
};
var Qa = (i) => new Promise((t, e) => {
  const s = typeof i == "string" ? ze(i, true) : i;
  if (!s) {
    e(i);
    return;
  }
  hn([s || i], (n) => {
    if (n.length && s) {
      const o = Se(s);
      if (o) {
        t({
          ...Le,
          ...o
        });
        return;
      }
    }
    e(i);
  });
});
function Ka(i) {
  try {
    const t = typeof i == "string" ? JSON.parse(i) : i;
    if (typeof t.body == "string")
      return {
        ...t
      };
  } catch {
  }
}
function Za(i, t) {
  const e = typeof i == "string" ? ze(i, true, true) : null;
  if (!e) {
    const o = Ka(i);
    return {
      value: i,
      data: o
    };
  }
  const s = Se(e);
  if (s !== void 0 || !e.prefix)
    return {
      value: i,
      name: e,
      data: s
      // could be 'null' -> icon is missing
    };
  const n = hn([e], () => t(i, e, Se(e)));
  return {
    value: i,
    name: e,
    loading: n
  };
}
function Ii(i) {
  return i.hasAttribute("inline");
}
var uo = false;
try {
  uo = navigator.vendor.indexOf("Apple") === 0;
} catch {
}
function tl(i, t) {
  switch (t) {
    case "svg":
    case "bg":
    case "mask":
      return t;
  }
  return t !== "style" && (uo || i.indexOf("<a") === -1) ? "svg" : i.indexOf("currentColor") === -1 ? "bg" : "mask";
}
var el = /(-?[0-9.]*[0-9]+[0-9.]*)/g;
var il = /^-?[0-9.]*[0-9]+[0-9.]*$/g;
function Gi(i, t, e) {
  if (t === 1)
    return i;
  if (e = e || 100, typeof i == "number")
    return Math.ceil(i * t * e) / e;
  if (typeof i != "string")
    return i;
  const s = i.split(el);
  if (s === null || !s.length)
    return i;
  const n = [];
  let o = s.shift(), r = il.test(o);
  for (; ; ) {
    if (r) {
      const a = parseFloat(o);
      isNaN(a) ? n.push(o) : n.push(Math.ceil(a * t * e) / e);
    } else
      n.push(o);
    if (o = s.shift(), o === void 0)
      return n.join("");
    r = !r;
  }
}
function nl(i, t = "defs") {
  let e = "";
  const s = i.indexOf("<" + t);
  for (; s >= 0; ) {
    const n = i.indexOf(">", s), o = i.indexOf("</" + t);
    if (n === -1 || o === -1)
      break;
    const r = i.indexOf(">", o);
    if (r === -1)
      break;
    e += i.slice(n + 1, o).trim(), i = i.slice(0, s).trim() + i.slice(r + 1);
  }
  return {
    defs: e,
    content: i
  };
}
function sl(i, t) {
  return i ? "<defs>" + i + "</defs>" + t : t;
}
function ol(i, t, e) {
  const s = nl(i);
  return sl(s.defs, t + s.content + e);
}
var rl = (i) => i === "unset" || i === "undefined" || i === "none";
function ho(i, t) {
  const e = {
    ...Le,
    ...i
  }, s = {
    ...Xs,
    ...t
  }, n = {
    left: e.left,
    top: e.top,
    width: e.width,
    height: e.height
  };
  let o = e.body;
  [e, s].forEach((w) => {
    const _ = [], x = w.hFlip, m = w.vFlip;
    let b = w.rotate;
    x ? m ? b += 2 : (_.push(
      "translate(" + (n.width + n.left).toString() + " " + (0 - n.top).toString() + ")"
    ), _.push("scale(-1 1)"), n.top = n.left = 0) : m && (_.push(
      "translate(" + (0 - n.left).toString() + " " + (n.height + n.top).toString() + ")"
    ), _.push("scale(1 -1)"), n.top = n.left = 0);
    let v;
    switch (b < 0 && (b -= Math.floor(b / 4) * 4), b = b % 4, b) {
      case 1:
        v = n.height / 2 + n.top, _.unshift(
          "rotate(90 " + v.toString() + " " + v.toString() + ")"
        );
        break;
      case 2:
        _.unshift(
          "rotate(180 " + (n.width / 2 + n.left).toString() + " " + (n.height / 2 + n.top).toString() + ")"
        );
        break;
      case 3:
        v = n.width / 2 + n.left, _.unshift(
          "rotate(-90 " + v.toString() + " " + v.toString() + ")"
        );
        break;
    }
    b % 2 === 1 && (n.left !== n.top && (v = n.left, n.left = n.top, n.top = v), n.width !== n.height && (v = n.width, n.width = n.height, n.height = v)), _.length && (o = ol(
      o,
      '<g transform="' + _.join(" ") + '">',
      "</g>"
    ));
  });
  const r = s.width, a = s.height, l = n.width, u = n.height;
  let h, d;
  r === null ? (d = a === null ? "1em" : a === "auto" ? u : a, h = Gi(d, l / u)) : (h = r === "auto" ? l : r, d = a === null ? Gi(h, u / l) : a === "auto" ? u : a);
  const c = {}, p = (w, _) => {
    rl(_) || (c[w] = _.toString());
  };
  p("width", h), p("height", d);
  const g = [n.left, n.top, l, u];
  return c.viewBox = g.join(" "), {
    attributes: c,
    viewBox: g,
    body: o
  };
}
function dn2(i, t) {
  let e = i.indexOf("xlink:") === -1 ? "" : ' xmlns:xlink="http://www.w3.org/1999/xlink"';
  for (const s in t)
    e += " " + s + '="' + t[s] + '"';
  return '<svg xmlns="http://www.w3.org/2000/svg"' + e + ">" + i + "</svg>";
}
function al(i) {
  return i.replace(/"/g, "'").replace(/%/g, "%25").replace(/#/g, "%23").replace(/</g, "%3C").replace(/>/g, "%3E").replace(/\s+/g, " ");
}
function ll(i) {
  return "data:image/svg+xml," + al(i);
}
function po(i) {
  return 'url("' + ll(i) + '")';
}
var cl = () => {
  let i;
  try {
    if (i = fetch, typeof i == "function")
      return i;
  } catch {
  }
};
var li = cl();
function ul(i) {
  li = i;
}
function hl() {
  return li;
}
function dl(i, t) {
  const e = gi(i);
  if (!e)
    return 0;
  let s;
  if (!e.maxURL)
    s = 0;
  else {
    let n = 0;
    e.resources.forEach((r) => {
      n = Math.max(n, r.length);
    });
    const o = t + ".json?icons=";
    s = e.maxURL - n - e.path.length - o.length;
  }
  return s;
}
function pl(i) {
  return i === 404;
}
var fl = (i, t, e) => {
  const s = [], n = dl(i, t), o = "icons";
  let r = {
    type: o,
    provider: i,
    prefix: t,
    icons: []
  }, a = 0;
  return e.forEach((l, u) => {
    a += l.length + 1, a >= n && u > 0 && (s.push(r), r = {
      type: o,
      provider: i,
      prefix: t,
      icons: []
    }, a = l.length), r.icons.push(l);
  }), s.push(r), s;
};
function ml(i) {
  if (typeof i == "string") {
    const t = gi(i);
    if (t)
      return t.path;
  }
  return "/";
}
var bl = (i, t, e) => {
  if (!li) {
    e("abort", 424);
    return;
  }
  let s = ml(t.provider);
  switch (t.type) {
    case "icons": {
      const o = t.prefix, r = t.icons.join(","), a = new URLSearchParams({
        icons: r
      });
      s += o + ".json?" + a.toString();
      break;
    }
    case "custom": {
      const o = t.uri;
      s += o.slice(0, 1) === "/" ? o.slice(1) : o;
      break;
    }
    default:
      e("abort", 400);
      return;
  }
  let n = 503;
  li(i + s).then((o) => {
    const r = o.status;
    if (r !== 200) {
      setTimeout(() => {
        e(pl(r) ? "abort" : "next", r);
      });
      return;
    }
    return n = 501, o.json();
  }).then((o) => {
    if (typeof o != "object" || o === null) {
      setTimeout(() => {
        o === 404 ? e("abort", o) : e("next", n);
      });
      return;
    }
    setTimeout(() => {
      e("success", o);
    });
  }).catch(() => {
    e("next", n);
  });
};
var gl = {
  prepare: fl,
  send: bl
};
function Zn(i, t) {
  switch (i) {
    case "local":
    case "session":
      Nt[i] = t;
      break;
    case "all":
      for (const e in Nt)
        Nt[e] = t;
      break;
  }
}
var Pi = "data-style";
var fo = "";
function vl(i) {
  fo = i;
}
function ts(i, t) {
  let e = Array.from(i.childNodes).find((s) => s.hasAttribute && s.hasAttribute(Pi));
  e || (e = document.createElement("style"), e.setAttribute(Pi, Pi), i.appendChild(e)), e.textContent = ":host{display:inline-block;vertical-align:" + (t ? "-0.125em" : "0") + "}span,svg{display:block}" + fo;
}
function mo() {
  Gn("", gl), Zs(true);
  let i;
  try {
    i = window;
  } catch {
  }
  if (i) {
    if (co(), i.IconifyPreload !== void 0) {
      const t = i.IconifyPreload, e = "Invalid IconifyPreload syntax.";
      typeof t == "object" && t !== null && (t instanceof Array ? t : [t]).forEach((s) => {
        try {
          (typeof s != "object" || s === null || s instanceof Array || // Check for 'icons' and 'prefix'
          typeof s.icons != "object" || typeof s.prefix != "string" || // Add icon set
          !Vn(s)) && console.error(e);
        } catch {
          console.error(e);
        }
      });
    }
    if (i.IconifyProviders !== void 0) {
      const t = i.IconifyProviders;
      if (typeof t == "object" && t !== null)
        for (const e in t) {
          const s = "IconifyProviders[" + e + "] is invalid.";
          try {
            const n = t[e];
            if (typeof n != "object" || !n || n.resources === void 0)
              continue;
            qn(e, n) || console.error(s);
          } catch {
            console.error(s);
          }
        }
    }
  }
  return {
    enableCache: (t) => Zn(t, true),
    disableCache: (t) => Zn(t, false),
    iconLoaded: Wn,
    iconExists: Wn,
    getIcon: Na,
    listIcons: Ra,
    addIcon: to,
    addCollection: Vn,
    calculateSize: Gi,
    buildIcon: ho,
    iconToHTML: dn2,
    svgToURL: po,
    loadIcons: hn,
    loadIcon: Qa,
    addAPIProvider: qn,
    appendCustomStyle: vl,
    _api: {
      getAPIConfig: gi,
      setAPIModule: Gn,
      sendAPIQuery: no,
      setFetch: ul,
      getFetch: hl,
      listAPIProviders: Ba
    }
  };
}
var qi = {
  "background-color": "currentColor"
};
var bo = {
  "background-color": "transparent"
};
var es = {
  image: "var(--svg)",
  repeat: "no-repeat",
  size: "100% 100%"
};
var is = {
  "-webkit-mask": qi,
  mask: qi,
  background: bo
};
for (const i in is) {
  const t = is[i];
  for (const e in es)
    t[i + "-" + e] = es[e];
}
function ns(i) {
  return i ? i + (i.match(/^[-0-9.]+$/) ? "px" : "") : "inherit";
}
function yl(i, t, e) {
  const s = document.createElement("span");
  let n = i.body;
  n.indexOf("<a") !== -1 && (n += "<!-- " + Date.now() + " -->");
  const o = i.attributes, r = dn2(n, {
    ...o,
    width: t.width + "",
    height: t.height + ""
  }), a = po(r), l = s.style, u = {
    "--svg": a,
    width: ns(o.width),
    height: ns(o.height),
    ...e ? qi : bo
  };
  for (const h in u)
    l.setProperty(h, u[h]);
  return s;
}
var ye;
function _l() {
  try {
    ye = window.trustedTypes.createPolicy("iconify", {
      // eslint-disable-next-line @typescript-eslint/no-unsafe-return
      createHTML: (i) => i
    });
  } catch {
    ye = null;
  }
}
function xl(i) {
  return ye === void 0 && _l(), ye ? ye.createHTML(i) : i;
}
function wl(i) {
  const t = document.createElement("span"), e = i.attributes;
  let s = "";
  e.width || (s = "width: inherit;"), e.height || (s += "height: inherit;"), s && (e.style = s);
  const n = dn2(i.body, e);
  return t.innerHTML = xl(n), t.firstChild;
}
function Yi(i) {
  return Array.from(i.childNodes).find((t) => {
    const e = t.tagName && t.tagName.toUpperCase();
    return e === "SPAN" || e === "SVG";
  });
}
function ss(i, t) {
  const e = t.icon.data, s = t.customisations, n = ho(e, s);
  s.preserveAspectRatio && (n.attributes.preserveAspectRatio = s.preserveAspectRatio);
  const o = t.renderedMode;
  let r;
  switch (o) {
    case "svg":
      r = wl(n);
      break;
    default:
      r = yl(n, {
        ...Le,
        ...e
      }, o === "mask");
  }
  const a = Yi(i);
  a ? r.tagName === "SPAN" && a.tagName === r.tagName ? a.setAttribute("style", r.getAttribute("style")) : i.replaceChild(r, a) : i.appendChild(r);
}
function os(i, t, e) {
  const s = e && (e.rendered ? e : e.lastRender);
  return {
    rendered: false,
    inline: t,
    icon: i,
    lastRender: s
  };
}
function $l(i = "iconify-icon") {
  let t, e;
  try {
    t = window.customElements, e = window.HTMLElement;
  } catch {
    return;
  }
  if (!t || !e)
    return;
  const s = t.get(i);
  if (s)
    return s;
  const n = [
    // Icon
    "icon",
    // Mode
    "mode",
    "inline",
    "observe",
    // Customisations
    "width",
    "height",
    "rotate",
    "flip"
  ], o = class extends e {
    /**
     * Constructor
     */
    constructor() {
      super(), Ot(this, "_shadowRoot"), Ot(this, "_initialised", false), Ot(this, "_state"), Ot(this, "_checkQueued", false), Ot(this, "_connected", false), Ot(this, "_observer", null), Ot(this, "_visible", true);
      const a = this._shadowRoot = this.attachShadow({
        mode: "open"
      }), l = Ii(this);
      ts(a, l), this._state = os({
        value: ""
      }, l), this._queueCheck();
    }
    /**
     * Connected to DOM
     */
    connectedCallback() {
      this._connected = true, this.startObserver();
    }
    /**
     * Disconnected from DOM
     */
    disconnectedCallback() {
      this._connected = false, this.stopObserver();
    }
    /**
     * Observed attributes
     */
    static get observedAttributes() {
      return n.slice(0);
    }
    /**
     * Observed properties that are different from attributes
     *
     * Experimental! Need to test with various frameworks that support it
     */
    /*
    static get properties() {
        return {
            inline: {
                type: Boolean,
                reflect: true,
            },
            // Not listing other attributes because they are strings or combination
            // of string and another type. Cannot have multiple types
        };
    }
    */
    /**
     * Attribute has changed
     */
    attributeChangedCallback(a) {
      switch (a) {
        case "inline": {
          const l = Ii(this), u = this._state;
          l !== u.inline && (u.inline = l, ts(this._shadowRoot, l));
          break;
        }
        case "observer": {
          this.observer ? this.startObserver() : this.stopObserver();
          break;
        }
        default:
          this._queueCheck();
      }
    }
    /**
     * Get/set icon
     */
    get icon() {
      const a = this.getAttribute("icon");
      if (a && a.slice(0, 1) === "{")
        try {
          return JSON.parse(a);
        } catch {
        }
      return a;
    }
    set icon(a) {
      typeof a == "object" && (a = JSON.stringify(a)), this.setAttribute("icon", a);
    }
    /**
     * Get/set inline
     */
    get inline() {
      return Ii(this);
    }
    set inline(a) {
      a ? this.setAttribute("inline", "true") : this.removeAttribute("inline");
    }
    /**
     * Get/set observer
     */
    get observer() {
      return this.hasAttribute("observer");
    }
    set observer(a) {
      a ? this.setAttribute("observer", "true") : this.removeAttribute("observer");
    }
    /**
     * Restart animation
     */
    restartAnimation() {
      const a = this._state;
      if (a.rendered) {
        const l = this._shadowRoot;
        if (a.renderedMode === "svg")
          try {
            l.lastChild.setCurrentTime(0);
            return;
          } catch {
          }
        ss(l, a);
      }
    }
    /**
     * Get status
     */
    get status() {
      const a = this._state;
      return a.rendered ? "rendered" : a.icon.data === null ? "failed" : "loading";
    }
    /**
     * Queue attributes re-check
     */
    _queueCheck() {
      this._checkQueued || (this._checkQueued = true, setTimeout(() => {
        this._check();
      }));
    }
    /**
     * Check for changes
     */
    _check() {
      if (!this._checkQueued)
        return;
      this._checkQueued = false;
      const a = this._state, l = this.getAttribute("icon");
      if (l !== a.icon.value) {
        this._iconChanged(l);
        return;
      }
      if (!a.rendered || !this._visible)
        return;
      const u = this.getAttribute("mode"), h = Bn2(this);
      (a.attrMode !== u || Aa(a.customisations, h) || !Yi(this._shadowRoot)) && this._renderIcon(a.icon, h, u);
    }
    /**
     * Icon value has changed
     */
    _iconChanged(a) {
      const l = Za(a, (u, h, d) => {
        const c = this._state;
        if (c.rendered || this.getAttribute("icon") !== u)
          return;
        const p = {
          value: u,
          name: h,
          data: d
        };
        p.data ? this._gotIconData(p) : c.icon = p;
      });
      l.data ? this._gotIconData(l) : this._state = os(l, this._state.inline, this._state);
    }
    /**
     * Force render icon on state change
     */
    _forceRender() {
      if (!this._visible) {
        const a = Yi(this._shadowRoot);
        a && this._shadowRoot.removeChild(a);
        return;
      }
      this._queueCheck();
    }
    /**
     * Got new icon data, icon is ready to (re)render
     */
    _gotIconData(a) {
      this._checkQueued = false, this._renderIcon(a, Bn2(this), this.getAttribute("mode"));
    }
    /**
     * Re-render based on icon data
     */
    _renderIcon(a, l, u) {
      const h = tl(a.data.body, u), d = this._state.inline;
      ss(this._shadowRoot, this._state = {
        rendered: true,
        icon: a,
        inline: d,
        customisations: l,
        attrMode: u,
        renderedMode: h
      });
    }
    /**
     * Start observer
     */
    startObserver() {
      if (!this._observer)
        try {
          this._observer = new IntersectionObserver((a) => {
            const l = a.some((u) => u.isIntersecting);
            l !== this._visible && (this._visible = l, this._forceRender());
          }), this._observer.observe(this);
        } catch {
          if (this._observer) {
            try {
              this._observer.disconnect();
            } catch {
            }
            this._observer = null;
          }
        }
    }
    /**
     * Stop observer
     */
    stopObserver() {
      this._observer && (this._observer.disconnect(), this._observer = null, this._visible = true, this._connected && this._forceRender());
    }
  };
  n.forEach((a) => {
    a in o.prototype || Object.defineProperty(o.prototype, a, {
      get: function() {
        return this.getAttribute(a);
      },
      set: function(l) {
        l !== null ? this.setAttribute(a, l) : this.removeAttribute(a);
      }
    });
  });
  const r = mo();
  for (const a in r)
    o[a] = o.prototype[a] = r[a];
  return t.define(i, o), o;
}
$l() || mo();
var Cl = Object.defineProperty;
var st = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Cl(t, e, n), n;
};
var Xe;
var Z = (Xe = class extends k {
  constructor() {
    super(), this.labelHidden = false, this.active = false, this.disabled = false, this.vertical = false, this.tooltipVisible = false, this._parent = gt(), this._tooltip = gt(), this._contextMenu = gt(), this._mouseLeave = false, this.onWindowMouseUp = (i) => {
      const { value: t } = this._contextMenu;
      !this.contains(i.target) && t && (t.visible = false);
    }, this.mouseLeave = true, this.addEventListener("click", (i) => i.stopPropagation());
  }
  set mouseLeave(i) {
    this._mouseLeave = i, i && (this.tooltipVisible = false, clearTimeout(this.timeoutID));
  }
  get mouseLeave() {
    return this._mouseLeave;
  }
  computeTooltipPosition() {
    const { value: i } = this._parent, { value: t } = this._tooltip;
    i && t && Hs(i, t, {
      placement: "bottom",
      middleware: [Ss(10), js(), Ds(), zs({ padding: 5 })]
    }).then((e) => {
      const { x: s, y: n } = e;
      Object.assign(t.style, {
        left: `${s}px`,
        top: `${n}px`
      });
    });
  }
  onMouseEnter() {
    if (!(this.tooltipTitle || this.tooltipText))
      return;
    this.mouseLeave = false;
    const i = this.tooltipTime ?? 700;
    this.timeoutID = setTimeout(() => {
      this.mouseLeave || (this.computeTooltipPosition(), this.tooltipVisible = true);
    }, i);
  }
  onChildrenClick(i) {
    i.stopPropagation();
    const { value: t } = this._contextMenu;
    t && (t.visible = !t.visible);
  }
  onSlotChange(i) {
    const { value: t } = this._contextMenu, e = i.target.assignedElements();
    for (const s of e) {
      if (!(s instanceof Xe)) {
        s.remove(), console.warn(
          "Only bim-button is allowed inside bim-button. Child has been removed."
        );
        continue;
      }
      s.addEventListener("click", () => t == null ? void 0 : t.updatePosition());
    }
    this.requestUpdate();
  }
  connectedCallback() {
    super.connectedCallback(), window.addEventListener("mouseup", this.onWindowMouseUp);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), window.removeEventListener("mouseup", this.onWindowMouseUp);
  }
  render() {
    const i = y`
      <div ${Q(this._tooltip)} class="tooltip">
        ${this.tooltipTitle ? y`<p style="text-wrap: nowrap;">
              <strong>${this.tooltipTitle}</strong>
            </p>` : null}
        ${this.tooltipText ? y`<p style="width: 9rem;">${this.tooltipText}</p>` : null}
      </div>
    `, t = this.children.length > 0;
    return y`
      <style>
        .button {
          border-radius: var(
            --bim-button--bdrs,
            ${t ? "var(--bim-ui_size-4xs) 0 0 var(--bim-ui_size-4xs)" : "var(--bim-ui_size-4xs)"}
          );
        }
        .children {
          border-radius: var(
            --bim-button--bdrs,
            ${t ? "0 var(--bim-ui_size-4xs) var(--bim-ui_size-4xs) 0" : "var(--bim-ui_size-4xs)"}
          );
        }
      </style>
      <div ${Q(this._parent)} class="parent">
        ${this.label || this.icon ? y`
              <div
                class="button"
                @mouseenter=${this.onMouseEnter}
                @mouseleave=${() => this.mouseLeave = true}
              >
                <bim-label
                  .icon=${this.icon}
                  .vertical=${this.vertical}
                  .labelHidden=${this.labelHidden}
                  >${this.label}</bim-label
                >
              </div>
            ` : null}
        ${this.tooltipTitle || this.tooltipText ? i : null}
        ${t ? y`
              <div class="children" @click=${this.onChildrenClick}>
                <bim-icon .icon=${"ic:round-plus"}></bim-icon>
              </div>
            ` : null}
        <bim-context-menu
          ${Q(this._contextMenu)}
          style="row-gap: var(--bim-ui_size-4xs)"
        >
          <slot @slotchange=${this.onSlotChange}></slot>
        </bim-context-menu>
      </div>
    `;
  }
}, Xe.styles = T`
    :host {
      --bim-label--c: var(--bim-ui_bg-contrast-100);
      display: block;
      flex: 1;
      pointer-events: none;
    }

    :host(:not([disabled]):hover) {
      cursor: pointer;
    }

    bim-label {
      pointer-events: none;
    }

    .parent {
      --bim-label--fz: var(--bim-ui_size-xs);
      --bim-icon--c: var(--bim-label--c);
      display: flex;
      height: 100%;
      user-select: none;
      row-gap: 0.125rem;
      column-gap: 0.125rem;
    }

    .button,
    .children {
      box-sizing: border-box;
      display: flex;
      align-items: center;
      justify-content: center;
      pointer-events: auto;
      min-height: var(--bim-ui_size-5xl);
      min-width: var(--bim-ui_size-5xl);
      background-color: var(--bim-button--bgc, var(--bim-ui_bg-contrast-20));
      outline: var(--bim-button--olw) solid var(--bim-button--olc);
    }

    .button {
      flex-grow: 1;
    }

    :host(:not([label-hidden])[label]) .button {
      justify-content: var(--bim-button--jc, center);
    }

    :host(:hover) .button,
    :host(:hover) .children {
      --bim-label--c: var(--bim-ui_main-contrast);
      --bim-icon--c: var(--bim-ui_main-contrast);
      fill: white;
      background-color: var(--bim-ui_main-base);
    }

    :host(:not([label]):not([icon])) .children {
      flex: 1;
    }

    :host([active]) .button {
      --bim-label--c: var(--bim-ui_main-contrast);
      --bim-icon--c: var(--bim-ui_main-contrast);
      background-color: var(--bim-ui_main-base);
    }

    :host([vertical]) .parent {
      justify-content: center;
    }

    :host(:not([label-hidden])[label]) .button {
      padding: 0 0.5rem;
    }

    :host([disabled]) .parent {
      background-color: gray;
    }

    .children {
      --bim-icon--fz: var(--bim-ui_size-base);
      padding: 0 0.125rem;
    }

    ::slotted(bim-button) {
      --bim-icon--fz: var(--bim-ui_size-base);
      --bim-button--bgc: var(
        --bim-context-menu--bgc,
        var(--bim-ui_bg-contrast-20)
      );
      --bim-button--bdrs: var(--bim-ui_size-4xs);
      --bim-button--olw: 0;
      --bim-button--olc: transparent;
    }

    .tooltip {
      position: absolute;
      padding: 0.75rem;
      z-index: 99;
      display: flex;
      flex-flow: column;
      row-gap: 0.375rem;
      box-shadow: 0 0 10px 3px rgba(0 0 0 / 20%);
      outline: 1px solid var(--bim-ui_bg-contrast-40);
      font-size: var(--bim-ui_size-xs);
      border-radius: var(--bim-ui_size-4xs);
      background-color: var(--bim-ui_bg-contrast-20);
      color: var(--bim-ui_bg-contrast-100);
    }

    .tooltip p {
      margin: 0;
      padding: 0;
    }

    :host(:not([tooltip-visible])) .tooltip {
      display: none;
    }
  `, Xe);
st([
  f({ type: String, reflect: true })
], Z.prototype, "label");
st([
  f({ type: Boolean, attribute: "label-hidden", reflect: true })
], Z.prototype, "labelHidden");
st([
  f({ type: Boolean, reflect: true })
], Z.prototype, "active");
st([
  f({ type: Boolean, reflect: true, attribute: "disabled" })
], Z.prototype, "disabled");
st([
  f({ type: String, reflect: true })
], Z.prototype, "icon");
st([
  f({ type: Boolean, reflect: true })
], Z.prototype, "vertical");
st([
  f({ type: Number, attribute: "tooltip-time", reflect: true })
], Z.prototype, "tooltipTime");
st([
  f({ type: Boolean, attribute: "tooltip-visible", reflect: true })
], Z.prototype, "tooltipVisible");
st([
  f({ type: String, attribute: "tooltip-title", reflect: true })
], Z.prototype, "tooltipTitle");
st([
  f({ type: String, attribute: "tooltip-text", reflect: true })
], Z.prototype, "tooltipText");
var El = Z;
var Al = Object.defineProperty;
var De = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Al(t, e, n), n;
};
var go = class extends k {
  constructor() {
    super(...arguments), this.checked = false, this.inverted = false, this.onValueChange = new Event("change");
  }
  /**
   * A getter that returns the current checked state of the checkbox. This is useful for retrieving the checkbox's value in form submissions or JavaScript interactions as it provides a consistent `value` property as many other components.
   * @type {boolean}
   * @default false
   * @example <script>console.log(document.querySelector('bim-checkbox').value);<\/script>
   * @example
   * const checkbox = document.createElement('bim-checkbox');
   * document.body.appendChild(checkbox);
   * console.log(checkbox.value); // false initially
   */
  get value() {
    return this.checked;
  }
  onChange(t) {
    t.stopPropagation(), this.checked = t.target.checked, this.dispatchEvent(this.onValueChange);
  }
  render() {
    return y`
      <div class="parent">
        ${this.label ? y`<bim-label .icon="${this.icon}">${this.label}</bim-label> ` : null}
        <input
          type="checkbox"
          aria-label=${this.label || this.name || "Checkbox Input"}
          @change="${this.onChange}"
          .checked="${this.checked}"
        />
      </div>
    `;
  }
};
go.styles = T`
    :host {
      display: block;
    }

    .parent {
      display: flex;
      justify-content: space-between;
      height: 1.75rem;
      column-gap: 0.5rem;
      width: 100%;
      align-items: center;
    }

    :host([inverted]) .parent {
      flex-direction: row-reverse;
      justify-content: start;
    }

    input {
      height: 1rem;
      width: 1rem;
      cursor: pointer;
      border: none;
      outline: none;
      accent-color: var(--bim-checkbox--c, var(--bim-ui_main-base));
    }

    input:focus {
      outline: var(--bim-checkbox--olw, 2px) solid
        var(--bim-checkbox--olc, var(--bim-ui_accent-base));
    }
  `;
var ie = go;
De([
  f({ type: String, reflect: true })
], ie.prototype, "icon");
De([
  f({ type: String, reflect: true })
], ie.prototype, "name");
De([
  f({ type: String, reflect: true })
], ie.prototype, "label");
De([
  f({ type: Boolean, reflect: true })
], ie.prototype, "checked");
De([
  f({ type: Boolean, reflect: true })
], ie.prototype, "inverted");
var Sl = Object.defineProperty;
var ne = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Sl(t, e, n), n;
};
var vo = class extends k {
  constructor() {
    super(...arguments), this.vertical = false, this.color = "#bcf124", this._colorInput = gt(), this._textInput = gt(), this.onValueChange = new Event("input"), this.onOpacityInput = (t) => {
      const e = t.target;
      this.opacity = e.value, this.dispatchEvent(this.onValueChange);
    };
  }
  /**
   * Represents both the color and opacity values combined into a single object. This is an instance property, not an HTMLElement attribute.
   * @type {Object}
   * @example
   * const colorInput = document.createElement('bim-color-input');
   * colorInput.value = { color: '#ff0000', opacity: 0.5 };
   */
  set value(t) {
    const { color: e, opacity: s } = t;
    this.color = e, s && (this.opacity = s);
  }
  get value() {
    const t = {
      color: this.color
    };
    return this.opacity && (t.opacity = this.opacity), t;
  }
  onColorInput(t) {
    t.stopPropagation();
    const { value: e } = this._colorInput;
    e && (this.color = e.value, this.dispatchEvent(this.onValueChange));
  }
  onTextInput(t) {
    t.stopPropagation();
    const { value: e } = this._textInput;
    if (!e)
      return;
    const { value: s } = e;
    let n = s.replace(/[^a-fA-F0-9]/g, "");
    n.startsWith("#") || (n = `#${n}`), e.value = n.slice(0, 7), e.value.length === 7 && (this.color = e.value, this.dispatchEvent(this.onValueChange));
  }
  /**
   * Focuses on the color input by programmatically triggering a click event on the underlying color input element.
   * If the color input element is not available, the function does nothing.
   */
  focus() {
    const { value: t } = this._colorInput;
    t && t.click();
  }
  render() {
    return y`
      <div class="parent">
        <bim-input
          .label=${this.label}
          .icon=${this.icon}
          .vertical="${this.vertical}"
        >
          <div class="color-container">
            <div
              style="display: flex; align-items: center; gap: .375rem; height: 100%; flex: 1; padding: 0 0.5rem;"
            >
              <input
                ${Q(this._colorInput)}
                @input="${this.onColorInput}"
                type="color"
                aria-label=${this.label || this.name || "Color Input"}
                value="${this.color}"
              />
              <div
                @click=${this.focus}
                class="sample"
                style="background-color: ${this.color}"
              ></div>
              <input
                ${Q(this._textInput)}
                @input="${this.onTextInput}"
                value="${this.color}"
                type="text"
                aria-label=${this.label || this.name || "Text Color Input"}
              />
            </div>
            ${this.opacity !== void 0 ? y`<bim-number-input
                  @change=${this.onOpacityInput}
                  slider
                  suffix="%"
                  min="0"
                  value=${this.opacity}
                  max="100"
                ></bim-number-input>` : null}
          </div>
        </bim-input>
      </div>
    `;
  }
};
vo.styles = T`
    :host {
      --bim-input--bgc: var(--bim-ui_bg-contrast-20);
      flex: 1;
      display: block;
    }

    :host(:focus) {
      --bim-input--olw: var(--bim-number-input--olw, 2px);
      --bim-input--olc: var(--bim-ui_accent-base);
    }

    .parent {
      display: flex;
      gap: 0.375rem;
    }

    .color-container {
      position: relative;
      outline: none;
      display: flex;
      height: 100%;
      gap: 0.5rem;
      justify-content: flex-start;
      align-items: center;
      flex: 1;
      border-radius: var(--bim-color-input--bdrs, var(--bim-ui_size-4xs));
    }

    .color-container input[type="color"] {
      position: absolute;
      bottom: -0.25rem;
      visibility: hidden;
      width: 0;
      height: 0;
    }

    .color-container .sample {
      width: 1rem;
      height: 1rem;
      border-radius: 0.125rem;
      background-color: #fff;
    }

    .color-container input[type="text"] {
      height: 100%;
      flex: 1;
      width: 3.25rem;
      text-transform: uppercase;
      font-size: 0.75rem;
      background-color: transparent;
      padding: 0%;
      outline: none;
      border: none;
      color: var(--bim-color-input--c, var(--bim-ui_bg-contrast-100));
    }

    bim-number-input {
      flex-grow: 0;
    }
  `;
var Dt = vo;
ne([
  f({ type: String, reflect: true })
], Dt.prototype, "name");
ne([
  f({ type: String, reflect: true })
], Dt.prototype, "label");
ne([
  f({ type: String, reflect: true })
], Dt.prototype, "icon");
ne([
  f({ type: Boolean, reflect: true })
], Dt.prototype, "vertical");
ne([
  f({ type: Number, reflect: true })
], Dt.prototype, "opacity");
ne([
  f({ type: String, reflect: true })
], Dt.prototype, "color");
var kl = T`
  ::-webkit-scrollbar {
    width: 0.4rem;
    height: 0.4rem;
    overflow: hidden;
  }

  ::-webkit-scrollbar-thumb {
    border-radius: 0.25rem;
    background-color: var(
      --bim-scrollbar--c,
      color-mix(in lab, var(--bim-ui_main-base), white 15%)
    );
  }

  ::-webkit-scrollbar-track {
    background-color: var(--bim-scrollbar--bgc, var(--bim-ui_bg-base));
  }
`;
var Ol = T`
  :root {
    /* Grayscale Colors */
    --bim-ui_gray-0: hsl(210 10% 5%);
    --bim-ui_gray-1: hsl(210 10% 10%);
    --bim-ui_gray-2: hsl(210 10% 20%);
    --bim-ui_gray-4: hsl(210 10% 40%);
    --bim-ui_gray-6: hsl(210 10% 60%);
    --bim-ui_gray-8: hsl(210 10% 80%);
    --bim-ui_gray-9: hsl(210 10% 90%);
    --bim-ui_gray-10: hsl(210 10% 95%);

    /* Brand Colors */
    --bim-ui_main-base: #6528d7;
    --bim-ui_accent-base: #bcf124;

    /* Brand Colors Contrasts */
    --bim-ui_main-contrast: var(--bim-ui_gray-10);
    --bim-ui_accent-contrast: var(--bim-ui_gray-0);

    /* Sizes */
    --bim-ui_size-4xs: 0.375rem;
    --bim-ui_size-3xs: 0.5rem;
    --bim-ui_size-2xs: 0.625rem;
    --bim-ui_size-xs: 0.75rem;
    --bim-ui_size-sm: 0.875rem;
    --bim-ui_size-base: 1rem;
    --bim-ui_size-lg: 1.125rem;
    --bim-ui_size-xl: 1.25rem;
    --bim-ui_size-2xl: 1.375rem;
    --bim-ui_size-3xl: 1.5rem;
    --bim-ui_size-4xl: 1.625rem;
    --bim-ui_size-5xl: 1.75rem;
    --bim-ui_size-6xl: 1.875rem;
    --bim-ui_size-7xl: 2rem;
    --bim-ui_size-8xl: 2.125rem;
    --bim-ui_size-9xl: 2.25rem;
  }

  /* Background Colors */
  @media (prefers-color-scheme: dark) {
    :root {
      --bim-ui_bg-base: var(--bim-ui_gray-0);
      --bim-ui_bg-contrast-10: var(--bim-ui_gray-1);
      --bim-ui_bg-contrast-20: var(--bim-ui_gray-2);
      --bim-ui_bg-contrast-40: var(--bim-ui_gray-4);
      --bim-ui_bg-contrast-60: var(--bim-ui_gray-6);
      --bim-ui_bg-contrast-80: var(--bim-ui_gray-8);
      --bim-ui_bg-contrast-100: var(--bim-ui_gray-10);
    }
  }

  @media (prefers-color-scheme: light) {
    :root {
      --bim-ui_bg-base: var(--bim-ui_gray-10);
      --bim-ui_bg-contrast-10: var(--bim-ui_gray-9);
      --bim-ui_bg-contrast-20: var(--bim-ui_gray-8);
      --bim-ui_bg-contrast-40: var(--bim-ui_gray-6);
      --bim-ui_bg-contrast-60: var(--bim-ui_gray-4);
      --bim-ui_bg-contrast-80: var(--bim-ui_gray-2);
      --bim-ui_bg-contrast-100: var(--bim-ui_gray-0);
      --bim-ui_accent-base: #6528d7;
    }
  }

  html.bim-ui-dark {
    --bim-ui_bg-base: var(--bim-ui_gray-0);
    --bim-ui_bg-contrast-10: var(--bim-ui_gray-1);
    --bim-ui_bg-contrast-20: var(--bim-ui_gray-2);
    --bim-ui_bg-contrast-40: var(--bim-ui_gray-4);
    --bim-ui_bg-contrast-60: var(--bim-ui_gray-6);
    --bim-ui_bg-contrast-80: var(--bim-ui_gray-8);
    --bim-ui_bg-contrast-100: var(--bim-ui_gray-10);
  }

  html.bim-ui-light {
    --bim-ui_bg-base: var(--bim-ui_gray-10);
    --bim-ui_bg-contrast-10: var(--bim-ui_gray-9);
    --bim-ui_bg-contrast-20: var(--bim-ui_gray-8);
    --bim-ui_bg-contrast-40: var(--bim-ui_gray-6);
    --bim-ui_bg-contrast-60: var(--bim-ui_gray-4);
    --bim-ui_bg-contrast-80: var(--bim-ui_gray-2);
    --bim-ui_bg-contrast-100: var(--bim-ui_gray-0);
    --bim-ui_accent-base: #6528d7;
  }
`;
var jt = {
  scrollbar: kl,
  globalStyles: Ol
};
var Tl = Object.defineProperty;
var Il = Object.getOwnPropertyDescriptor;
var Pl = (i, t, e, s) => {
  for (var n = Il(t, e), o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Tl(t, e, n), n;
};
var yo = class extends k {
  constructor() {
    super(...arguments), this._visible = false, this._middleware = {
      name: "middleware",
      async fn(t) {
        const { right: e, top: s } = await tn(t);
        return t.x -= Math.sign(e) === 1 ? e + 5 : 0, t.y -= Math.sign(s) === 1 ? s + 5 : 0, t;
      }
    };
  }
  get visible() {
    return this._visible;
  }
  set visible(t) {
    this._visible = t, t && this.updatePosition();
  }
  /**
   * Asynchronously updates the position of the context menu relative to a target element.
   * If no target element is provided, it attempts to use the parent node as the target.
   * The position is calculated using the `computePosition` function from `@floating-ui/dom`,
   * which considers various adjustments like offset, inline positioning, flipping, and shifting
   * to ensure the context menu is properly placed relative to the target element.
   *
   * @param [target] - The target element relative to which the context menu should be positioned.
   *                                 If not provided, the parent node is used as the target.
   * @returns A promise that resolves once the position has been updated. This method
   *                          does not explicitly return a value but updates the context menu's style
   *                          properties to position it on the screen.
   */
  async updatePosition(t) {
    const e = t || this.parentNode;
    if (!e) {
      this.visible = false, console.warn("No target element found for context-menu.");
      return;
    }
    const s = await Hs(e, this, {
      placement: "right",
      middleware: [
        Ss(10),
        js(),
        Ds(),
        zs({ padding: 5 }),
        this._middleware
      ]
    }), { x: n, y: o } = s;
    this.style.left = `${n}px`, this.style.top = `${o}px`;
  }
  render() {
    return y` <slot></slot> `;
  }
};
yo.styles = [
  jt.scrollbar,
  T`
      :host {
        --bim-label--fz: var(--bim-ui_size-xs);
        position: absolute;
        top: 0;
        left: 0;
        z-index: 999;
        overflow: auto;
        max-height: 20rem;
        min-width: 3rem;
        flex-direction: column;
        box-shadow: 1px 2px 8px 2px rgba(0, 0, 0, 0.15);
        padding: 0.5rem;
        border-radius: var(--bim-ui_size-4xs);
        background-color: var(
          --bim-context-menu--bgc,
          var(--bim-ui_bg-contrast-20)
        );
      }

      :host([visible]) {
        display: flex;
      }

      :host(:not([visible])) {
        display: none;
      }
    `
];
var _o = yo;
Pl([
  f({ type: Boolean, reflect: true })
], _o.prototype, "visible");
var $t = class extends k {
  constructor() {
    super(...arguments), this._lazyLoadObserver = null, this._visibleElements = [], this.ELEMENTS_BEFORE_OBSERVER = 20, this.useObserver = false, this.elements = /* @__PURE__ */ new Set(), this.observe = (t) => {
      if (!this.useObserver)
        return;
      for (const s of t)
        this.elements.add(s);
      const e = t.slice(this.ELEMENTS_BEFORE_OBSERVER);
      for (const s of e)
        s.remove();
      this.observeLastElement();
    };
  }
  set visibleElements(t) {
    this._visibleElements = this.useObserver ? t : [], this.requestUpdate();
  }
  get visibleElements() {
    return this._visibleElements;
  }
  getLazyObserver() {
    if (!this.useObserver)
      return null;
    if (this._lazyLoadObserver)
      return this._lazyLoadObserver;
    const t = new IntersectionObserver(
      (e) => {
        const s = e[0];
        if (!s.isIntersecting)
          return;
        const n = s.target;
        t.unobserve(n);
        const o = this.ELEMENTS_BEFORE_OBSERVER + this.visibleElements.length, r = [...this.elements][o];
        r && (this.visibleElements = [...this.visibleElements, r], t.observe(r));
      },
      { threshold: 0.5 }
    );
    return t;
  }
  observeLastElement() {
    const t = this.getLazyObserver();
    if (!t)
      return;
    const e = this.ELEMENTS_BEFORE_OBSERVER + this.visibleElements.length - 1, s = [...this.elements][e];
    s && t.observe(s);
  }
  resetVisibleElements() {
    const t = this.getLazyObserver();
    if (t) {
      for (const e of this.elements)
        t.unobserve(e);
      this.visibleElements = [], this.observeLastElement();
    }
  }
  /**
   * Creates a new UI component instance based on the provided template and initial state.
   *
   * @template T - The type of the UI component element.
   * @template S - The type of the component state.
   *
   * @param template - The component template function (stateless or stateful).
   * @param initialState - The initial state of the component (optional for stateless components).
   * @returns The created UI component element or an array containing the element and a function to update its state.
   */
  static create(t, e) {
    const s = document.createDocumentFragment();
    if (t.length === 0)
      return Kt(t(), s), s.firstElementChild;
    if (!e)
      throw new Error(
        "UIComponent: Initial state is required for statefull components."
      );
    let n = e;
    const o = t, r = (a) => (n = { ...n, ...a }, Kt(o(n), s), n);
    return r(e), [s.firstElementChild, r];
  }
};
var ci = (i, t = true) => {
  let e = {};
  for (const s of i.children) {
    const n = s, o = n.getAttribute("name") || n.getAttribute("label");
    if (o) {
      if ("value" in n) {
        const r = n.value;
        if (typeof r == "object" && !Array.isArray(r) && Object.keys(r).length === 0)
          continue;
        e[o] = n.value;
      } else if (t) {
        const r = ci(n);
        if (Object.keys(r).length === 0)
          continue;
        e[o] = r;
      }
    } else
      t && (e = { ...e, ...ci(n) });
  }
  return e;
};
var vi = (i) => i === "true" || i === "false" ? i === "true" : i && !isNaN(Number(i)) && i.trim() !== "" ? Number(i) : i;
var Rl = [">=", "<=", "=", ">", "<", "?", "/", "#"];
function rs(i) {
  const t = Rl.find(
    (r) => i.split(r).length === 2
  ), e = i.split(t).map((r) => r.trim()), [s, n] = e, o = n.startsWith("'") && n.endsWith("'") ? n.replace(/'/g, "") : vi(n);
  return { key: s, condition: t, value: o };
}
var Xi = (i) => {
  try {
    const t = [], e = i.split(/&(?![^()]*\))/).map((s) => s.trim());
    for (const s of e) {
      const n = !s.startsWith("(") && !s.endsWith(")"), o = s.startsWith("(") && s.endsWith(")");
      if (n) {
        const r = rs(s);
        t.push(r);
      }
      if (o) {
        const r = {
          operator: "&",
          queries: s.replace(/^(\()|(\))$/g, "").split("&").map((a) => a.trim()).map((a, l) => {
            const u = rs(a);
            return l > 0 && (u.operator = "&"), u;
          })
        };
        t.push(r);
      }
    }
    return t;
  } catch {
    return null;
  }
};
var as = (i, t, e) => {
  let s = false;
  switch (t) {
    case "=":
      s = i === e;
      break;
    case "?":
      s = String(i).includes(String(e));
      break;
    case "<":
      (typeof i == "number" || typeof e == "number") && (s = i < e);
      break;
    case "<=":
      (typeof i == "number" || typeof e == "number") && (s = i <= e);
      break;
    case ">":
      (typeof i == "number" || typeof e == "number") && (s = i > e);
      break;
    case ">=":
      (typeof i == "number" || typeof e == "number") && (s = i >= e);
      break;
    case "/":
      s = String(i).startsWith(String(e));
      break;
  }
  return s;
};
var Nl = Object.defineProperty;
var Ml = Object.getOwnPropertyDescriptor;
var Ct = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? Ml(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && Nl(t, e, n), n;
};
var xo = class extends k {
  constructor() {
    super(...arguments), this.checked = false, this.checkbox = false, this.noMark = false, this.vertical = false;
  }
  get value() {
    return this._value !== void 0 ? this._value : this.label ? vi(this.label) : this.label;
  }
  set value(t) {
    this._value = t;
  }
  render() {
    return y`
      <div class="parent" .title=${this.label ?? ""}>
        ${this.img || this.icon || this.label ? y` <div style="display: flex; column-gap: 0.375rem">
              ${this.checkbox && !this.noMark ? y`<bim-checkbox
                    style="pointer-events: none"
                    .checked=${this.checked}
                  ></bim-checkbox>` : null}
              <bim-label
                .vertical=${this.vertical}
                .icon=${this.icon}
                .img=${this.img}
                >${this.label}</bim-label
              >
            </div>` : null}
        ${!this.checkbox && !this.noMark && this.checked ? y`<svg
              xmlns="http://www.w3.org/2000/svg"
              height="1.125rem"
              viewBox="0 0 24 24"
              width="1.125rem"
              fill="#FFFFFF"
            >
              <path d="M0 0h24v24H0z" fill="none" />
              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
            </svg>` : null}
        <slot></slot>
      </div>
    `;
  }
};
xo.styles = T`
    :host {
      --bim-label--c: var(--bim-ui_bg-contrast-100);
      display: block;
      box-sizing: border-box;
      flex: 1;
      padding: 0rem 0.5rem;
      border-radius: var(--bim-ui_size-4xs);
    }

    :host(:hover) {
      cursor: pointer;
      background-color: color-mix(
        in lab,
        var(--bim-selector--bgc, var(--bim-ui_bg-contrast-20)),
        var(--bim-ui_main-base) 10%
      );
    }

    :host([checked]) {
      --bim-label--c: color-mix(in lab, var(--bim-ui_main-base), white 30%);
    }

    :host([checked]) svg {
      fill: color-mix(in lab, var(--bim-ui_main-base), white 30%);
    }

    .parent {
      box-sizing: border-box;
      display: flex;
      justify-content: var(--bim-option--jc, space-between);
      column-gap: 0.5rem;
      align-items: center;
      min-height: 1.75rem;
      height: 100%;
    }

    input {
      height: 1rem;
      width: 1rem;
      cursor: pointer;
      border: none;
      outline: none;
      accent-color: var(--bim-checkbox--c, var(--bim-ui_main-base));
    }

    input:focus {
      outline: var(--bim-checkbox--olw, 2px) solid
        var(--bim-checkbox--olc, var(--bim-ui_accent-base));
    }

    bim-label {
      pointer-events: none;
    }
  `;
var L = xo;
Ct([
  f({ type: String, reflect: true })
], L.prototype, "img", 2);
Ct([
  f({ type: String, reflect: true })
], L.prototype, "label", 2);
Ct([
  f({ type: String, reflect: true })
], L.prototype, "icon", 2);
Ct([
  f({ type: Boolean, reflect: true })
], L.prototype, "checked", 2);
Ct([
  f({ type: Boolean, reflect: true })
], L.prototype, "checkbox", 2);
Ct([
  f({ type: Boolean, attribute: "no-mark", reflect: true })
], L.prototype, "noMark", 2);
Ct([
  f({
    converter: {
      fromAttribute(i) {
        return i && vi(i);
      }
    }
  })
], L.prototype, "value", 1);
Ct([
  f({ type: Boolean, reflect: true })
], L.prototype, "vertical", 2);
var Ll = Object.defineProperty;
var zl = Object.getOwnPropertyDescriptor;
var Et = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? zl(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && Ll(t, e, n), n;
};
var wo = class extends $t {
  constructor() {
    super(), this.multiple = false, this.required = false, this.vertical = false, this._inputContainer = gt(), this._listElement = gt(), this._visible = false, this._value = [], this.onValueChange = new Event("change"), this.onWindowMouseUp = (t) => {
      this.visible && (this.contains(t.target) || (this.visible = false));
    }, this.onOptionClick = (t) => {
      const e = t.target, s = this._value.includes(e);
      if (!this.multiple && !this.required && !s)
        this._value = [e];
      else if (!this.multiple && !this.required && s)
        this._value = [];
      else if (!this.multiple && this.required && !s)
        this._value = [e];
      else if (this.multiple && !this.required && !s)
        this._value = [...this._value, e];
      else if (this.multiple && !this.required && s)
        this._value = this._value.filter((n) => n !== e);
      else if (this.multiple && this.required && !s)
        this._value = [...this._value, e];
      else if (this.multiple && this.required && s) {
        const n = this._value.filter((o) => o !== e);
        n.length !== 0 && (this._value = n);
      }
      this.updateOptionsState(), this.dispatchEvent(this.onValueChange);
    }, this.useObserver = true;
  }
  set visible(t) {
    this._visible = t, t || this.resetVisibleElements();
  }
  get visible() {
    return this._visible;
  }
  /**
   * The selected values in the dropdown.
   * @type {any[]}
   * @example
   * const dropdown = document.createElement('bim-dropdown');
   * dropdown.value = ['option1', 'option2'];
   */
  set value(t) {
    if (this.required && Object.keys(t).length === 0)
      return;
    const e = [];
    for (const s of t) {
      const n = this.findOption(s);
      if (n && (e.push(n), !this.multiple && Object.keys(t).length > 1))
        break;
    }
    this._value = e, this.updateOptionsState(), this.dispatchEvent(this.onValueChange);
  }
  get value() {
    return this._value.filter(
      (t) => t instanceof L && t.checked
    ).map((t) => t.value);
  }
  get _options() {
    const t = [...this.elements];
    for (const e of this.children)
      e instanceof L && t.push(e);
    return t;
  }
  onSlotChange(t) {
    const e = t.target.assignedElements();
    this.observe(e);
    for (const s of e) {
      if (!(s instanceof L)) {
        s.remove();
        continue;
      }
      s.removeEventListener("click", this.onOptionClick), s.addEventListener("click", this.onOptionClick);
    }
  }
  updateOptionsState() {
    for (const t of this._options)
      t instanceof L && (this._value.includes(t) ? t.checked = true : t.checked = false);
  }
  findOption(t) {
    return this._options.find((e) => e instanceof L ? e.label === t || e.value === t : false);
  }
  connectedCallback() {
    super.connectedCallback(), window.addEventListener("mouseup", this.onWindowMouseUp);
  }
  firstUpdated() {
    for (const t of this.children)
      t instanceof L && t.checked && this._value.push(t);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), window.removeEventListener("mouseup", this.onWindowMouseUp);
  }
  render() {
    let t, e, s;
    if (this._value.length === 0)
      t = "Select an option...";
    else if (this._value.length === 1) {
      const n = this._value[0];
      t = (n == null ? void 0 : n.label) || (n == null ? void 0 : n.value), e = n == null ? void 0 : n.img, s = n == null ? void 0 : n.icon;
    } else
      t = `Multiple (${this._value.length})`;
    return y`
      <bim-input
        title=${this.label ?? ""}
        .label=${this.label}
        .icon=${this.icon}
        .vertical=${this.vertical}
      >
        <div
          ${Q(this._inputContainer)}
          class="input"
          @click=${() => this.visible = !this.visible}
        >
          <bim-label
            .img=${e}
            .icon=${s}
            style="overflow: hidden;"
            >${t}</bim-label
          >
          <svg
            style="flex-shrink: 0"
            xmlns="http://www.w3.org/2000/svg"
            height="1.125rem"
            viewBox="0 0 24 24"
            width="1.125rem"
            fill="#9ca3af"
          >
            <path d="M0 0h24v24H0V0z" fill="none" />
            <path d="M7.41 8.59 12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z" />
          </svg>
        </div>
        <bim-context-menu ${Q(this._listElement)} .visible=${this.visible}>
          <slot @slotchange=${this.onSlotChange}></slot>
          ${this.visibleElements.map((n) => n)}
        </bim-context-menu>
      </bim-input>
    `;
  }
};
wo.styles = [
  jt.scrollbar,
  T`
      :host {
        --bim-input--bgc: var(
          --bim-dropdown--bgc,
          var(--bim-ui_bg-contrast-20)
        );
        --bim-input--olw: var(--bim-dropdown--olw, 2px);
        --bim-input--olc: var(--bim-dropdown--olc, transparent);
        --bim-input--bdrs: var(--bim-dropdown--bdrs, var(--bim-ui_size-4xs));
        flex: 1;
        display: block;
      }

      :host([visible]) {
        --bim-input--olc: var(
          --bim-dropdown¡focus--c,
          var(--bim-ui_accent-base)
        );
      }

      .input {
        --bim-label--fz: var(--bim-drodown--fz, var(--bim-ui_size-xs));
        --bim-label--c: var(--bim-dropdown--c, var(--bim-ui_bg-contrast-100));
        height: 100%;
        display: flex;
        flex: 1;
        overflow: hidden;
        column-gap: 0.25rem;
        outline: none;
        cursor: pointer;
        align-items: center;
        justify-content: space-between;
        padding: 0 0.5rem;
      }

      bim-label {
        pointer-events: none;
      }
    `
];
var ct = wo;
Et([
  f({ type: String, reflect: true })
], ct.prototype, "name", 2);
Et([
  f({ type: String, reflect: true })
], ct.prototype, "icon", 2);
Et([
  f({ type: String, reflect: true })
], ct.prototype, "label", 2);
Et([
  f({ type: Boolean, reflect: true })
], ct.prototype, "multiple", 2);
Et([
  f({ type: Boolean, reflect: true })
], ct.prototype, "required", 2);
Et([
  f({ type: Boolean, reflect: true })
], ct.prototype, "vertical", 2);
Et([
  f({ type: Boolean, reflect: true })
], ct.prototype, "visible", 1);
Et([
  Me()
], ct.prototype, "_value", 2);
var Dl = Object.defineProperty;
var $o = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Dl(t, e, n), n;
};
var Co = class extends k {
  constructor() {
    super(...arguments), this.floating = false, this.layouts = {};
  }
  // private isVerticalArea(area: string) {
  //   const { rows } = this;
  //   const row = rows.find((row) => row.includes(area));
  //   if (!row)
  //     throw new Error(
  //       `${area} wasn't defined in the grid-template of this bim-grid`,
  //     );
  //   const index = rows.indexOf(row);
  //   const abovePanel = index > 0 && rows[index - 1].includes(area);
  //   const belowPanel =
  //     index < rows.length - 1 && rows[index + 1].includes(area);
  //   return abovePanel || belowPanel;
  // }
  getUniqueAreasFromTemplate(t) {
    const e = t.split(`
`).map((s) => s.trim()).map((s) => s.split('"')[1]).filter((s) => s !== void 0).flatMap((s) => s.split(/\s+/));
    return [...new Set(e)].filter((s) => s !== "");
  }
  firstUpdated() {
    this._onLayoutChange = new Event("layoutchange");
  }
  render() {
    if (this.layout) {
      if (this.layouts[this.layout]) {
        this.innerHTML = "";
        const t = this.layouts[this.layout], e = this.getUniqueAreasFromTemplate(t.template).map((s) => {
          const n = t.elements[s];
          return n && (n.style.gridArea = s), n;
        }).filter((s) => !!s);
        this.style.gridTemplate = t.template, this._onLayoutChange && this.dispatchEvent(this._onLayoutChange), this.append(...e);
      }
    } else
      this.innerHTML = "", this.style.gridTemplate = "", this._onLayoutChange && this.dispatchEvent(this._onLayoutChange);
    return y`<slot></slot>`;
  }
};
Co.styles = T`
    :host {
      display: grid;
      height: 100%;
      width: 100%;
      overflow: hidden;
      box-sizing: border-box;
    }

    /* :host(:not([layout])) {
      display: none;
    } */

    :host([floating]) {
      --bim-panel--bdrs: var(--bim-ui_size-4xs);
      background-color: transparent;
      padding: 1rem;
      gap: 1rem;
      position: absolute;
      pointer-events: none;
      top: 0px;
      left: 0px;
    }

    :host(:not([floating])) {
      --bim-panel--bdrs: 0;
      background-color: var(--bim-ui_bg-contrast-20);
      gap: 1px;
    }
  `;
var pn = Co;
$o([
  f({ type: Boolean, reflect: true })
], pn.prototype, "floating");
$o([
  f({ type: String, reflect: true })
], pn.prototype, "layout");
var Ji = class extends k {
  render() {
    return y`
      <iconify-icon .icon=${this.icon} height="none"></iconify-icon>
    `;
  }
};
Ji.styles = T`
    :host {
      height: var(--bim-icon--fz, var(--bim-ui_size-sm));
      width: var(--bim-icon--fz, var(--bim-ui_size-sm));
    }

    iconify-icon {
      height: var(--bim-icon--fz, var(--bim-ui_size-sm));
      width: var(--bim-icon--fz, var(--bim-ui_size-sm));
      color: var(--bim-icon--c);
    }
  `, Ji.properties = {
  icon: { type: String }
};
var jl = Ji;
var Hl = Object.defineProperty;
var yi = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Hl(t, e, n), n;
};
var Eo = class extends k {
  constructor() {
    super(...arguments), this.vertical = false, this.onValueChange = new Event("change");
  }
  get value() {
    const t = {};
    for (const e of this.children) {
      const s = e;
      "value" in s ? t[s.name || s.label] = s.value : "checked" in s && (t[s.name || s.label] = s.checked);
    }
    return t;
  }
  set value(t) {
    const e = [...this.children];
    for (const s in t) {
      const n = e.find((a) => {
        const l = a;
        return l.name === s || l.label === s;
      });
      if (!n)
        continue;
      const o = n, r = t[s];
      typeof r == "boolean" ? o.checked = r : o.value = r;
    }
  }
  render() {
    return y`
      <div class="parent">
        ${this.label || this.icon ? y`<bim-label .icon=${this.icon}>${this.label}</bim-label>` : null}
        <div class="input">
          <slot></slot>
        </div>
      </div>
    `;
  }
};
Eo.styles = T`
    :host {
      flex: 1;
      display: block;
    }

    .parent {
      display: flex;
      flex-wrap: wrap;
      column-gap: 1rem;
      row-gap: 0.375rem;
      user-select: none;
      flex: 1;
    }

    :host(:not([vertical])) .parent {
      justify-content: space-between;
    }

    :host([vertical]) .parent {
      flex-direction: column;
    }

    .input {
      overflow: hidden;
      box-sizing: border-box;
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      min-height: 1.75rem;
      min-width: 3rem;
      gap: var(--bim-input--g, var(--bim-ui_size-4xs));
      padding: var(--bim-input--p, 0);
      background-color: var(--bim-input--bgc, transparent);
      outline: var(--bim-input--olw, 2px) solid
        var(--bim-input--olc, transparent);
      border-radius: var(--bim-input--bdrs, var(--bim-ui_size-4xs));
    }

    :host(:not([vertical])) .input {
      flex: 1;
      justify-content: flex-end;
    }

    :host(:not([vertical])[label]) .input {
      max-width: fit-content;
    }
  `;
var je = Eo;
yi([
  f({ type: String, reflect: true })
], je.prototype, "name");
yi([
  f({ type: String, reflect: true })
], je.prototype, "label");
yi([
  f({ type: String, reflect: true })
], je.prototype, "icon");
yi([
  f({ type: Boolean, reflect: true })
], je.prototype, "vertical");
var Fl = Object.defineProperty;
var He = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Fl(t, e, n), n;
};
var Ao = class extends k {
  constructor() {
    super(...arguments), this.labelHidden = false, this.iconHidden = false, this.vertical = false;
  }
  get value() {
    return this.textContent ? vi(this.textContent) : this.textContent;
  }
  render() {
    return y`
      <div class="parent" .title=${this.textContent ?? ""}>
        ${this.img ? y`<img .src=${this.img} .alt=${this.textContent || ""} />` : null}
        ${!this.iconHidden && this.icon ? y`<bim-icon .icon=${this.icon}></bim-icon>` : null}
        <p><slot></slot></p>
      </div>
    `;
  }
};
Ao.styles = T`
    :host {
      --bim-icon--c: var(--bim-label--c);
      color: var(--bim-label--c, var(--bim-ui_bg-contrast-60));
      font-size: var(--bim-label--fz, var(--bim-ui_size-xs));
      overflow: hidden;
      display: block;
      white-space: nowrap;
      line-height: 1.1rem;
    }

    .parent {
      display: flex;
      align-items: center;
      column-gap: 0.25rem;
      row-gap: 0.125rem;
      user-select: none;
      height: 100%;
    }

    :host([vertical]) .parent {
      flex-direction: column;
    }

    .parent p {
      margin: 0;
      text-overflow: ellipsis;
      overflow: hidden;
    }

    :host([label-hidden]) .parent p,
    :host(:empty) .parent p {
      display: none;
    }

    img {
      height: 100%;
      aspect-ratio: 1;
      border-radius: 100%;
      margin-right: 0.125rem;
    }

    :host(:not([vertical])) img {
      max-height: var(
        --bim-label_icon--sz,
        calc(var(--bim-label--fz, var(--bim-ui_size-xs)) * 1.5)
      );
    }

    :host([vertical]) img {
      max-height: var(
        --bim-label_icon--sz,
        calc(var(--bim-label--fz, var(--bim-ui_size-xs)) * 4)
      );
    }
  `;
var se = Ao;
He([
  f({ type: String, reflect: true })
], se.prototype, "img");
He([
  f({ type: Boolean, attribute: "label-hidden", reflect: true })
], se.prototype, "labelHidden");
He([
  f({ type: String, reflect: true })
], se.prototype, "icon");
He([
  f({ type: Boolean, attribute: "icon-hidden", reflect: true })
], se.prototype, "iconHidden");
He([
  f({ type: Boolean, reflect: true })
], se.prototype, "vertical");
var Bl = Object.defineProperty;
var Ul = Object.getOwnPropertyDescriptor;
var J = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? Ul(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && Bl(t, e, n), n;
};
var So = class extends k {
  constructor() {
    super(...arguments), this._value = 0, this.vertical = false, this.slider = false, this._input = gt(), this.onValueChange = new Event("change");
  }
  set value(t) {
    this.setValue(t.toString());
  }
  get value() {
    return this._value;
  }
  onChange(t) {
    t.stopPropagation();
    const { value: e } = this._input;
    e && this.setValue(e.value);
  }
  setValue(t) {
    const { value: e } = this._input;
    let s = t;
    if (s = s.replace(/[^0-9.-]/g, ""), s = s.replace(/(\..*)\./g, "$1"), s.endsWith(".") || (s.lastIndexOf("-") > 0 && (s = s[0] + s.substring(1).replace(/-/g, "")), s === "-" || s === "-0"))
      return;
    let n = Number(s);
    Number.isNaN(n) || (n = this.min !== void 0 ? Math.max(n, this.min) : n, n = this.max !== void 0 ? Math.min(n, this.max) : n, this.value !== n && (this._value = n, e && (e.value = this.value.toString()), this.requestUpdate(), this.dispatchEvent(this.onValueChange)));
  }
  onBlur() {
    const { value: t } = this._input;
    t && Number.isNaN(Number(t.value)) && (t.value = this.value.toString());
  }
  onSliderMouseDown(t) {
    document.body.style.cursor = "w-resize";
    const { clientX: e } = t, s = this.value;
    let n = false;
    const o = (l) => {
      var u;
      n = true;
      const { clientX: h } = l, d = this.step ?? 1, c = ((u = d.toString().split(".")[1]) == null ? void 0 : u.length) || 0, p = 1 / (this.sensitivity ?? 1), g = (h - e) / p;
      if (Math.floor(Math.abs(g)) !== Math.abs(g))
        return;
      const w = s + g * d;
      this.setValue(w.toFixed(c));
    }, r = () => {
      this.slider = true, this.removeEventListener("blur", r);
    }, a = () => {
      document.removeEventListener("mousemove", o), document.body.style.cursor = "default", n ? n = false : (this.addEventListener("blur", r), this.slider = false, requestAnimationFrame(() => this.focus())), document.removeEventListener("mouseup", a);
    };
    document.addEventListener("mousemove", o), document.addEventListener("mouseup", a);
  }
  onFocus(t) {
    t.stopPropagation();
    const e = (s) => {
      s.key === "Escape" && (this.blur(), window.removeEventListener("keydown", e));
    };
    window.addEventListener("keydown", e);
  }
  connectedCallback() {
    super.connectedCallback(), this.min && this.min > this.value && (this._value = this.min), this.max && this.max < this.value && (this._value = this.max);
  }
  /**
   * Sets focus to the input element of the number input component.
   * This method is useful for programmatically focusing the input element, for example,
   * in response to a user action or to emphasize the input in the UI.
   *
   * If the input element reference is not available (not yet rendered or disconnected),
   * this method will do nothing.
   */
  focus() {
    const { value: t } = this._input;
    t && t.focus();
  }
  render() {
    const t = y`
      ${this.pref || this.icon ? y`<bim-label
            style="pointer-events: auto"
            @mousedown=${this.onSliderMouseDown}
            .icon=${this.icon}
            >${this.pref}</bim-label
          >` : null}
      <input
        ${Q(this._input)}
        type="text"
        aria-label=${this.label || this.name || "Number Input"}
        size="1"
        @input=${(a) => a.stopPropagation()}
        @change=${this.onChange}
        @blur=${this.onBlur}
        @focus=${this.onFocus}
        .value=${this.value.toString()}
      />
      ${this.suffix ? y`<bim-label
            style="pointer-events: auto"
            @mousedown=${this.onSliderMouseDown}
            >${this.suffix}</bim-label
          >` : null}
    `, e = this.min ?? -1 / 0, s = this.max ?? 1 / 0, n = 100 * (this.value - e) / (s - e), o = y`
      <style>
        .slider-indicator {
          width: ${`${n}%`};
        }
      </style>
      <div class="slider" @mousedown=${this.onSliderMouseDown}>
        <div class="slider-indicator"></div>
        ${this.pref || this.icon ? y`<bim-label
              style="z-index: 1; margin-right: 0.125rem"
              .icon=${this.icon}
              >${`${this.pref}: `}</bim-label
            >` : null}
        <bim-label style="z-index: 1;">${this.value}</bim-label>
        ${this.suffix ? y`<bim-label style="z-index: 1;">${this.suffix}</bim-label>` : null}
      </div>
    `, r = `${this.label || this.name || this.pref ? `${this.label || this.name || this.pref}: ` : ""}${this.value}${this.suffix ?? ""}`;
    return y`
      <bim-input
        title=${r}
        .label=${this.label}
        .icon=${this.icon}
        .vertical=${this.vertical}
      >
        ${this.slider ? o : t}
      </bim-input>
    `;
  }
};
So.styles = T`
    :host {
      --bim-input--bgc: var(
        --bim-number-input--bgc,
        var(--bim-ui_bg-contrast-20)
      );
      --bim-input--olw: var(--bim-number-input--olw, 2px);
      --bim-input--olc: var(--bim-number-input--olc, transparent);
      --bim-input--bdrs: var(--bim-number-input--bdrs, var(--bim-ui_size-4xs));
      --bim-input--p: 0 0.375rem;
      flex: 1;
      display: block;
    }

    :host(:focus) {
      --bim-input--olw: var(--bim-number-input--olw, 2px);
      --bim-input--olc: var(
        --bim-number-input¡focus--c,
        var(--bim-ui_accent-base)
      );
    }

    :host(:not([slider])) bim-label {
      --bim-label--c: var(
        --bim-number-input_affixes--c,
        var(--bim-ui_bg-contrast-60)
      );
      --bim-label--fz: var(
        --bim-number-input_affixes--fz,
        var(--bim-ui_size-xs)
      );
    }

    p {
      margin: 0;
      padding: 0;
    }

    input {
      background-color: transparent;
      outline: none;
      border: none;
      padding: 0;
      flex-grow: 1;
      text-align: right;
      font-family: inherit;
      font-feature-settings: inherit;
      font-variation-settings: inherit;
      font-size: var(--bim-number-input--fz, var(--bim-ui_size-xs));
      color: var(--bim-number-input--c, var(--bim-ui_bg-contrast-100));
    }

    :host([suffix]:not([pref])) input {
      text-align: left;
    }

    :host([slider]) {
      --bim-input--p: 0;
    }

    :host([slider]) .slider {
      --bim-label--c: var(--bim-ui_bg-contrast-100);
    }

    .slider {
      position: relative;
      display: flex;
      justify-content: center;
      width: 100%;
      height: 100%;
      padding: 0 0.5rem;
    }

    .slider-indicator {
      height: 100%;
      background-color: var(--bim-ui_main-base);
      position: absolute;
      top: 0;
      left: 0;
      border-radius: var(--bim-input--bdrs, var(--bim-ui_size-4xs));
    }

    bim-input {
      display: flex;
    }

    bim-label {
      pointer-events: none;
    }
  `;
var W = So;
J([
  f({ type: String, reflect: true })
], W.prototype, "name", 2);
J([
  f({ type: String, reflect: true })
], W.prototype, "icon", 2);
J([
  f({ type: String, reflect: true })
], W.prototype, "label", 2);
J([
  f({ type: String, reflect: true })
], W.prototype, "pref", 2);
J([
  f({ type: Number, reflect: true })
], W.prototype, "min", 2);
J([
  f({ type: Number, reflect: true })
], W.prototype, "value", 1);
J([
  f({ type: Number, reflect: true })
], W.prototype, "step", 2);
J([
  f({ type: Number, reflect: true })
], W.prototype, "sensitivity", 2);
J([
  f({ type: Number, reflect: true })
], W.prototype, "max", 2);
J([
  f({ type: String, reflect: true })
], W.prototype, "suffix", 2);
J([
  f({ type: Boolean, reflect: true })
], W.prototype, "vertical", 2);
J([
  f({ type: Boolean, reflect: true })
], W.prototype, "slider", 2);
var Vl = Object.defineProperty;
var Wl = Object.getOwnPropertyDescriptor;
var Fe = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? Wl(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && Vl(t, e, n), n;
};
var ko = class extends k {
  constructor() {
    super(...arguments), this.onValueChange = new Event("change"), this._hidden = false, this.headerHidden = false, this.activationButton = document.createElement("bim-button");
  }
  set hidden(t) {
    this._hidden = t, this.activationButton.active = !t, this.dispatchEvent(new Event("hiddenchange"));
  }
  get hidden() {
    return this._hidden;
  }
  /**
   * The `value` getter computes and returns the current state of the panel's form elements as an object. This property is dynamic and reflects the current input values within the panel. When accessed, it traverses the panel's child elements, collecting values from those that have a `name` or `label` attribute, and constructs an object where each key corresponds to the `name` or `label` of the element, and the value is the element's value. This property is particularly useful for forms or interactive panels where the user's input needs to be retrieved programmatically. The value returned is a snapshot of the panel's state at the time of access, and it does not maintain a live link to the input elements.
   *
   * @type {Record<string, any>}
   * @default {}
   * @example <bim-panel></bim-panel> <!-- Access via JavaScript to get value -->
   * @example
   * const panel = document.createElement('bim-panel');
   * document.body.appendChild(panel);
   * console.log(panel.value); // Logs the current value object of the panel
   */
  get value() {
    return ci(this);
  }
  /**
   * The `value` setter allows programmatically updating the values of the panel's form elements. When a data object is passed to this property, it attempts to match the object's keys with the `name` or `label` attributes of the panel's child elements. If a match is found, the corresponding element's value is updated to the value associated with the key in the data object. This property is useful for initializing the panel with specific data or updating its state based on external inputs. Note that this operation does not affect elements without a matching `name` or `label`, and it only updates the values of elements that are direct children of the panel.
   *
   * @type {Record<string, any>}
   * @example <bim-panel></bim-panel> <!-- Set value via JavaScript -->
   * @example
   * const panel = document.createElement('bim-panel');
   * document.body.appendChild(panel);
   * panel.value = { 'input-name': 'John Doe', 'checkbox-name': true };
   */
  set value(t) {
    const e = [...this.children];
    for (const s in t) {
      const n = e.find((r) => {
        const a = r;
        return a.name === s || a.label === s;
      });
      if (!n)
        continue;
      const o = n;
      o.value = t[s];
    }
  }
  connectedCallback() {
    super.connectedCallback(), this.activationButton.active = !this.hidden, this.activationButton.onclick = () => this.hidden = !this.hidden;
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.activationButton.remove();
  }
  /**
   * Collapses all `bim-panel-section` elements within the panel.
   * This method iterates over each `bim-panel-section` found within the panel's DOM and sets their `collapsed` property to `true`,
   * effectively hiding their content from view. This can be used to programmatically minimize the space taken up by sections
   * within the panel, making the panel more compact or to hide details that are not immediately necessary.
   */
  collapseSections() {
    const t = this.querySelectorAll("bim-panel-section");
    for (const e of t)
      e.collapsed = true;
  }
  /**
   * Expands all `bim-panel-section` elements within the panel.
   * This method iterates over each `bim-panel-section` found within the panel's DOM and sets their `collapsed` property to `false`,
   * effectively showing their content. This can be used to programmatically reveal the content of sections within the panel,
   * making the panel more informative or to display details that are necessary for the user.
   */
  expandSections() {
    const t = this.querySelectorAll("bim-panel-section");
    for (const e of t)
      e.collapsed = false;
  }
  render() {
    return this.activationButton.icon = this.icon, this.activationButton.label = this.label || this.name, this.activationButton.tooltipTitle = this.label || this.name, y`
      <div class="parent">
        ${this.label || this.name || this.icon ? y`<bim-label .icon=${this.icon}>${this.label}</bim-label>` : null}
        <div class="sections">
          <slot></slot>
        </div>
      </div>
    `;
  }
};
ko.styles = [
  jt.scrollbar,
  T`
      :host {
        display: flex;
        border-radius: var(--bim-ui_size-base);
        background-color: var(--bim-ui_bg-base);
        overflow: auto;
      }

      :host([hidden]) {
        display: none;
      }

      .parent {
        display: flex;
        flex: 1;
        flex-direction: column;
        pointer-events: auto;
        overflow: auto;
      }

      .parent bim-label {
        --bim-label--c: var(--bim-panel--c, var(--bim-ui_bg-contrast-80));
        --bim-label--fz: var(--bim-panel--fz, var(--bim-ui_size-sm));
        font-weight: 600;
        padding: 1rem;
        flex-shrink: 0;
        border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([header-hidden]) .parent bim-label {
        display: none;
      }

      .sections {
        display: flex;
        flex-direction: column;
        overflow: auto;
      }

      ::slotted(bim-panel-section:not(:last-child)) {
        border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
      }
    `
];
var oe = ko;
Fe([
  f({ type: String, reflect: true })
], oe.prototype, "icon", 2);
Fe([
  f({ type: String, reflect: true })
], oe.prototype, "name", 2);
Fe([
  f({ type: String, reflect: true })
], oe.prototype, "label", 2);
Fe([
  f({ type: Boolean, reflect: true })
], oe.prototype, "hidden", 1);
Fe([
  f({ type: Boolean, attribute: "header-hidden", reflect: true })
], oe.prototype, "headerHidden", 2);
var Gl = Object.defineProperty;
var Be = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Gl(t, e, n), n;
};
var Oo = class extends k {
  constructor() {
    super(...arguments), this.onValueChange = new Event("change");
  }
  /**
   * The `value` getter computes and returns the current state of the panel section's form elements as an object. This object's keys are the `name` or `label` attributes of the child elements, and the values are the corresponding values of these elements. This property is particularly useful for retrieving a consolidated view of the user's input or selections within the panel section. When the value of any child element changes, the returned object from this getter will reflect those changes, providing a dynamic snapshot of the panel section's state. Note that this property does not have a default value as it dynamically reflects the current state of the panel section's form elements.
   * @type {Record<string, any>}
   * @example <bim-panel-section></bim-panel-section> <!-- Usage in HTML not directly applicable as this is a getter -->
   * @example
   * const section = document.createElement('bim-panel-section');
   * console.log(section.value); // Logs the current value object
   */
  get value() {
    return ci(this);
  }
  /**
   * The `value` setter allows programmatically updating the values of the panel section's child elements. It accepts an object where keys correspond to the `name` or `label` attributes of the child elements, and the values are the new values to be set for these elements. This property is useful for initializing the panel section with specific values or updating its state based on external data. When the property changes, the corresponding child elements' values are updated to reflect the new state. This does not have a default value as it is a method for updating child elements' values.
   * @type {Record<string, any>}
   * @default undefined
   * @example <bim-panel-section></bim-panel-section> <!-- Usage in HTML not directly applicable as this is a setter -->
   * @example
   * const section = document.createElement('bim-panel-section');
   * section.value = { 'user-settings': 'John Doe' }; // Programmatically sets the value of a child element named 'user-settings'
   */
  set value(t) {
    const e = [...this.children];
    for (const s in t) {
      const n = e.find((r) => {
        const a = r;
        return a.name === s || a.label === s;
      });
      if (!n)
        continue;
      const o = n;
      o.value = t[s];
    }
  }
  onHeaderClick() {
    this.fixed || (this.collapsed = !this.collapsed);
  }
  render() {
    const t = this.label || this.icon || this.name || this.fixed, e = y`<svg
      xmlns="http://www.w3.org/2000/svg"
      height="1.125rem"
      viewBox="0 0 24 24"
      width="1.125rem"
    >
      <path d="M0 0h24v24H0V0z" fill="none" />
      <path d="M7.41 8.59 12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z" />
    </svg>`, s = y`<svg
      xmlns="http://www.w3.org/2000/svg"
      height="1.125rem"
      viewBox="0 0 24 24"
      width="1.125rem"
    >
      <path d="M0 0h24v24H0z" fill="none" />
      <path d="M12 8l-6 6 1.41 1.41L12 10.83l4.59 4.58L18 14z" />
    </svg>`, n = this.collapsed ? e : s, o = y`
      <div
        class="header"
        title=${this.label ?? ""}
        @click=${this.onHeaderClick}
      >
        ${this.label || this.icon || this.name ? y`<bim-label .icon=${this.icon}>${this.label}</bim-label>` : null}
        ${this.fixed ? null : n}
      </div>
    `;
    return y`
      <div class="parent">
        ${t ? o : null}
        <div class="components">
          <slot></slot>
        </div>
      </div>
    `;
  }
};
Oo.styles = [
  jt.scrollbar,
  T`
      :host {
        display: block;
        pointer-events: auto;
      }

      :host(:not([fixed])) .header:hover {
        --bim-label--c: var(--bim-ui_accent-base);
        color: var(--bim-ui_accent-base);
        cursor: pointer;
      }

      :host(:not([fixed])) .header:hover svg {
        fill: var(--bim-ui_accent-base);
      }

      .header {
        --bim-label--fz: var(--bim-ui_size-sm);
        --bim-label--c: var(--bim-ui_bg-contrast-80);
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-weight: 600;
        height: 1.5rem;
        padding: 0.75rem 1rem;
      }

      .header svg {
        fill: var(--bim-ui_bg-contrast-80);
      }

      .title {
        display: flex;
        align-items: center;
        column-gap: 0.5rem;
      }

      .title p {
        font-size: var(--bim-ui_size-sm);
      }

      .components {
        display: flex;
        flex-direction: column;
        row-gap: 0.75rem;
        padding: 0.125rem 1rem 1rem;
      }

      :host(:not([fixed])[collapsed]) .components {
        display: none;
      }

      bim-label {
        pointer-events: none;
      }
    `
];
var re = Oo;
Be([
  f({ type: String, reflect: true })
], re.prototype, "icon");
Be([
  f({ type: String, reflect: true })
], re.prototype, "label");
Be([
  f({ type: String, reflect: true })
], re.prototype, "name");
Be([
  f({ type: Boolean, reflect: true })
], re.prototype, "fixed");
Be([
  f({ type: Boolean, reflect: true })
], re.prototype, "collapsed");
var ql = Object.defineProperty;
var Ue = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && ql(t, e, n), n;
};
var To = class extends k {
  constructor() {
    super(...arguments), this.vertical = false, this.onValueChange = new Event("change"), this._canEmitEvents = false, this._value = document.createElement("bim-option"), this.onOptionClick = (t) => {
      this._value = t.target, this.dispatchEvent(this.onValueChange);
      for (const e of this.children)
        e instanceof L && (e.checked = e === t.target);
    };
  }
  get _options() {
    return [...this.querySelectorAll("bim-option")];
  }
  /**
   * Sets the value of the selector.
   * It finds the matching option based on the provided value and sets it as the selected option.
   * If no matching option is found, it does nothing.
   *
   * @param value - The value to set for the selector.
   */
  set value(t) {
    const e = this.findOption(t);
    if (e) {
      for (const s of this._options)
        s.checked = s === e;
      this._value = e, this._canEmitEvents && this.dispatchEvent(this.onValueChange);
    }
  }
  get value() {
    return this._value.value;
  }
  onSlotChange(t) {
    const e = t.target.assignedElements();
    for (const s of e)
      s instanceof L && (s.noMark = true, s.removeEventListener("click", this.onOptionClick), s.addEventListener("click", this.onOptionClick));
  }
  findOption(t) {
    return this._options.find((e) => e instanceof L ? e.label === t || e.value === t : false);
  }
  firstUpdated() {
    const t = [...this.children].find(
      (e) => e instanceof L && e.checked
    );
    t && (this._value = t);
  }
  render() {
    return y`
      <bim-input
        .vertical=${this.vertical}
        .label=${this.label}
        .icon=${this.icon}
      >
        <slot @slotchange=${this.onSlotChange}></slot>
      </bim-input>
    `;
  }
};
To.styles = T`
    :host {
      --bim-input--bgc: var(--bim-ui_bg-contrast-20);
      --bim-input--g: 0;
      --bim-option--jc: center;
      flex: 1;
      display: block;
    }

    ::slotted(bim-option) {
      border-radius: 0;
    }

    ::slotted(bim-option[checked]) {
      --bim-label--c: var(--bim-ui_main-contrast);
      background-color: var(--bim-ui_main-base);
    }
  `;
var ae = To;
Ue([
  f({ type: String, reflect: true })
], ae.prototype, "name");
Ue([
  f({ type: String, reflect: true })
], ae.prototype, "icon");
Ue([
  f({ type: String, reflect: true })
], ae.prototype, "label");
Ue([
  f({ type: Boolean, reflect: true })
], ae.prototype, "vertical");
Ue([
  Me()
], ae.prototype, "_value");
var Yl = Object.defineProperty;
var Xl = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Yl(t, e, n), n;
};
var Io = class extends k {
  constructor() {
    super(...arguments), this.column = "", this.columnIndex = 0, this.rowData = {};
  }
  get data() {
    return this.column ? this.rowData[this.column] : null;
  }
  render() {
    return y`
      <style>
        :host {
          grid-area: ${this.column ?? "unset"};
        }
      </style>
      <slot></slot>
    `;
  }
};
Io.styles = T`
    :host {
      padding: 0.25rem 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    :host([data-column-index="0"]:not([data-cell-header])) {
      justify-content: normal;
    }

    :host([data-column-index="0"]:not([data-cell-header]))
      ::slotted(bim-label) {
      text-align: left;
    }

    ::slotted(*) {
      --bim-input--bgc: transparent;
      --bim-input--olc: var(--bim-ui_bg-contrast-20);
      --bim-input--olw: 1px;
    }

    ::slotted(bim-input) {
      --bim-input--olw: 0;
    }

    ::slotted(bim-label) {
      white-space: normal;
      text-align: center;
    }
  `;
var Po = Io;
Xl([
  f({ type: String, reflect: true })
], Po.prototype, "column");
var Jl = Object.defineProperty;
var Ql = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Jl(t, e, n), n;
};
var Ro = class extends k {
  constructor() {
    super(...arguments), this._groups = [], this.data = [], this.table = this.closest("bim-table");
  }
  toggleGroups(t, e = false) {
    for (const s of this._groups)
      s.childrenHidden = typeof t > "u" ? !s.childrenHidden : !t, e && s.toggleChildren(t, e);
  }
  render() {
    return this._groups = [], y`
      <slot></slot>
      ${this.data.map((t) => {
      const e = document.createElement(
        "bim-table-group"
      );
      return this._groups.push(e), e.table = this.table, e.data = t, e;
    })}
    `;
  }
};
Ro.styles = T`
    :host {
      position: relative;
      grid-area: Children;
    }

    :host([hidden]) {
      display: none;
    }

    ::slotted(.branch.branch-vertical) {
      top: 0;
      bottom: 1.125rem;
    }
  `;
var No = Ro;
Ql([
  f({ type: Array, attribute: false })
], No.prototype, "data");
var Kl = Object.defineProperty;
var Zl = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Kl(t, e, n), n;
};
var Mo = class extends k {
  constructor() {
    super(...arguments), this.data = { data: {} }, this.childrenHidden = true, this.table = this.closest("bim-table");
  }
  connectedCallback() {
    super.connectedCallback(), this.table && this.table.expanded ? this.childrenHidden = false : this.childrenHidden = true;
  }
  toggleChildren(t, e = false) {
    this._children && (this.childrenHidden = typeof t > "u" ? !this.childrenHidden : !t, e && this._children.toggleGroups(t, e));
  }
  render() {
    var t, e;
    const s = ((t = this.table) == null ? void 0 : t.getGroupIndentation(this.data)) ?? 0, n = y`
      <style>
        .branch-vertical {
          left: ${s + 0.5625}rem;
        }
      </style>
      <div class="branch branch-vertical"></div>
    `, o = document.createDocumentFragment();
    Kt(n, o);
    const r = document.createElement("div");
    r.classList.add("branch", "branch-horizontal"), r.style.left = `${s - 1 + 0.5625}rem`;
    const a = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "svg"
    );
    a.setAttribute("height", "9.5"), a.setAttribute("width", "7.5"), a.setAttribute("viewBox", "0 0 4.6666672 7.3333333");
    const l = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "path"
    );
    l.setAttribute(
      "d",
      "m 1.7470835,6.9583848 2.5899999,-2.59 c 0.39,-0.39 0.39,-1.02 0,-1.41 L 1.7470835,0.36838483 c -0.63,-0.62000003 -1.71000005,-0.18 -1.71000005,0.70999997 v 5.17 c 0,0.9 1.08000005,1.34 1.71000005,0.71 z"
    ), a.append(l);
    const u = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "svg"
    );
    u.setAttribute("height", "6.5"), u.setAttribute("width", "9.5"), u.setAttribute("viewBox", "0 0 5.9111118 5.0175439");
    const h = document.createElementNS(
      "http://www.w3.org/2000/svg",
      "path"
    );
    h.setAttribute(
      "d",
      "M -0.33616196,1.922522 2.253838,4.5125219 c 0.39,0.39 1.02,0.39 1.41,0 L 6.2538379,1.922522 c 0.6200001,-0.63 0.18,-1.71000007 -0.7099999,-1.71000007 H 0.37383804 c -0.89999997,0 -1.33999997,1.08000007 -0.71,1.71000007 z"
    ), u.append(h);
    const d = document.createElement("div");
    d.addEventListener("click", (g) => {
      g.stopPropagation(), this.toggleChildren();
    }), d.classList.add("caret"), d.style.left = `${0.125 + s}rem`, this.childrenHidden ? d.append(a) : d.append(u);
    const c = document.createElement("bim-table-row");
    this.data.children && !this.childrenHidden && c.append(o), c.table = this.table, c.data = this.data.data, (e = this.table) == null || e.dispatchEvent(
      new CustomEvent("rowcreated", { detail: { row: c } })
    ), this.data.children && c.append(d), s !== 0 && (!this.data.children || this.childrenHidden) && c.append(r);
    let p;
    if (this.data.children) {
      p = document.createElement("bim-table-children"), this._children = p, p.table = this.table, p.data = this.data.children;
      const g = document.createDocumentFragment();
      Kt(n, g), p.append(g);
    }
    return y`
      <div class="parent">${c} ${this.childrenHidden ? null : p}</div>
    `;
  }
};
Mo.styles = T`
    :host {
      position: relative;
    }

    .parent {
      display: grid;
      grid-template-areas: "Data" "Children";
    }

    .branch {
      position: absolute;
      z-index: 1;
    }

    .branch-vertical {
      border-left: 1px dotted var(--bim-ui_bg-contrast-40);
    }

    .branch-horizontal {
      top: 50%;
      width: 1rem;
      border-bottom: 1px dotted var(--bim-ui_bg-contrast-40);
    }

    .caret {
      position: absolute;
      z-index: 2;
      transform: translateY(-50%) rotate(0deg);
      top: 50%;
      display: flex;
      width: 0.95rem;
      height: 0.95rem;
      justify-content: center;
      align-items: center;
      cursor: pointer;
    }

    .caret svg {
      fill: var(--bim-ui_bg-contrast-60);
    }
  `;
var Lo = Mo;
Zl([
  f({ type: Boolean, attribute: "children-hidden", reflect: true })
], Lo.prototype, "childrenHidden");
var tc = Object.defineProperty;
var Ve = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && tc(t, e, n), n;
};
var zo = class extends k {
  constructor() {
    super(...arguments), this.columns = [], this.hiddenColumns = [], this.data = {}, this.isHeader = false, this.table = this.closest("bim-table"), this.onTableColumnsChange = () => {
      this.table && (this.columns = this.table.columns);
    }, this.onTableColumnsHidden = () => {
      this.table && (this.hiddenColumns = this.table.hiddenColumns);
    }, this._observer = new IntersectionObserver(
      (t) => {
        this._intersecting = t[0].isIntersecting;
      },
      { rootMargin: "36px" }
    );
  }
  get _columnNames() {
    return this.columns.filter(
      (t) => !this.hiddenColumns.includes(t.name)
    ).map((t) => t.name);
  }
  get _columnWidths() {
    return this.columns.filter(
      (t) => !this.hiddenColumns.includes(t.name)
    ).map((t) => t.width);
  }
  connectedCallback() {
    super.connectedCallback(), this._observer.observe(this), this.table && (this.columns = this.table.columns, this.hiddenColumns = this.table.hiddenColumns, this.table.addEventListener("columnschange", this.onTableColumnsChange), this.table.addEventListener("columnshidden", this.onTableColumnsHidden));
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this._observer.unobserve(this), this.table && (this.columns = [], this.hiddenColumns = [], this.table.removeEventListener("columnschange", this.onTableColumnsChange), this.table.removeEventListener("columnshidden", this.onTableColumnsHidden));
  }
  compute() {
    var t, e, s;
    const n = ((t = this.table) == null ? void 0 : t.getRowIndentation(this.data)) ?? 0, o = this.isHeader ? this.data : ((e = this.table) == null ? void 0 : e.computeRowDeclaration(this.data)) ?? this.data, r = [];
    for (const a in o) {
      if (this.hiddenColumns.includes(a))
        continue;
      const l = o[a];
      let u;
      if (typeof l == "string" || typeof l == "boolean" || typeof l == "number" ? (u = document.createElement("bim-label"), u.textContent = String(l)) : l instanceof HTMLElement ? u = l : (u = document.createDocumentFragment(), Kt(l, u)), !u)
        continue;
      const h = document.createElement("bim-table-cell");
      h.append(u), h.column = a, this._columnNames.indexOf(a) === 0 && !this.isHeader && (h.style.marginLeft = `${n + 0.125}rem`);
      const d = this._columnNames.indexOf(a);
      h.setAttribute("data-column-index", String(d)), h.toggleAttribute("data-cell-header", this.isHeader), h.rowData = this.data, (s = this.table) == null || s.dispatchEvent(
        new CustomEvent("cellcreated", {
          detail: { cell: h }
        })
      ), r.push(h);
    }
    return this.style.gridTemplateAreas = `"${this._columnNames.join(" ")}"`, this.style.gridTemplateColumns = `${this._columnWidths.join(" ")}`, y`
      ${r}
      <slot></slot>
    `;
  }
  render() {
    return y`${this._intersecting ? this.compute() : y``}`;
  }
};
zo.styles = T`
    :host {
      position: relative;
      grid-area: Data;
      display: grid;
      min-height: 2.25rem;
    }

    ::slotted(.branch.branch-vertical) {
      top: 50%;
      bottom: 0;
    }
  `;
var le = zo;
Ve([
  f({ attribute: false })
], le.prototype, "columns");
Ve([
  f({ attribute: false })
], le.prototype, "hiddenColumns");
Ve([
  f({ attribute: false })
], le.prototype, "data");
Ve([
  f({ type: Boolean, attribute: "is-header", reflect: true })
], le.prototype, "isHeader");
Ve([
  Me()
], le.prototype, "_intersecting");
var ec = Object.defineProperty;
var ic = Object.getOwnPropertyDescriptor;
var ce = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? ic(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && ec(t, e, n), n;
};
var Do = class extends k {
  constructor() {
    super(...arguments), this._columnsChange = new Event("columnschange"), this._filteredData = [], this.headersHidden = false, this.minColWidth = "4rem", this._columns = [], this._textDelimiters = {
      comma: ",",
      tab: "	"
    }, this._queryString = null, this._data = [], this.expanded = false, this.preserveStructureOnFilter = false, this.indentationInText = false, this.dataTransform = {}, this._onColumnsHidden = new Event("columnshidden"), this._hiddenColumns = [], this._stringFilterFunction = (t, e) => Object.values(e.data).some((s) => String(s).toLowerCase().includes(t.toLowerCase())), this._queryFilterFunction = (t, e) => {
      let s = false;
      const n = Xi(t) ?? [];
      for (const o of n) {
        if ("queries" in o) {
          s = false;
          break;
        }
        const { condition: r, value: a } = o;
        let { key: l } = o;
        if (l.startsWith("[") && l.endsWith("]")) {
          const u = l.replace("[", "").replace("]", "");
          l = u, s = Object.keys(e.data).filter((h) => h.includes(u)).map(
            (h) => as(e.data[h], r, a)
          ).some((h) => h);
        } else
          s = as(e.data[l], r, a);
        if (!s)
          break;
      }
      return s;
    };
  }
  set columns(t) {
    const e = [];
    for (const s of t) {
      const n = typeof s == "string" ? { name: s, width: `minmax(${this.minColWidth}, 1fr)` } : s;
      e.push(n);
    }
    this._columns = e, this.computeMissingColumns(this.data), this.dispatchEvent(this._columnsChange);
  }
  get columns() {
    return this._columns;
  }
  get _headerRowData() {
    const t = {};
    for (const e of this.columns)
      if (typeof e == "string")
        t[e] = e;
      else {
        const { name: s } = e;
        t[s] = s;
      }
    return t;
  }
  /**
   * Getter for the `value` property.
   * Returns the filtered data if a search string is provided, otherwise returns the original data.
   *
   * @example
   * ```typescript
   * const tableValue = table.value;
   * console.log(tableValue); // Output: The filtered or original data.
   * ```
   */
  get value() {
    return this._filteredData;
  }
  /**
   * Sets the search string for filtering the table data.
   * This property allows you to filter the table data based on a search string.
   * If a search string is provided, the table will only display rows that match the search criteria.
   * The search criteria can be a simple string or a complex query.
   * If a simple string is provided, the table will filter rows based on the string's presence in any column.
   * If a complex query is provided, the table will filter rows based on the query's conditions and values.
   *
   * @example
   * ```typescript
   * table.queryString = "example";
   * ```
   *
   * @example
   * ```typescript
   * table.queryString = "column1="Jhon Doe" & column2=20";
   * ```
   */
  set queryString(t) {
    this._queryString = t && t.trim() !== "" ? t.trim() : null, this.updateFilteredData();
  }
  get queryString() {
    return this._queryString;
  }
  set data(t) {
    this._data = t, this.updateFilteredData(), this.computeMissingColumns(t) && (this.columns = this._columns);
  }
  get data() {
    return this._data;
  }
  get dataAsync() {
    return new Promise((t) => {
      setTimeout(() => {
        t(this.data);
      });
    });
  }
  set hiddenColumns(t) {
    this._hiddenColumns = t, setTimeout(() => {
      this.dispatchEvent(this._onColumnsHidden);
    });
  }
  get hiddenColumns() {
    return this._hiddenColumns;
  }
  updateFilteredData() {
    this.queryString ? (Xi(this.queryString) ? (this.filterFunction = this._queryFilterFunction, this._filteredData = this.filter(this.queryString)) : (this.filterFunction = this._stringFilterFunction, this._filteredData = this.filter(this.queryString)), this.preserveStructureOnFilter && (this._expandedBeforeFilter === void 0 && (this._expandedBeforeFilter = this.expanded), this.expanded = true)) : (this.preserveStructureOnFilter && this._expandedBeforeFilter !== void 0 && (this.expanded = this._expandedBeforeFilter, this._expandedBeforeFilter = void 0), this._filteredData = this.data);
  }
  computeMissingColumns(t) {
    let e = false;
    for (const s of t) {
      const { children: n, data: o } = s;
      for (const r in o)
        this._columns.map((a) => typeof a == "string" ? a : a.name).includes(r) || (this._columns.push({
          name: r,
          width: `minmax(${this.minColWidth}, 1fr)`
        }), e = true);
      if (n) {
        const r = this.computeMissingColumns(n);
        r && !e && (e = r);
      }
    }
    return e;
  }
  generateText(t = "comma", e = this.value, s = "", n = true) {
    const o = this._textDelimiters[t];
    let r = "";
    const a = this.columns.map((l) => l.name);
    if (n) {
      this.indentationInText && (r += `Indentation${o}`);
      const l = `${a.join(o)}
`;
      r += l;
    }
    for (const [l, u] of e.entries()) {
      const { data: h, children: d } = u, c = this.indentationInText ? `${s}${l + 1}${o}` : "", p = a.map((w) => h[w] ?? ""), g = `${c}${p.join(o)}
`;
      r += g, d && (r += this.generateText(
        t,
        u.children,
        `${s}${l + 1}.`,
        false
      ));
    }
    return r;
  }
  /**
   * A getter function that generates a CSV (Comma Separated Values) representation of the table data.
   *
   * @returns A string containing the CSV representation of the table data.
   *
   * @example
   * ```typescript
   * const csvData = table.csv;
   * console.log(csvData); // Output: "Column 1,Column 2\nValue 1,Value 2\nValue 3,Value 4"
   * ```
   */
  get csv() {
    return this.generateText("comma");
  }
  /**
   * A getter function that generates a Tab Separated Values (TSV) representation of the table data.
   *
   * @returns A string containing the TSV representation of the table data.
   *
   * @example
   * ```typescript
   * const tsvData = table.tsv;
   * console.log(tsvData); // Output: "Column 1\tColumn 2\nValue 1\tValue 2\nValue 3\tValue 4"
   * ```
   */
  get tsv() {
    return this.generateText("tab");
  }
  computeRowDeclaration(t) {
    const e = {};
    for (const s in t) {
      const n = this.dataTransform[s];
      n ? e[s] = n(t[s], t) : e[s] = t[s];
    }
    return e;
  }
  /**
   * The `downloadData` method is used to download the table data in different formats.
   *
   * @param fileName - The name of the downloaded file. Default is "BIM Table Data".
   * @param format - The format of the downloaded file. Can be "json", "tsv", or "csv". Default is "json".
   *
   * @returns - This method does not return any value.
   *
   * @example
   * ```typescript
   * table.downloadData("MyTableData", "tsv");
   * ```
   */
  downloadData(t = "BIM Table Data", e = "json") {
    let s = null;
    if (e === "json" && (s = new File(
      [JSON.stringify(this.value, void 0, 2)],
      `${t}.json`
    )), e === "csv" && (s = new File([this.csv], `${t}.csv`)), e === "tsv" && (s = new File([this.tsv], `${t}.tsv`)), !s)
      return;
    const n = document.createElement("a");
    n.href = URL.createObjectURL(s), n.download = s.name, n.click(), URL.revokeObjectURL(n.href);
  }
  getRowIndentation(t, e = this.value, s = 0) {
    for (const n of e) {
      if (n.data === t)
        return s;
      if (n.children) {
        const o = this.getRowIndentation(
          t,
          n.children,
          s + 1
        );
        if (o !== null)
          return o;
      }
    }
    return null;
  }
  getGroupIndentation(t, e = this.value, s = 0) {
    for (const n of e) {
      if (n === t)
        return s;
      if (n.children) {
        const o = this.getGroupIndentation(
          t,
          n.children,
          s + 1
        );
        if (o !== null)
          return o;
      }
    }
    return null;
  }
  connectedCallback() {
    super.connectedCallback(), this.dispatchEvent(new Event("connected"));
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.dispatchEvent(new Event("disconnected"));
  }
  filter(t, e = this.filterFunction ?? this._stringFilterFunction, s = this.data) {
    const n = [];
    for (const o of s)
      if (e(t, o)) {
        if (this.preserveStructureOnFilter) {
          const r = { data: o.data };
          if (o.children) {
            const a = this.filter(
              t,
              e,
              o.children
            );
            a.length && (r.children = a);
          }
          n.push(r);
        } else if (n.push({ data: o.data }), o.children) {
          const r = this.filter(
            t,
            e,
            o.children
          );
          n.push(...r);
        }
      } else if (o.children) {
        const r = this.filter(
          t,
          e,
          o.children
        );
        this.preserveStructureOnFilter && r.length ? n.push({
          data: o.data,
          children: r
        }) : n.push(...r);
      }
    return n;
  }
  render() {
    const t = document.createElement("bim-table-row");
    t.table = this, t.isHeader = true, t.data = this._headerRowData, t.style.gridArea = "Header", t.style.position = "sticky", t.style.top = "0", t.style.zIndex = "5";
    const e = document.createElement("bim-table-children");
    return e.table = this, e.data = this.value, e.style.gridArea = "Body", e.style.backgroundColor = "transparent", y`
      <div class="parent">
        ${this.headersHidden ? null : t}
        <div style="overflow-x: hidden; grid-area: Body">${e}</div>
      </div>
    `;
  }
};
Do.styles = [
  jt.scrollbar,
  T`
      :host {
        --bim-button--bgc: transparent;
        position: relative;
        overflow: auto;
        display: block;
        pointer-events: auto;
      }

      .parent {
        display: grid;
        grid-template:
          "Header" auto
          "Body" 1fr
          "Footer" auto;
        overflow: auto;
        height: 100%;
      }

      .parent > bim-table-row[is-header] {
        color: var(--bim-table_header--c, var(--bim-ui_bg-contrast-100));
        background-color: var(
          --bim-table_header--bgc,
          var(--bim-ui_bg-contrast-20)
        );
      }

      .controls {
        display: flex;
        gap: 0.375rem;
        flex-wrap: wrap;
        margin-bottom: 0.5rem;
      }
    `
];
var Ht = Do;
ce([
  Me()
], Ht.prototype, "_filteredData", 2);
ce([
  f({
    type: Boolean,
    attribute: "headers-hidden",
    reflect: true
  })
], Ht.prototype, "headersHidden", 2);
ce([
  f({ type: String, attribute: "min-col-width", reflect: true })
], Ht.prototype, "minColWidth", 2);
ce([
  f({ type: Array, attribute: false })
], Ht.prototype, "columns", 1);
ce([
  f({ type: Array, attribute: false })
], Ht.prototype, "data", 1);
ce([
  f({ type: Boolean, reflect: true })
], Ht.prototype, "expanded", 2);
var nc = Object.defineProperty;
var sc = Object.getOwnPropertyDescriptor;
var _i = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? sc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && nc(t, e, n), n;
};
var jo = class extends k {
  constructor() {
    super(...arguments), this._defaultName = "__unnamed__", this.name = this._defaultName, this._hidden = false;
  }
  set hidden(t) {
    this._hidden = t, this.dispatchEvent(new Event("hiddenchange"));
  }
  get hidden() {
    return this._hidden;
  }
  connectedCallback() {
    super.connectedCallback();
    const { parentElement: t } = this;
    if (t && this.name === this._defaultName) {
      const e = [...t.children].indexOf(this);
      this.name = `${this._defaultName}${e}`;
    }
  }
  render() {
    return y` <slot></slot> `;
  }
};
jo.styles = T`
    :host {
      display: block;
      height: 100%;
    }

    :host([hidden]) {
      display: none;
    }
  `;
var Y = jo;
_i([
  f({ type: String, reflect: true })
], Y.prototype, "name", 2);
_i([
  f({ type: String, reflect: true })
], Y.prototype, "label", 2);
_i([
  f({ type: String, reflect: true })
], Y.prototype, "icon", 2);
_i([
  f({ type: Boolean, reflect: true })
], Y.prototype, "hidden", 1);
var oc = Object.defineProperty;
var rc = Object.getOwnPropertyDescriptor;
var ue = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? rc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && oc(t, e, n), n;
};
var Ho = class extends k {
  constructor() {
    super(...arguments), this._switchers = [], this.bottom = false, this.switchersHidden = false, this.floating = false, this.switchersFull = false, this.onTabHiddenChange = (t) => {
      const e = t.target;
      e instanceof Y && !e.hidden && (e.removeEventListener("hiddenchange", this.onTabHiddenChange), this.tab = e.name, e.addEventListener("hiddenchange", this.onTabHiddenChange));
    };
  }
  set tab(t) {
    this._tab = t;
    const e = [...this.children], s = e.find(
      (n) => n instanceof Y && n.name === t
    );
    for (const n of e) {
      if (!(n instanceof Y))
        continue;
      n.hidden = s !== n;
      const o = this.getTabSwitcher(n.name);
      o && o.toggleAttribute("data-active", !n.hidden);
    }
  }
  get tab() {
    return this._tab;
  }
  getTabSwitcher(t) {
    return this._switchers.find(
      (e) => e.getAttribute("data-name") === t
    );
  }
  createSwitchers() {
    this._switchers = [];
    for (const t of this.children) {
      if (!(t instanceof Y))
        continue;
      const e = document.createElement("div");
      e.addEventListener("click", () => {
        this.tab === t.name ? this.toggleAttribute("tab", false) : this.tab = t.name;
      }), e.setAttribute("data-name", t.name), e.className = "switcher";
      const s = document.createElement("bim-label");
      s.textContent = t.label ?? "", s.icon = t.icon, e.append(s), this._switchers.push(e);
    }
  }
  onSlotChange(t) {
    this.createSwitchers();
    const e = t.target.assignedElements(), s = e.find((n) => n instanceof Y ? this.tab ? n.name === this.tab : !n.hidden : false);
    s && s instanceof Y && (this.tab = s.name);
    for (const n of e) {
      if (!(n instanceof Y)) {
        n.remove();
        continue;
      }
      n.removeEventListener("hiddenchange", this.onTabHiddenChange), s !== n && (n.hidden = true), n.addEventListener("hiddenchange", this.onTabHiddenChange);
    }
  }
  render() {
    return y`
      <div class="parent">
        <div class="switchers">${this._switchers}</div>
        <div class="content">
          <slot @slotchange=${this.onSlotChange}></slot>
        </div>
      </div>
    `;
  }
};
Ho.styles = [
  jt.scrollbar,
  T`
      * {
        box-sizing: border-box;
      }

      :host {
        background-color: var(--bim-ui_bg-base);
        display: block;
        overflow: auto;
      }

      .parent {
        display: grid;
        grid-template: "switchers" auto "content" 1fr;
        height: 100%;
      }

      :host([bottom]) .parent {
        grid-template: "content" 1fr "switchers" auto;
      }

      .switchers {
        display: flex;
        height: 2.25rem;
        font-weight: 600;
        grid-area: switchers;
      }

      .switcher {
        --bim-label--c: var(--bim-ui_bg-contrast-80);
        background-color: var(--bim-ui_bg-base);
        cursor: pointer;
        pointer-events: auto;
        padding: 0rem 0.75rem;
        display: flex;
        justify-content: center;
      }

      :host([switchers-full]) .switcher {
        flex: 1;
      }

      .switcher:hover,
      .switcher[data-active] {
        --bim-label--c: var(--bim-ui_main-contrast);
        background-color: var(--bim-ui_main-base);
      }

      .switchers bim-label {
        pointer-events: none;
      }

      :host([switchers-hidden]) .switchers {
        display: none;
      }

      .content {
        grid-area: content;
        overflow: auto;
      }

      :host(:not([bottom])) .content {
        border-top: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([bottom]) .content {
        border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host(:not([tab])) .content {
        display: none;
      }

      :host([floating]) {
        background-color: transparent;
      }

      :host([floating]) .switchers {
        justify-self: center;
        overflow: auto;
      }

      :host([floating]:not([bottom])) .switchers {
        border-radius: var(--bim-ui_size-2xs) var(--bim-ui_size-2xs) 0 0;
        border-top: 1px solid var(--bim-ui_bg-contrast-20);
        border-left: 1px solid var(--bim-ui_bg-contrast-20);
        border-right: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([floating][bottom]) .switchers {
        border-radius: 0 0 var(--bim-ui_size-2xs) var(--bim-ui_size-2xs);
        border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
        border-left: 1px solid var(--bim-ui_bg-contrast-20);
        border-right: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([floating]:not([tab])) .switchers {
        border-radius: var(--bim-ui_size-2xs);
        border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([floating][bottom]:not([tab])) .switchers {
        border-top: 1px solid var(--bim-ui_bg-contrast-20);
      }

      :host([floating]) .content {
        border: 1px solid var(--bim-ui_bg-contrast-20);
        border-radius: var(--bim-ui_size-2xs);
        background-color: var(--bim-ui_bg-base);
      }
    `
];
var Ft = Ho;
ue([
  Me()
], Ft.prototype, "_switchers", 2);
ue([
  f({ type: Boolean, reflect: true })
], Ft.prototype, "bottom", 2);
ue([
  f({ type: Boolean, attribute: "switchers-hidden", reflect: true })
], Ft.prototype, "switchersHidden", 2);
ue([
  f({ type: Boolean, reflect: true })
], Ft.prototype, "floating", 2);
ue([
  f({ type: String, reflect: true })
], Ft.prototype, "tab", 1);
ue([
  f({ type: Boolean, attribute: "switchers-full", reflect: true })
], Ft.prototype, "switchersFull", 2);
var ac = (i) => i ?? M;
var lc = Object.defineProperty;
var cc = Object.getOwnPropertyDescriptor;
var At = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? cc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && lc(t, e, n), n;
};
var Fo = class extends k {
  constructor() {
    super(...arguments), this._inputTypes = [
      "date",
      "datetime-local",
      "email",
      "month",
      "password",
      "search",
      "tel",
      "text",
      "time",
      "url",
      "week"
    ], this.value = "", this.vertical = false, this._type = "text", this.onValueChange = new Event("input");
  }
  set type(t) {
    this._inputTypes.includes(t) && (this._type = t);
  }
  get type() {
    return this._type;
  }
  /**
   * Gets the query value derived from the current input value.
   * The `getQuery` function is assumed to be a utility function that takes a string as input
   * and returns a processed query value based on the input.
   *
   * @returns The processed query value derived from the current input value.
   *
   * @example
   * ```typescript
   * const textInput = new TextInput();
   * textInput.value = "Key?Value";
   * console.log(textInput.query);
   * ```
   */
  get query() {
    return Xi(this.value);
  }
  onInputChange(t) {
    t.stopPropagation();
    const e = t.target;
    clearTimeout(this._debounceTimeoutID), this._debounceTimeoutID = setTimeout(() => {
      this.value = e.value, this.dispatchEvent(this.onValueChange);
    }, this.debounce);
  }
  focus() {
    setTimeout(() => {
      var t;
      const e = (t = this.shadowRoot) == null ? void 0 : t.querySelector("input");
      e == null || e.focus();
    });
  }
  render() {
    return y`
      <bim-input
        .name=${this.name}
        .icon=${this.icon}
        .label=${this.label}
        .vertical=${this.vertical}
      >
        <input
          aria-label=${this.label || this.name || "Text Input"}
          .type=${this.type}
          .value=${this.value}
          placeholder=${ac(this.placeholder)}
          @input=${this.onInputChange}
        />
      </bim-input>
    `;
  }
};
Fo.styles = T`
    :host {
      --bim-input--bgc: var(--bim-ui_bg-contrast-20);
      flex: 1;
      display: block;
    }

    input {
      background-color: transparent;
      outline: none;
      border: none;
      width: 100%;
      height: 100%;
      padding: 0 var(--bim-ui_size-3xs);
      border-radius: var(--bim-text-input--bdrs, var(--bim-ui_size-4xs));
      color: var(--bim-text-input--c, var(--bim-ui_bg-contrast-100));
    }

    :host(:focus) {
      --bim-input--olc: var(--bim-ui_accent-base);
    }

    /* :host([disabled]) {
      --bim-input--bgc: var(--bim-ui_bg-contrast-20);
    } */
  `;
var ut2 = Fo;
At([
  f({ type: String, reflect: true })
], ut2.prototype, "icon", 2);
At([
  f({ type: String, reflect: true })
], ut2.prototype, "label", 2);
At([
  f({ type: String, reflect: true })
], ut2.prototype, "name", 2);
At([
  f({ type: String, reflect: true })
], ut2.prototype, "placeholder", 2);
At([
  f({ type: String, reflect: true })
], ut2.prototype, "value", 2);
At([
  f({ type: Boolean, reflect: true })
], ut2.prototype, "vertical", 2);
At([
  f({ type: Number, reflect: true })
], ut2.prototype, "debounce", 2);
At([
  f({ type: String, reflect: true })
], ut2.prototype, "type", 1);
var uc = Object.defineProperty;
var hc = Object.getOwnPropertyDescriptor;
var Bo = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? hc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && uc(t, e, n), n;
};
var Uo = class extends k {
  constructor() {
    super(...arguments), this.rows = 2, this._vertical = false;
  }
  set vertical(t) {
    this._vertical = t, this.updateChildren();
  }
  get vertical() {
    return this._vertical;
  }
  updateChildren() {
    const t = this.children;
    for (const e of t)
      this.vertical ? e.setAttribute("label-hidden", "") : e.removeAttribute("label-hidden");
  }
  render() {
    return y`
      <style>
        .parent {
          grid-auto-flow: ${this.vertical ? "row" : "column"};
          grid-template-rows: repeat(${this.rows}, 1fr);
        }
      </style>
      <div class="parent">
        <slot @slotchange=${this.updateChildren}></slot>
      </div>
    `;
  }
};
Uo.styles = T`
    .parent {
      display: grid;
      gap: 0.25rem;
    }

    ::slotted(bim-button[label]:not([vertical])) {
      --bim-button--jc: flex-start;
    }

    ::slotted(bim-button) {
      --bim-label--c: var(--bim-ui_bg-contrast-80);
    }
  `;
var xi = Uo;
Bo([
  f({ type: Number, reflect: true })
], xi.prototype, "rows", 2);
Bo([
  f({ type: Boolean, reflect: true })
], xi.prototype, "vertical", 1);
var dc = Object.defineProperty;
var pc = Object.getOwnPropertyDescriptor;
var wi = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? pc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && dc(t, e, n), n;
};
var Vo = class extends k {
  constructor() {
    super(...arguments), this._vertical = false, this._labelHidden = false;
  }
  set vertical(t) {
    this._vertical = t, this.updateChildren();
  }
  get vertical() {
    return this._vertical;
  }
  set labelHidden(t) {
    this._labelHidden = t, this.updateChildren();
  }
  get labelHidden() {
    return this._labelHidden;
  }
  updateChildren() {
    const t = this.children;
    for (const e of t)
      e instanceof xi && (e.vertical = this.vertical), e.toggleAttribute("label-hidden", this.vertical);
  }
  render() {
    return y`
      <div class="parent">
        <div class="children">
          <slot @slotchange=${this.updateChildren}></slot>
        </div>
        ${!this.labelHidden && (this.label || this.icon) ? y`<bim-label .icon=${this.icon}>${this.label}</bim-label>` : null}
      </div>
    `;
  }
};
Vo.styles = T`
    :host {
      --bim-label--fz: var(--bim-ui_size-xs);
      --bim-label--c: var(--bim-ui_bg-contrast-60);
      display: block;
      flex: 1;
    }

    :host(:not([vertical])) ::slotted(bim-button[vertical]) {
      --bim-icon--fz: var(--bim-ui_size-5xl);
      min-height: 3.75rem;
    }

    ::slotted(bim-button) {
      --bim-label--c: var(--bim-ui_bg-contrast-80);
    }

    .parent {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      align-items: center;
      padding: 0.5rem;
      height: 100%;
      box-sizing: border-box;
      justify-content: space-between;
    }

    :host([vertical]) .parent {
      flex-direction: row-reverse;
    }

    :host([vertical]) .parent > bim-label {
      writing-mode: tb;
    }

    .children {
      display: flex;
      gap: 0.25rem;
    }

    :host([vertical]) .children {
      flex-direction: column;
    }
  `;
var he = Vo;
wi([
  f({ type: String, reflect: true })
], he.prototype, "label", 2);
wi([
  f({ type: String, reflect: true })
], he.prototype, "icon", 2);
wi([
  f({ type: Boolean, reflect: true })
], he.prototype, "vertical", 1);
wi([
  f({ type: Boolean, attribute: "label-hidden", reflect: true })
], he.prototype, "labelHidden", 1);
var Wo = class S {
  static set config(t) {
    this._config = { ...S._config, ...t };
  }
  static get config() {
    return S._config;
  }
  static addGlobalStyles() {
    let t = document.querySelector("style[id='bim-ui']");
    if (t)
      return;
    t = document.createElement("style"), t.id = "bim-ui", t.textContent = jt.globalStyles.cssText;
    const e = document.head.firstChild;
    e ? document.head.insertBefore(t, e) : document.head.append(t);
  }
  static defineCustomElement(t, e) {
    customElements.get(t) || customElements.define(t, e);
  }
  /**
   * @deprecated Use `Manager.init()` instead.
   */
  static registerComponents() {
    S.init();
  }
  /**
   * Initializes the BIM UI library by defining custom elements.
   * It ensures that all necessary styles and custom elements are registered for use in BIM UI components.
   *
   * @example
   * ```typescript
   * import { Manager } from "@thatopen/ui";
   * Manager.init();
   * ```
   */
  static init() {
    S.addGlobalStyles(), S.defineCustomElement("bim-button", El), S.defineCustomElement("bim-checkbox", ie), S.defineCustomElement("bim-color-input", Dt), S.defineCustomElement("bim-context-menu", _o), S.defineCustomElement("bim-dropdown", ct), S.defineCustomElement("bim-grid", pn), S.defineCustomElement("bim-icon", jl), S.defineCustomElement("bim-input", je), S.defineCustomElement("bim-label", se), S.defineCustomElement("bim-number-input", W), S.defineCustomElement("bim-option", L), S.defineCustomElement("bim-panel", oe), S.defineCustomElement("bim-panel-section", re), S.defineCustomElement("bim-selector", ae), S.defineCustomElement("bim-table", Ht), S.defineCustomElement("bim-tabs", Ft), S.defineCustomElement("bim-tab", Y), S.defineCustomElement("bim-table-cell", Po), S.defineCustomElement("bim-table-children", No), S.defineCustomElement("bim-table-group", Lo), S.defineCustomElement("bim-table-row", le), S.defineCustomElement("bim-text-input", ut2), S.defineCustomElement("bim-toolbar", $i), S.defineCustomElement("bim-toolbar-group", xi), S.defineCustomElement(
      "bim-toolbar-section",
      he
    ), S.defineCustomElement("bim-viewport", Yo);
  }
  static newRandomId() {
    const t = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let e = "";
    for (let s = 0; s < 10; s++) {
      const n = Math.floor(Math.random() * t.length);
      e += t.charAt(n);
    }
    return e;
  }
};
Wo._config = {
  sectionLabelOnVerticalToolbar: false
  // draggableToolbars: true,
  // draggablePanels: true,
};
var Qi = Wo;
var fc = Object.defineProperty;
var mc = Object.getOwnPropertyDescriptor;
var fn = (i, t, e, s) => {
  for (var n = s > 1 ? void 0 : s ? mc(t, e) : t, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = (s ? r(t, e, n) : r(n)) || n);
  return s && n && fc(t, e, n), n;
};
var Go = class extends k {
  constructor() {
    super(...arguments), this.labelsHidden = false, this._vertical = false, this._hidden = false;
  }
  set vertical(t) {
    this._vertical = t, this.updateSections();
  }
  get vertical() {
    return this._vertical;
  }
  set hidden(t) {
    this._hidden = t, this.dispatchEvent(new Event("hiddenchange"));
  }
  get hidden() {
    return this._hidden;
  }
  // private setActivationButton() {
  //   this.activationButton.draggable = Manager.config.draggableToolbars;
  //   this.activationButton.addEventListener(
  //     "click",
  //     () => (this.hidden = !this.hidden),
  //   );
  //   this.activationButton.setAttribute("data-ui-manager-id", this._managerID);
  //   this.activationButton.addEventListener("dragstart", (e) => {
  //     const id = this.getAttribute("data-ui-manager-id");
  //     if (e.dataTransfer && id) {
  //       e.dataTransfer.setData("id", id);
  //       e.dataTransfer.effectAllowed = "move";
  //     }
  //     const containers = document.querySelectorAll("bim-toolbars-container");
  //     for (const container of containers) {
  //       if (container === this.parentElement) continue;
  //       container.dropping = true;
  //     }
  //   });
  //   this.activationButton.addEventListener("dragend", (e) => {
  //     if (e.dataTransfer) e.dataTransfer.clearData();
  //     const containers = document.querySelectorAll("bim-toolbars-container");
  //     for (const container of containers) {
  //       container.dropping = false;
  //     }
  //   });
  // }
  updateSections() {
    const t = this.children;
    for (const e of t)
      e instanceof he && (e.labelHidden = this.vertical && !Qi.config.sectionLabelOnVerticalToolbar, e.vertical = this.vertical);
  }
  // firstUpdated() {
  //   this.setAttribute("data-ui-manager-id", this._managerID);
  // }
  render() {
    return y`
      <div class="parent">
        <slot @slotchange=${this.updateSections}></slot>
      </div>
    `;
  }
};
Go.styles = T`
    :host {
      --bim-button--bgc: transparent;
      background-color: var(--bim-ui_bg-base);
      border-radius: var(--bim-ui_size-2xs);
      display: block;
    }

    :host([hidden]) {
      display: none;
    }

    .parent {
      display: flex;
      width: min-content;
      pointer-events: auto;
    }

    :host([vertical]) .parent {
      flex-direction: column;
    }

    :host([vertical]) {
      width: min-content;
      border-radius: var(--bim-ui_size-2xs);
      border: 1px solid var(--bim-ui_bg-contrast-20);
    }

    ::slotted(bim-toolbar-section:not(:last-child)) {
      border-right: 1px solid var(--bim-ui_bg-contrast-20);
      border-bottom: none;
    }

    :host([vertical]) ::slotted(bim-toolbar-section:not(:last-child)) {
      border-bottom: 1px solid var(--bim-ui_bg-contrast-20);
      border-right: none;
    }
  `;
var $i = Go;
fn([
  f({ type: String, reflect: true })
], $i.prototype, "icon", 2);
fn([
  f({ type: Boolean, attribute: "labels-hidden", reflect: true })
], $i.prototype, "labelsHidden", 2);
fn([
  f({ type: Boolean, reflect: true })
], $i.prototype, "vertical", 1);
var bc = Object.defineProperty;
var gc = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && bc(t, e, n), n;
};
var qo = class extends k {
  constructor() {
    super(), this._onResize = new Event("resize"), new ResizeObserver(() => {
      setTimeout(() => {
        this.dispatchEvent(this._onResize);
      });
    }).observe(this);
  }
  render() {
    return y`
      <div class="parent">
        <slot></slot>
      </div>
    `;
  }
};
qo.styles = T`
    :host {
      display: grid;
      min-width: 0;
      min-height: 0;
      height: 100%;
    }

    .parent {
      overflow: hidden;
      position: relative;
    }
  `;
var Yo = qo;
gc([
  f({ type: String, reflect: true })
], Yo.prototype, "name");
var vc = (i) => {
  const { components: t } = i, e = t.get(Ri);
  return y`
    <bim-button
      data-ui-id="import-ifc"
      label="Load IFC"
      icon="mage:box-3d-fill"
      @click=${() => {
    const n = document.createElement("input");
    n.type = "file", n.accept = ".ifc", n.onchange = async () => {
      if (n.files === null || n.files.length === 0)
        return;
      const o = n.files[0];
      n.remove();
      const r = await o.arrayBuffer(), a = new Uint8Array(r), l = await e.load(a);
      l.name = o.name.replace(".ifc", "");
    }, n.click();
  }}
    ></bim-button>
  `;
};
var yc = (i) => $t.create(
  vc,
  i
);
var _c = Object.freeze(Object.defineProperty({
  __proto__: null,
  loadIfc: yc
}, Symbol.toStringTag, { value: "Module" }));
var nh = {
  ..._c
  // fragmentExploder,
};
var xc2 = (i) => {
  const { components: t } = i, e = i.schemaTag ?? true, s = i.viewDefinitionTag ?? true, n = t.get(ut), o = document.createElement("bim-table");
  o.addEventListener("cellcreated", ({ detail: a }) => {
    const { cell: l } = a;
    l.style.padding = "0.25rem 0";
  }), o.hiddenColumns = ["modelID"], o.headersHidden = true;
  const r = [];
  for (const [, a] of n.groups) {
    if (!a)
      continue;
    const l = {
      data: {
        Name: a.name || a.uuid,
        modelID: a.uuid
      }
    };
    r.push(l);
  }
  return o.dataTransform = {
    Name: (a, l) => {
      const { modelID: u } = l;
      if (typeof u != "string")
        return a;
      const h = n.groups.get(u);
      if (!h)
        return u;
      const d = {};
      for (const x of h.items)
        d[x.id] = x.ids;
      let c;
      const { schema: p } = h.ifcMetadata;
      e && p && (c = y`
          <bim-label style="background-color: var(--bim-ui_main-base); padding: 0 0.25rem; color: var(--bim-ui_main-contrast); border-radius: 0.25rem;">${p}</bim-label>
          `);
      let g;
      if (s && "viewDefinition" in h.ifcMetadata) {
        const x = h.ifcMetadata.viewDefinition;
        g = y`
          ${x.split(",").map((m) => y`<bim-label style="background-color: var(--bim-ui_main-base); padding: 0 0.25rem; color: var(--bim-ui_main-contrast); border-radius: 0.25rem;">${m}</bim-label>`)}
        `;
      }
      return y`
       <div style="display: flex; flex: 1; gap: var(--bim-ui_size-4xs); justify-content: space-between; overflow: auto;">
        <div style="display: flex; flex-wrap: wrap; align-items: center; gap: 0 var(--bim-ui_size-4xs); flex-grow: 1; overflow: auto;">
          <div style="min-height: 1.75rem; overflow: auto; display: flex;">
            <bim-label style="white-space: normal;">${a}</bim-label>
          </div>
          <div style="display: flex; flex-wrap: wrap; gap: var(--bim-ui_size-4xs); overflow: auto;">
            ${c}
            ${g}
          </div>
        </div>
        <div style="display: flex; gap: var(--bim-ui_size-4xs); align-self: flex-start; flex-shrink: 0;">
          <bim-button @click=${(x) => {
        const m = t.get(_n), b = x.target;
        m.set(b.hasAttribute("data-model-hidden"), d), b.toggleAttribute("data-model-hidden"), b.icon = b.hasAttribute("data-model-hidden") ? "mdi:eye-off" : "mdi:eye";
      }} icon="mdi:eye"></bim-button>
          <bim-button @click=${() => n.disposeGroup(h)} icon="mdi:delete"></bim-button>
        </div>
       </div>
      `;
    }
  }, o.data = r, y`
    <div>
      ${r.length === 0 ? y`<bim-label>No models has been loaded yet</bim-label>` : o}
    </div>
  `;
};
var wc = (i, t = true) => {
  const e = $t.create(
    xc2,
    i
  );
  if (t) {
    const { components: s } = i, n = s.get(ut), [, o] = e;
    n.onFragmentsLoaded.add(() => setTimeout(() => o())), n.onFragmentsDisposed.add(() => o());
  }
  return e;
};
var $c = Object.freeze(Object.defineProperty({
  __proto__: null,
  modelsList: wc
}, Symbol.toStringTag, { value: "Module" }));
var Xo = [
  "Name",
  "ContainedInStructure",
  "ForLayerSet",
  "LayerThickness",
  "HasProperties",
  "HasAssociations",
  "HasAssignments",
  "HasPropertySets",
  "PredefinedType",
  "Quantities",
  "ReferencedSource",
  "Identification",
  (i) => i.includes("Value"),
  (i) => i.startsWith("Material"),
  (i) => i.startsWith("Relating"),
  (i) => {
    const t = ["IsGroupedBy", "IsDecomposedBy"];
    return i.startsWith("Is") && !t.includes(i);
  }
];
async function Ke(i, t, e, s = Xo, n = false) {
  const o = i.get(ke), r = await t.getProperties(e);
  if (!r)
    return {
      data: { Entity: `${e} properties not found...` }
      // onRowCreated(row) {
      //   row.addEventListener("cellcreated", (event) => {
      //     if (!(event instanceof CustomEvent)) return;
      //     const { cell } = event.detail;
      //     cell.style.gridColumn = "1 / -1";
      //   });
      // },
    };
  const a = o.relationMaps[t.uuid], l = {
    data: {}
  };
  for (const u in r) {
    const h = s.map((c) => typeof c == "string" ? u === c : c(u)).includes(true);
    if (!(u === "type" || h))
      continue;
    const d = r[u];
    if (d)
      if (d.type === 5) {
        l.children || (l.children = []);
        const c = await Ke(
          i,
          t,
          d.value,
          s,
          n
        );
        l.children.push(c);
      } else if (typeof d == "object" && !Array.isArray(d)) {
        const { value: c, type: p } = d;
        if (n)
          p === 1 || p === 2 || p === 3 || (l.data[u] = c);
        else {
          const g = typeof c == "number" ? Number(c.toFixed(3)) : c;
          l.data[u] = g;
        }
      } else if (Array.isArray(d))
        for (const c of d) {
          if (c.type !== 5)
            continue;
          l.children || (l.children = []);
          const p = await Ke(
            i,
            t,
            c.value,
            s,
            n
          );
          l.children.push(p);
        }
      else if (u === "type") {
        const c = Bn[d];
        l.data.Entity = c;
      } else
        l.data[u] = d;
  }
  if (a && a.get(r.expressID)) {
    const u = a.get(r.expressID);
    for (const h of s) {
      const d = [];
      if (typeof h == "string") {
        const c = o._inverseAttributes.indexOf(
          h
        );
        c !== -1 && d.push(c);
      } else {
        const c = o._inverseAttributes.filter(
          (p) => h(p)
        );
        for (const p of c) {
          const g = o._inverseAttributes.indexOf(p);
          d.push(g);
        }
      }
      for (const c of d) {
        const p = u.get(c);
        if (p)
          for (const g of p) {
            const w = await Ke(
              i,
              t,
              g,
              s,
              n
            );
            l.children || (l.children = []), l.children.push(w);
          }
      }
    }
  }
  return l;
}
var Cc = (i) => {
  const {
    components: t,
    fragmentIdMap: e,
    attributesToInclude: s,
    editable: n,
    tableDefinition: o
  } = i, r = t.get(ut);
  let a;
  return typeof s == "function" ? a = s(Xo) : a = s, y`<bim-table ${Q(async (u) => {
    if (!u)
      return;
    const h = u, d = [], c = [];
    for (const p in e) {
      const g = r.list.get(p);
      if (!(g && g.group))
        continue;
      const w = g.group, _ = c.find((x) => x.model === w);
      if (_)
        for (const x of e[p])
          _.expressIDs.add(x);
      else {
        const x = { model: w, expressIDs: new Set(e[p]) };
        c.push(x);
      }
    }
    for (const p of c) {
      const { model: g, expressIDs: w } = p;
      for (const _ of w) {
        const x = await Ke(
          t,
          g,
          _,
          a,
          n
        );
        d.push(x);
      }
    }
    h.dataTransform = o, h.data = d, h.columns = [{ name: "Entity", width: "minmax(15rem, 1fr)" }];
  })}></bim-table>`;
};
var Ec = (i) => $t.create(
  Cc,
  i
);
var Ac = Object.freeze(Object.defineProperty({
  __proto__: null,
  entityAttributes: Ec
}, Symbol.toStringTag, { value: "Module" }));
function Sc(i) {
  const t = Object.keys(i).pop();
  return t && i[t].length > 0 ? i[t][0] : "";
}
function Jo(i) {
  return i.map((t) => {
    const e = {
      data: {
        System: Sc(t.filter)
        // Actions: JSON.stringify(node.filter),
      }
    };
    return t.children && t.children.length > 0 && (e.children = Jo(t.children)), e;
  });
}
var kc = (i) => {
  const { components: t, classifications: e } = i, s = t.get(Ai), n = (o) => {
    if (!o)
      return;
    const r = o;
    r.dataTransform = {
      Actions: (u) => u
    };
    const a = (u, h = {}) => {
      const d = s.list, c = u[0], p = d[c], g = [];
      if (!c || !p)
        return g;
      for (const w in p) {
        const _ = { ...h, [c]: [w] }, x = s.find(_);
        if (Object.keys(x).length > 0) {
          const b = { filter: _ };
          b.children = a(u.slice(1), _), g.push(b);
        }
      }
      return g;
    }, l = [];
    for (const u in e) {
      const h = e[u], d = a(h), c = Jo(d);
      l.push({
        data: { System: u },
        children: c
      });
    }
    r.data = l;
  };
  return y`
  <div>
    ${Object.keys(e).length === 0 ? y`<bim-label label="No classifications to show"></bim-label>` : y`<bim-table ${Q(n)} headers-hidden expanded></bim-table>`}
  </div>
  `;
};
var Oc = (i, t = true) => {
  const e = $t.create(
    kc,
    i
  );
  if (t) {
    const { components: s } = i, n = s.get(ut), [, o] = e;
    n.onFragmentsDisposed.add(() => o());
  }
  return e;
};
var Tc = Object.freeze(Object.defineProperty({
  __proto__: null,
  classificationTree: Oc
}, Symbol.toStringTag, { value: "Module" }));
var Ic = ["OwnerHistory", "ObjectPlacement", "CompositionType"];
var ls = async (i, t, e) => {
  const n = { ...{
    groupName: "Attributes",
    includeClass: false
  }, ...e }, { groupName: o, includeClass: r } = n, a = await i.getProperties(t) ?? {}, l = { data: { Name: o } };
  r && (l.children || (l.children = []), l.children.push({
    data: {
      Name: "Class",
      Value: Bn[a.type]
    }
  }));
  for (const u in a) {
    if (Ic.includes(u))
      continue;
    const h = a[u];
    if (h && typeof h == "object" && !Array.isArray(h)) {
      if (h.type === REF)
        continue;
      const d = {
        data: { Name: u, Value: h.value }
      };
      l.children || (l.children = []), l.children.push(d);
    }
  }
  return l;
};
var Pc = async (i, t) => {
  const e = { data: { Name: "Property Sets" } };
  for (const s of t) {
    const n = await i.getProperties(s);
    if (!n)
      continue;
    const o = {
      data: { Name: n.Name.value }
    };
    if (n.type === IFCPROPERTYSET) {
      for (const r of n.HasProperties) {
        const { value: a } = r, l = await i.getProperties(a);
        if (!l)
          continue;
        const u = Object.keys(l).find(
          (d) => d.includes("Value")
        );
        if (!(u && l[u]))
          continue;
        const h = {
          data: {
            Name: l.Name.value,
            Value: l[u].value
          }
        };
        o.children || (o.children = []), o.children.push(h);
      }
      o.children && (e.children || (e.children = []), e.children.push(o));
    }
  }
  return e;
};
var Rc = async (i, t) => {
  const e = { data: { Name: "Quantity Sets" } };
  for (const s of t) {
    const n = await i.getProperties(s);
    if (!n)
      continue;
    const o = {
      data: { Name: n.Name.value }
    };
    if (n.type === IFCELEMENTQUANTITY) {
      for (const r of n.Quantities) {
        const { value: a } = r, l = await i.getProperties(a);
        if (!l)
          continue;
        const u = Object.keys(l).find(
          (d) => d.includes("Value")
        );
        if (!(u && l[u]))
          continue;
        const h = {
          data: {
            Name: l.Name.value,
            Value: l[u].value
          }
        };
        o.children || (o.children = []), o.children.push(h);
      }
      o.children && (e.children || (e.children = []), e.children.push(o));
    }
  }
  return e;
};
var Nc = async (i, t) => {
  const e = { data: { Name: "Materials" } };
  for (const s of t) {
    const n = await i.getProperties(s);
    if (n && n.type === IFCMATERIALLAYERSETUSAGE) {
      const o = n.ForLayerSet.value, r = await i.getProperties(o);
      if (!r)
        continue;
      for (const a of r.MaterialLayers) {
        const { value: l } = a, u = await i.getProperties(l);
        if (!u)
          continue;
        const h = await i.getProperties(
          u.Material.value
        );
        if (!h)
          continue;
        const d = {
          data: {
            Name: "Layer"
          },
          children: [
            {
              data: {
                Name: "Thickness",
                Value: u.LayerThickness.value
              }
            },
            {
              data: {
                Name: "Material",
                Value: h.Name.value
              }
            }
          ]
        };
        e.children || (e.children = []), e.children.push(d);
      }
    }
    if (n && n.type === IFCMATERIALLIST)
      for (const o of n.Materials) {
        const { value: r } = o, a = await i.getProperties(r);
        if (!a)
          continue;
        const l = {
          data: {
            Name: "Name",
            Value: a.Name.value
          }
        };
        e.children || (e.children = []), e.children.push(l);
      }
    if (n && n.type === IFCMATERIAL) {
      const o = await i.getProperties(s);
      if (!o)
        continue;
      const r = {
        data: {
          Name: "Name",
          Value: o.Name.value
        }
      };
      e.children || (e.children = []), e.children.push(r);
    }
  }
  return e;
};
var Mc = async (i, t) => {
  var s, n;
  const e = { data: { Name: "Classifications" } };
  for (const o of t) {
    const r = await i.getProperties(o);
    if (r && r.type === IFCCLASSIFICATIONREFERENCE) {
      const { value: a } = r.ReferencedSource, l = await i.getProperties(a);
      if (!l)
        continue;
      const u = {
        data: {
          Name: l.Name.value
        },
        children: [
          {
            data: {
              Name: "Identification",
              Value: ((s = r.Identification) == null ? void 0 : s.value) || ((n = r.ItemReference) == null ? void 0 : n.value)
            }
          },
          {
            data: {
              Name: "Name",
              Value: r.Name.value
            }
          }
        ]
      };
      e.children || (e.children = []), e.children.push(u);
    }
  }
  return e;
};
var Lc = async (i, t) => {
  var r;
  const e = i.get(ke), s = i.get(ut), n = [], o = [];
  for (const a in t) {
    const l = s.list.get(a);
    if (!(l && l.group))
      continue;
    const u = l.group, h = o.find((d) => d.model === u);
    if (h)
      for (const d of t[a])
        h.expressIDs.add(d);
    else {
      const d = { model: u, expressIDs: new Set(t[a]) };
      o.push(d);
    }
  }
  for (const a in o) {
    const { model: l, expressIDs: u } = o[a], h = e.relationMaps[l.uuid];
    if (h)
      for (const d of u) {
        const c = await l.getProperties(d);
        if (!c)
          continue;
        const p = {
          data: {
            Name: (r = c.Name) == null ? void 0 : r.value
          }
        };
        n.push(p);
        const g = await ls(l, d, {
          includeClass: true
        });
        if (p.children || (p.children = []), p.children.push(g), !h.get(d))
          continue;
        const _ = e.getEntityRelations(
          l,
          d,
          "IsDefinedBy"
        );
        if (_) {
          const b = _.filter(async (E) => {
            const A = await l.getProperties(E);
            return A ? A.type === IFCPROPERTYSET : false;
          }), v = await Pc(l, b);
          v.children && p.children.push(v);
          const $ = _.filter(async (E) => {
            const A = await l.getProperties(E);
            return A ? A.type === IFCELEMENTQUANTITY : false;
          }), C = await Rc(l, $);
          C.children && p.children.push(C);
        }
        const x = e.getEntityRelations(
          l,
          d,
          "HasAssociations"
        );
        if (x) {
          const b = x.filter(async (E) => {
            const A = await l.getProperties(E);
            return A ? A.type === IFCMATERIALLAYERSETUSAGE || A.type === IFCMATERIALLAYERSET || A.type === IFCMATERIALLAYER || A.type === IFCMATERIAL || A.type === IFCMATERIALLIST : false;
          }), v = await Nc(l, b);
          v.children && p.children.push(v);
          const $ = x.filter(
            async (E) => {
              const A = await l.getProperties(E);
              return A ? A.type === IFCCLASSIFICATIONREFERENCE : false;
            }
          ), C = await Mc(
            l,
            $
          );
          C.children && p.children.push(C);
        }
        const m = e.getEntityRelations(
          l,
          d,
          "ContainedInStructure"
        );
        if (m) {
          const b = m[0], v = await ls(l, b, {
            groupName: "SpatialContainer"
          });
          p.children.push(v);
        }
      }
  }
  return n;
};
var zc = new Event("datacomputed");
var pt;
var Dc = (i) => {
  const { components: t, fragmentIdMap: e } = i;
  return pt || (pt = document.createElement("bim-table"), pt.columns = [{ name: "Name", width: "12rem" }], pt.headersHidden = true, pt.addEventListener("cellcreated", ({ detail: s }) => {
    const { cell: n } = s;
    n.column === "Name" && !("Value" in n.rowData) && (n.style.gridColumn = "1 / -1");
  })), Lc(t, e).then((s) => {
    pt.data = s, s.length !== 0 && pt.dispatchEvent(zc);
  }), y`${pt}`;
};
var jc = (i) => $t.create(
  Dc,
  i
);
var Hc = Object.freeze(Object.defineProperty({
  __proto__: null,
  elementProperties: jc
}, Symbol.toStringTag, { value: "Module" }));
var Ki = async (i, t, e, s) => {
  var u;
  const n = [], o = i.get(ke), r = await t.getProperties(e);
  if (!r)
    return n;
  const { type: a } = r, l = {
    data: {
      Entity: Bn[a],
      Name: (u = r.Name) == null ? void 0 : u.value,
      modelID: t.uuid
    }
  };
  for (const h of s) {
    const d = o.getEntityRelations(t, e, h);
    if (l.data.expressID = e, !!d) {
      l.data.relations = JSON.stringify(d);
      for (const c of d) {
        const p = await Ki(
          i,
          t,
          c,
          s
        );
        l.children || (l.children = []), l.children.push(...p);
      }
    }
  }
  return n.push(l), n;
};
var Fc = async (i, t, e, s) => {
  const n = i.get(ke), o = [];
  for (const r of t) {
    let a;
    if (s)
      a = {
        data: {
          Entity: r.name !== "" ? r.name : r.uuid
        },
        children: await Ki(
          i,
          r,
          s,
          e
        )
      };
    else {
      const l = n.relationMaps[r.uuid], u = await r.getAllPropertiesOfType(
        IFCPROJECT
      );
      if (!(l && u))
        continue;
      const { expressID: h } = Object.values(u)[0];
      a = {
        data: {
          Entity: r.name !== "" ? r.name : r.uuid
        },
        children: await Ki(
          i,
          r,
          h,
          e
        )
      };
    }
    o.push(a);
  }
  return o;
};
var ot;
var cs = (i, t) => {
  const e = i.get(ut), { modelID: s, expressID: n, relations: o } = t.data;
  if (!(s && n))
    return null;
  const r = e.groups.get(s);
  return r ? r.getFragmentMap([
    n,
    ...JSON.parse(o ?? "[]")
  ]) : null;
};
var Bc = (i) => {
  const { components: t, models: e, expressID: s } = i, n = i.selectHighlighterName ?? "select", o = i.hoverHighlighterName ?? "hover";
  ot || (ot = document.createElement("bim-table"), ot.hiddenColumns = ["modelID", "expressID", "relations"], ot.columns = ["Entity", "Name"], ot.headersHidden = true, ot.addEventListener("cellcreated", ({ detail: a }) => {
    const { cell: l } = a;
    l.column === "Entity" && !("Name" in l.rowData) && (l.style.gridColumn = "1 / -1");
  })), ot.addEventListener("rowcreated", (a) => {
    a.stopImmediatePropagation();
    const { row: l } = a.detail, u = t.get(xc);
    l.onmouseover = () => {
      if (!o)
        return;
      const h = cs(t, l);
      h && Object.keys(h).length !== 0 && (l.style.backgroundColor = "var(--bim-ui_bg-contrast-20)", u.highlightByID(
        o,
        h,
        true,
        false,
        u.selection[n] ?? {}
      ));
    }, l.onmouseout = () => {
      l.style.backgroundColor = "", u.clear(o);
    }, l.onclick = () => {
      if (!n)
        return;
      const h = cs(t, l);
      h && Object.keys(h).length !== 0 && u.highlightByID(
        n,
        h,
        true,
        true
      );
    };
  });
  const r = i.inverseAttributes ?? [
    "IsDecomposedBy",
    "ContainsElements"
  ];
  return Fc(t, e, r, s).then(
    (a) => ot.data = a
  ), y`${ot}`;
};
var Uc = (i, t = true) => {
  const e = $t.create(
    Bc,
    i
  );
  if (t) {
    const [, s] = e, { components: n } = i, o = n.get(ut), r = n.get(ke), a = () => s({ models: o.groups.values() });
    r.onRelationsIndexed.add(a), o.onFragmentsDisposed.add(a);
  }
  return e;
};
var Vc = Object.freeze(Object.defineProperty({
  __proto__: null,
  relationsTree: Uc
}, Symbol.toStringTag, { value: "Module" }));
var fe = (i, t) => [...i.get(dn).list.values()].find((n) => n.world === t);
var Wc = (i, t) => y`
    <bim-color-input @input=${(s) => {
  const n = s.target;
  i.color = new Color(n.color);
}} color=${t}></bim-color-input>
  `;
var Gc = (i, t) => {
  const { postproduction: e } = i, s = e.n8ao.configuration;
  return y`
    <bim-color-input @input=${(o) => {
    const r = o.target;
    s.color = new Color(r.color);
  }} color=${t}></bim-color-input>
  `;
};
var qc = (i, t) => {
  const { color: e, opacity: s } = JSON.parse(t), { postproduction: n } = i, { customEffects: o } = n;
  return y`
    <bim-color-input @input=${(a) => {
    const { color: l, opacity: u } = a.target;
    o.lineColor = new Color(l).getHex(), u && (o.opacity = u / 100);
  }} color=${e} opacity=${s * 100}></bim-color-input>
  `;
};
var Yc = (i, t) => y`
    <bim-color-input @input=${(s) => {
  const n = s.target, o = new Color(n.color);
  i.material.uniforms.uColor.value = o;
}} color=${t}></bim-color-input>
  `;
var Xc = (i, t) => {
  const { postproduction: e } = i;
  return y`
    <bim-checkbox @change=${(n) => {
    const o = n.target;
    e.setPasses({ ao: o.checked });
  }} .checked=${t}></bim-checkbox>
  `;
};
var Jc = (i, t) => {
  const { postproduction: e } = i;
  return y`
    <bim-checkbox @change=${(n) => {
    const o = n.target;
    e.setPasses({ gamma: o.checked });
  }} .checked=${t}></bim-checkbox>
  `;
};
var Qc = (i, t) => {
  const { postproduction: e } = i;
  return y`
    <bim-checkbox @change=${(n) => {
    const o = n.target;
    e.setPasses({ custom: o.checked });
  }} .checked=${t}></bim-checkbox>
  `;
};
var ft = (i, t, e, s = () => {
}) => y`
    <bim-checkbox .checked="${e}" @change="${(o) => {
  const a = o.target.checked;
  i[t] = a, s(a);
}}"></bim-checkbox> 
  `;
var R = (i, t, e, s) => {
  const n = {
    slider: false,
    min: 0,
    max: 100,
    step: 1,
    prefix: null,
    suffix: null,
    onInputSet: () => {
    },
    ...s
  }, { slider: o, min: r, max: a, step: l, suffix: u, prefix: h, onInputSet: d } = n;
  return y`
    <bim-number-input
      .pref=${h}
      .suffix=${u}
      .slider=${o} 
      min=${r} 
      value="${e}" 
      max=${a} 
      step=${l} 
      @change="${(p) => {
    const w = p.target.value;
    i[t] = w, d(w);
  }}"
    ></bim-number-input> 
  `;
};
var Kc = (i) => {
  const { components: t } = i, e = t.get(Ci);
  return y`<bim-table ${Q(async (n) => {
    var a, l, u, h, d;
    if (!n)
      return;
    const o = n;
    o.preserveStructureOnFilter = true, o.dataTransform = {
      Value: (c, p) => {
        const g = p.World, w = e.list.get(g);
        if (!w)
          return c;
        const { scene: _, camera: x, renderer: m } = w, b = p.Name;
        if (b === "Grid" && p.IsGridConfig && typeof c == "boolean") {
          const v = fe(t, w);
          return v ? ft(v, "visible", c) : c;
        }
        if (b === "Color" && p.IsGridConfig && typeof c == "string") {
          const v = fe(t, w);
          return v ? Yc(v, c) : c;
        }
        if (b === "Distance" && p.IsGridConfig && typeof c == "number") {
          const v = fe(t, w);
          return v ? R(
            v.material.uniforms.uDistance,
            "value",
            c,
            { slider: true, min: 300, max: 1e3 }
          ) : c;
        }
        if (b === "Size" && p.IsGridConfig && typeof c == "string") {
          const v = fe(t, w);
          if (!v)
            return c;
          const { x: $, y: C } = JSON.parse(c), E = R(
            v.material.uniforms.uSize1,
            "value",
            $,
            { slider: true, suffix: "m", prefix: "A", min: 1, max: 20 }
          ), A = R(
            v.material.uniforms.uSize2,
            "value",
            C,
            { slider: true, suffix: "m", prefix: "B", min: 1, max: 20 }
          );
          return y`
            <div style="display: flex; gap: 0.25rem; width: 100%; flex-wrap: wrap">${E}${A}</div>
          `;
        }
        if (b === "Near Frustum" && x.three instanceof PerspectiveCamera && typeof c == "number") {
          const v = x.three;
          return R(x.three, "near", c, {
            slider: true,
            min: 0.1,
            max: 10,
            step: 0.1,
            onInputSet: () => v.updateProjectionMatrix()
          });
        }
        if (b === "Far Frustum" && x.three instanceof PerspectiveCamera && typeof c == "number") {
          const v = x.three;
          return R(x.three, "far", c, {
            slider: true,
            min: 300,
            max: 2e3,
            step: 10,
            onInputSet: () => v.updateProjectionMatrix()
          });
        }
        if (b === "Field of View" && x.three instanceof PerspectiveCamera && typeof c == "number") {
          const v = x.three;
          return R(x.three, "fov", c, {
            slider: true,
            min: 10,
            max: 120,
            onInputSet: () => v.updateProjectionMatrix()
          });
        }
        if (b === "Invert Drag" && x.hasCameraControls() && typeof c == "boolean")
          return ft(
            x.controls,
            "dollyDragInverted",
            c
          );
        if (b === "Dolly Speed" && x.hasCameraControls() && typeof c == "number")
          return R(x.controls, "dollySpeed", c, {
            slider: true,
            min: 0.5,
            max: 3,
            step: 0.1
          });
        if (b === "Truck Speed" && x.hasCameraControls() && typeof c == "number")
          return R(x.controls, "truckSpeed", c, {
            slider: true,
            min: 0.5,
            max: 6,
            step: 0.1
          });
        if (b === "Smooth Time" && x.hasCameraControls() && typeof c == "number")
          return R(x.controls, "smoothTime", c, {
            slider: true,
            min: 0.01,
            max: 2,
            step: 0.01
          });
        if (b === "Intensity" && typeof c == "number") {
          if (p.Light && typeof p.Light == "string") {
            const v = _.three.children.find(
              ($) => $.uuid === p.Light
            );
            return v && v instanceof Light ? R(v, "intensity", c, {
              slider: true,
              min: 0,
              max: 10,
              step: 0.1
            }) : c;
          }
          if (p.IsAOConfig && m instanceof xa)
            return R(
              m.postproduction.n8ao.configuration,
              "intensity",
              c,
              { slider: true, max: 16, step: 0.1 }
            );
        }
        if (b === "Color" && typeof c == "string") {
          const v = p.Light, $ = _.three.children.find(
            (C) => C.uuid === v
          );
          if ($ && $ instanceof Light)
            return Wc($, c);
          if (p.IsAOConfig && m instanceof xa)
            return Gc(m, c);
        }
        if (b === "Ambient Oclussion" && typeof c == "boolean" && p.IsAOConfig && m instanceof xa)
          return Xc(m, c);
        if (b === "Half Resolution" && p.IsAOConfig && m instanceof xa && typeof c == "boolean")
          return ft(
            m.postproduction.n8ao.configuration,
            "halfRes",
            c
          );
        if (b === "Screen Space Radius" && p.IsAOConfig && m instanceof xa && typeof c == "boolean")
          return ft(
            m.postproduction.n8ao.configuration,
            "screenSpaceRadius",
            c
          );
        if (b === "Radius" && p.IsAOConfig && m instanceof xa && typeof c == "number")
          return R(
            m.postproduction.n8ao.configuration,
            "aoRadius",
            c,
            { slider: true, max: 2, step: 0.1 }
          );
        if (b === "Denoise Samples" && p.IsAOConfig && m instanceof xa && typeof c == "number")
          return R(
            m.postproduction.n8ao.configuration,
            "denoiseSamples",
            c,
            { slider: true, min: 1, max: 16 }
          );
        if (b === "Samples" && p.IsAOConfig && m instanceof xa && typeof c == "number")
          return R(
            m.postproduction.n8ao.configuration,
            "aoSamples",
            c,
            { slider: true, min: 1, max: 16 }
          );
        if (b === "Denoise Radius" && p.IsAOConfig && m instanceof xa && typeof c == "number")
          return R(
            m.postproduction.n8ao.configuration,
            "denoiseRadius",
            c,
            { slider: true, min: 0, max: 16, step: 0.1 }
          );
        if (b === "Distance Falloff" && p.IsAOConfig && m instanceof xa && typeof c == "number")
          return R(
            m.postproduction.n8ao.configuration,
            "distanceFalloff",
            c,
            { slider: true, min: 0, max: 4, step: 0.1 }
          );
        if (b === "Directional Light" && p.Light && typeof p.Light == "string" && typeof c == "boolean") {
          const v = _.three.children.find(
            ($) => $.uuid === p.Light
          );
          return v && v instanceof Light ? ft(v, "visible", c) : c;
        }
        if (b === "Ambient Light" && p.Light && typeof p.Light == "string" && typeof c == "boolean") {
          const v = _.three.children.find(
            ($) => $.uuid === p.Light
          );
          return v && v instanceof Light ? ft(v, "visible", c) : c;
        }
        if (b === "Position" && p.Light && typeof p.Light == "string" && typeof c == "string") {
          const v = _.three.children.find(
            (G) => G.uuid === p.Light
          );
          if (!(v && v instanceof Light))
            return c;
          const { x: $, y: C, z: E } = JSON.parse(c), A = R(v.position, "x", $, {
            slider: true,
            prefix: "X",
            suffix: "m",
            min: -50,
            max: 50
          }), j = R(v.position, "y", C, {
            slider: true,
            prefix: "Y",
            suffix: "m",
            min: -50,
            max: 50
          }), O = R(v.position, "z", E, {
            slider: true,
            prefix: "Z",
            suffix: "m",
            min: -50,
            max: 50
          });
          return y`
            <div style="display: flex; gap: 0.25rem; width: 100%; flex-wrap: wrap">${A}${j}${O}</div>
          `;
        }
        return b === "Custom Effects" && p.IsCEConfig && m instanceof xa && typeof c == "boolean" ? Qc(m, c) : b === "Color" && p.IsOutlineConfig && m instanceof xa && typeof c == "string" ? qc(m, c) : b === "Tolerance" && p.IsOutlineConfig && m instanceof xa && typeof c == "number" ? R(
          m.postproduction.customEffects,
          "tolerance",
          c,
          { slider: true, min: 0, max: 6, step: 0.01 }
        ) : b === "Outline" && p.IsOutlineConfig && m instanceof xa && typeof c == "boolean" ? ft(
          m.postproduction.customEffects,
          "outlineEnabled",
          c
        ) : b === "Gloss" && p.IsGlossConfig && m instanceof xa && typeof c == "boolean" ? ft(
          m.postproduction.customEffects,
          "glossEnabled",
          c
        ) : b === "Min" && p.IsGlossConfig && m instanceof xa && typeof c == "number" ? R(
          m.postproduction.customEffects,
          "minGloss",
          c,
          { slider: true, min: -0.5, max: 0.5, step: 0.01 }
        ) : b === "Max" && p.IsGlossConfig && m instanceof xa && typeof c == "number" ? R(
          m.postproduction.customEffects,
          "maxGloss",
          c,
          { slider: true, min: -0.5, max: 0.5, step: 0.01 }
        ) : b === "Exponent" && p.IsGlossConfig && m instanceof xa && typeof c == "number" ? R(
          m.postproduction.customEffects,
          "glossExponent",
          c,
          { slider: true, min: 0, max: 5, step: 0.01 }
        ) : b === "Gamma Correction" && p.IsGammaConfig && m instanceof xa && typeof c == "boolean" ? Jc(m, c) : c;
      }
    }, o.addEventListener("cellcreated", ({ detail: c }) => {
      const p = c.cell.parentNode;
      if (!p)
        return;
      const g = p.querySelector(
        "bim-table-cell[column='Name']"
      ), w = p.querySelector(
        "bim-table-cell[column='Value']"
      );
      g && !w && (g.style.gridColumn = "1 / -1");
    });
    const r = [];
    for (const [, c] of e.list) {
      const { scene: p, camera: g, renderer: w } = c, _ = fe(t, c), x = {
        data: {
          Name: c instanceof Fr && c.name || c.uuid
        },
        children: []
      };
      if (p) {
        const m = {
          data: {
            Name: "Scene"
          }
        };
        if (_) {
          const $ = {
            data: {
              Name: "Grid",
              Value: _.three.visible,
              World: c.uuid,
              IsGridConfig: true
            },
            children: [
              {
                data: {
                  Name: "Color",
                  get Value() {
                    return `#${_.material.uniforms.uColor.value.getHexString()}`;
                  },
                  World: c.uuid,
                  IsGridConfig: true
                }
              },
              {
                data: {
                  Name: "Size",
                  get Value() {
                    const C = _.material.uniforms.uSize1.value, E = _.material.uniforms.uSize2.value;
                    return JSON.stringify({ x: C, y: E });
                  },
                  World: c.uuid,
                  IsGridConfig: true
                }
              },
              {
                data: {
                  Name: "Distance",
                  Value: _.material.uniforms.uDistance.value,
                  World: c.uuid,
                  IsGridConfig: true
                }
              }
            ]
          };
          m.children || (m.children = []), m.children.push($);
        }
        const b = p.three.children.filter(
          ($) => $ instanceof DirectionalLight
        );
        for (const $ of b) {
          const C = {
            data: {
              Name: "Directional Light",
              Value: $.visible,
              World: c.uuid,
              Light: $.uuid
            },
            children: [
              {
                data: {
                  Name: "Position",
                  Value: JSON.stringify($.position),
                  World: c.uuid,
                  Light: $.uuid
                }
              },
              {
                data: {
                  Name: "Intensity",
                  Value: $.intensity,
                  World: c.uuid,
                  Light: $.uuid
                }
              },
              {
                data: {
                  Name: "Color",
                  Value: `#${$.color.getHexString()}`,
                  World: c.uuid,
                  Light: $.uuid
                }
              }
            ]
          };
          m.children || (m.children = []), m.children.push(C);
        }
        const v = p.three.children.filter(
          ($) => $ instanceof AmbientLight
        );
        for (const $ of v) {
          const C = {
            data: {
              Name: "Ambient Light",
              Value: $.visible,
              World: c.uuid,
              Light: $.uuid
            },
            children: [
              {
                data: {
                  Name: "Intensity",
                  Value: $.intensity,
                  World: c.uuid,
                  Light: $.uuid
                }
              },
              {
                data: {
                  Name: "Color",
                  Value: `#${$.color.getHexString()}`,
                  World: c.uuid,
                  Light: $.uuid
                }
              }
            ]
          };
          m.children || (m.children = []), m.children.push(C);
        }
        m.children && ((a = m.children) == null ? void 0 : a.length) > 0 && ((l = x.children) == null || l.push(m));
      }
      if (g.three instanceof PerspectiveCamera) {
        const m = {
          data: {
            Name: "Perspective Camera"
          },
          children: [
            {
              data: {
                Name: "Near Frustum",
                Value: g.three.near,
                World: c.uuid
              }
            },
            {
              data: {
                Name: "Far Frustum",
                Value: g.three.far,
                World: c.uuid
              }
            },
            {
              data: {
                Name: "Field of View",
                Value: g.three.fov,
                World: c.uuid
              }
            }
          ]
        };
        if (g.hasCameraControls()) {
          const { controls: b } = g, v = {
            dollyDragInverted: "Invert Drag",
            dollySpeed: "Dolly Speed",
            truckSpeed: "Truck Speed",
            smoothTime: "Smooth Time"
          };
          for (const $ in v) {
            const C = b[$];
            C != null && ((u = m.children) == null || u.push({
              data: {
                Name: v[$],
                Value: C,
                World: c.uuid
              }
            }));
          }
        }
        (h = x.children) == null || h.push(m);
      }
      if (w instanceof xa) {
        const { postproduction: m } = w, b = m.n8ao.configuration, v = {
          data: {
            Name: "Renderer"
          },
          children: [
            {
              data: {
                Name: "Gamma Correction",
                Value: m.settings.gamma ?? false,
                World: c.uuid,
                IsGammaConfig: true
              }
            },
            {
              data: {
                Name: "Ambient Oclussion",
                Value: m.settings.ao ?? false,
                World: c.uuid,
                IsAOConfig: true
              },
              children: [
                {
                  data: {
                    Name: "Samples",
                    Value: b.aoSamples,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Color",
                    Value: `#${b.color.getHexString()}`,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Half Resolution",
                    Value: b.halfRes,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Screen Space Radius",
                    Value: b.screenSpaceRadius,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Radius",
                    Value: b.aoRadius,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Intensity",
                    Value: b.intensity,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Distance Falloff",
                    Value: b.distanceFalloff,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Denoise Samples",
                    Value: b.denoiseSamples,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                },
                {
                  data: {
                    Name: "Denoise Radius",
                    Value: b.denoiseRadius,
                    World: c.uuid,
                    IsAOConfig: true
                  }
                }
              ]
            },
            {
              data: {
                Name: "Custom Effects",
                Value: m.settings.custom ?? false,
                World: c.uuid,
                IsCEConfig: true
              },
              children: [
                {
                  data: {
                    Name: "Gloss",
                    Value: m.customEffects.glossEnabled,
                    World: c.uuid,
                    IsGlossConfig: true
                  },
                  children: [
                    {
                      data: {
                        Name: "Min",
                        Value: m.customEffects.minGloss,
                        World: c.uuid,
                        IsGlossConfig: true
                      }
                    },
                    {
                      data: {
                        Name: "Max",
                        Value: m.customEffects.maxGloss,
                        World: c.uuid,
                        IsGlossConfig: true
                      }
                    },
                    {
                      data: {
                        Name: "Exponent",
                        Value: m.customEffects.glossExponent,
                        World: c.uuid,
                        IsGlossConfig: true
                      }
                    }
                  ]
                },
                {
                  data: {
                    Name: "Outline",
                    Value: m.customEffects.outlineEnabled,
                    World: c.uuid,
                    IsOutlineConfig: true
                  },
                  children: [
                    {
                      data: {
                        Name: "Color",
                        get Value() {
                          const $ = new Color(
                            m.customEffects.lineColor
                          ), C = m.customEffects.opacity;
                          return JSON.stringify({
                            color: `#${$.getHexString()}`,
                            opacity: C
                          });
                        },
                        World: c.uuid,
                        IsOutlineConfig: true
                      }
                    },
                    {
                      data: {
                        Name: "Tolerance",
                        Value: m.customEffects.tolerance,
                        World: c.uuid,
                        IsOutlineConfig: true
                      }
                    }
                  ]
                }
              ]
            }
          ]
        };
        (d = x.children) == null || d.push(v);
      }
      r.push(x);
    }
    o.columns = [{ name: "Name", width: "11rem" }], o.hiddenColumns = [
      "World",
      "Light",
      "IsAOConfig",
      "IsCEConfig",
      "IsGlossConfig",
      "IsOutlineConfig",
      "IsGammaConfig",
      "IsGridConfig"
    ], o.data = r;
  })} headers-hidden expanded></bim-table>`;
};
var Zc = (i, t = true) => {
  const e = $t.create(
    Kc,
    i
  );
  if (t) {
    const [s] = e, n = () => e[1](), { components: o } = i, r = o.get(Ci);
    r.onDisposed.add(s.remove);
    for (const [, a] of r.list)
      a.onDisposed.add(n);
    s.addEventListener("disconnected", () => {
      r.onDisposed.remove(s.remove);
      for (const [, a] of r.list)
        a.onDisposed.remove(n);
    });
  }
  return e;
};
var tu = Object.freeze(Object.defineProperty({
  __proto__: null,
  worldsConfiguration: Zc
}, Symbol.toStringTag, { value: "Module" }));
var sh = {
  ...$c,
  ...Ac,
  ...Tc,
  ...Hc,
  ...Vc,
  ...tu
};
var Ze = globalThis;
var mn = Ze.ShadowRoot && (Ze.ShadyCSS === void 0 || Ze.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;
var bn = Symbol();
var us = /* @__PURE__ */ new WeakMap();
var Qo = class {
  constructor(t, e, s) {
    if (this._$cssResult$ = true, s !== bn)
      throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
    this.cssText = t, this.t = e;
  }
  get styleSheet() {
    let t = this.o;
    const e = this.t;
    if (mn && t === void 0) {
      const s = e !== void 0 && e.length === 1;
      s && (t = us.get(e)), t === void 0 && ((this.o = t = new CSSStyleSheet()).replaceSync(this.cssText), s && us.set(e, t));
    }
    return t;
  }
  toString() {
    return this.cssText;
  }
};
var eu = (i) => new Qo(typeof i == "string" ? i : i + "", void 0, bn);
var Ko = (i, ...t) => {
  const e = i.length === 1 ? i[0] : t.reduce((s, n, o) => s + ((r) => {
    if (r._$cssResult$ === true)
      return r.cssText;
    if (typeof r == "number")
      return r;
    throw Error("Value passed to 'css' function must be a 'css' function result: " + r + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
  })(n) + i[o + 1], i[0]);
  return new Qo(e, i, bn);
};
var iu = (i, t) => {
  if (mn)
    i.adoptedStyleSheets = t.map((e) => e instanceof CSSStyleSheet ? e : e.styleSheet);
  else
    for (const e of t) {
      const s = document.createElement("style"), n = Ze.litNonce;
      n !== void 0 && s.setAttribute("nonce", n), s.textContent = e.cssText, i.appendChild(s);
    }
};
var hs = mn ? (i) => i : (i) => i instanceof CSSStyleSheet ? ((t) => {
  let e = "";
  for (const s of t.cssRules)
    e += s.cssText;
  return eu(e);
})(i) : i;
var { is: nu, defineProperty: su, getOwnPropertyDescriptor: ou, getOwnPropertyNames: ru, getOwnPropertySymbols: au, getPrototypeOf: lu } = Object;
var vt = globalThis;
var ds = vt.trustedTypes;
var cu = ds ? ds.emptyScript : "";
var Ri2 = vt.reactiveElementPolyfillSupport;
var _e = (i, t) => i;
var ui = { toAttribute(i, t) {
  switch (t) {
    case Boolean:
      i = i ? cu : null;
      break;
    case Object:
    case Array:
      i = i == null ? i : JSON.stringify(i);
  }
  return i;
}, fromAttribute(i, t) {
  let e = i;
  switch (t) {
    case Boolean:
      e = i !== null;
      break;
    case Number:
      e = i === null ? null : Number(i);
      break;
    case Object:
    case Array:
      try {
        e = JSON.parse(i);
      } catch {
        e = null;
      }
  }
  return e;
} };
var gn = (i, t) => !nu(i, t);
var ps = { attribute: true, type: String, converter: ui, reflect: false, hasChanged: gn };
Symbol.metadata ?? (Symbol.metadata = Symbol("metadata")), vt.litPropertyMetadata ?? (vt.litPropertyMetadata = /* @__PURE__ */ new WeakMap());
var Ut = class extends HTMLElement {
  static addInitializer(t) {
    this._$Ei(), (this.l ?? (this.l = [])).push(t);
  }
  static get observedAttributes() {
    return this.finalize(), this._$Eh && [...this._$Eh.keys()];
  }
  static createProperty(t, e = ps) {
    if (e.state && (e.attribute = false), this._$Ei(), this.elementProperties.set(t, e), !e.noAccessor) {
      const s = Symbol(), n = this.getPropertyDescriptor(t, s, e);
      n !== void 0 && su(this.prototype, t, n);
    }
  }
  static getPropertyDescriptor(t, e, s) {
    const { get: n, set: o } = ou(this.prototype, t) ?? { get() {
      return this[e];
    }, set(r) {
      this[e] = r;
    } };
    return { get() {
      return n == null ? void 0 : n.call(this);
    }, set(r) {
      const a = n == null ? void 0 : n.call(this);
      o.call(this, r), this.requestUpdate(t, a, s);
    }, configurable: true, enumerable: true };
  }
  static getPropertyOptions(t) {
    return this.elementProperties.get(t) ?? ps;
  }
  static _$Ei() {
    if (this.hasOwnProperty(_e("elementProperties")))
      return;
    const t = lu(this);
    t.finalize(), t.l !== void 0 && (this.l = [...t.l]), this.elementProperties = new Map(t.elementProperties);
  }
  static finalize() {
    if (this.hasOwnProperty(_e("finalized")))
      return;
    if (this.finalized = true, this._$Ei(), this.hasOwnProperty(_e("properties"))) {
      const e = this.properties, s = [...ru(e), ...au(e)];
      for (const n of s)
        this.createProperty(n, e[n]);
    }
    const t = this[Symbol.metadata];
    if (t !== null) {
      const e = litPropertyMetadata.get(t);
      if (e !== void 0)
        for (const [s, n] of e)
          this.elementProperties.set(s, n);
    }
    this._$Eh = /* @__PURE__ */ new Map();
    for (const [e, s] of this.elementProperties) {
      const n = this._$Eu(e, s);
      n !== void 0 && this._$Eh.set(n, e);
    }
    this.elementStyles = this.finalizeStyles(this.styles);
  }
  static finalizeStyles(t) {
    const e = [];
    if (Array.isArray(t)) {
      const s = new Set(t.flat(1 / 0).reverse());
      for (const n of s)
        e.unshift(hs(n));
    } else
      t !== void 0 && e.push(hs(t));
    return e;
  }
  static _$Eu(t, e) {
    const s = e.attribute;
    return s === false ? void 0 : typeof s == "string" ? s : typeof t == "string" ? t.toLowerCase() : void 0;
  }
  constructor() {
    super(), this._$Ep = void 0, this.isUpdatePending = false, this.hasUpdated = false, this._$Em = null, this._$Ev();
  }
  _$Ev() {
    var t;
    this._$ES = new Promise((e) => this.enableUpdating = e), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), (t = this.constructor.l) == null || t.forEach((e) => e(this));
  }
  addController(t) {
    var e;
    (this._$EO ?? (this._$EO = /* @__PURE__ */ new Set())).add(t), this.renderRoot !== void 0 && this.isConnected && ((e = t.hostConnected) == null || e.call(t));
  }
  removeController(t) {
    var e;
    (e = this._$EO) == null || e.delete(t);
  }
  _$E_() {
    const t = /* @__PURE__ */ new Map(), e = this.constructor.elementProperties;
    for (const s of e.keys())
      this.hasOwnProperty(s) && (t.set(s, this[s]), delete this[s]);
    t.size > 0 && (this._$Ep = t);
  }
  createRenderRoot() {
    const t = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
    return iu(t, this.constructor.elementStyles), t;
  }
  connectedCallback() {
    var t;
    this.renderRoot ?? (this.renderRoot = this.createRenderRoot()), this.enableUpdating(true), (t = this._$EO) == null || t.forEach((e) => {
      var s;
      return (s = e.hostConnected) == null ? void 0 : s.call(e);
    });
  }
  enableUpdating(t) {
  }
  disconnectedCallback() {
    var t;
    (t = this._$EO) == null || t.forEach((e) => {
      var s;
      return (s = e.hostDisconnected) == null ? void 0 : s.call(e);
    });
  }
  attributeChangedCallback(t, e, s) {
    this._$AK(t, s);
  }
  _$EC(t, e) {
    var o;
    const s = this.constructor.elementProperties.get(t), n = this.constructor._$Eu(t, s);
    if (n !== void 0 && s.reflect === true) {
      const r = (((o = s.converter) == null ? void 0 : o.toAttribute) !== void 0 ? s.converter : ui).toAttribute(e, s.type);
      this._$Em = t, r == null ? this.removeAttribute(n) : this.setAttribute(n, r), this._$Em = null;
    }
  }
  _$AK(t, e) {
    var o;
    const s = this.constructor, n = s._$Eh.get(t);
    if (n !== void 0 && this._$Em !== n) {
      const r = s.getPropertyOptions(n), a = typeof r.converter == "function" ? { fromAttribute: r.converter } : ((o = r.converter) == null ? void 0 : o.fromAttribute) !== void 0 ? r.converter : ui;
      this._$Em = n, this[n] = a.fromAttribute(e, r.type), this._$Em = null;
    }
  }
  requestUpdate(t, e, s) {
    if (t !== void 0) {
      if (s ?? (s = this.constructor.getPropertyOptions(t)), !(s.hasChanged ?? gn)(this[t], e))
        return;
      this.P(t, e, s);
    }
    this.isUpdatePending === false && (this._$ES = this._$ET());
  }
  P(t, e, s) {
    this._$AL.has(t) || this._$AL.set(t, e), s.reflect === true && this._$Em !== t && (this._$Ej ?? (this._$Ej = /* @__PURE__ */ new Set())).add(t);
  }
  async _$ET() {
    this.isUpdatePending = true;
    try {
      await this._$ES;
    } catch (e) {
      Promise.reject(e);
    }
    const t = this.scheduleUpdate();
    return t != null && await t, !this.isUpdatePending;
  }
  scheduleUpdate() {
    return this.performUpdate();
  }
  performUpdate() {
    var s;
    if (!this.isUpdatePending)
      return;
    if (!this.hasUpdated) {
      if (this.renderRoot ?? (this.renderRoot = this.createRenderRoot()), this._$Ep) {
        for (const [o, r] of this._$Ep)
          this[o] = r;
        this._$Ep = void 0;
      }
      const n = this.constructor.elementProperties;
      if (n.size > 0)
        for (const [o, r] of n)
          r.wrapped !== true || this._$AL.has(o) || this[o] === void 0 || this.P(o, this[o], r);
    }
    let t = false;
    const e = this._$AL;
    try {
      t = this.shouldUpdate(e), t ? (this.willUpdate(e), (s = this._$EO) == null || s.forEach((n) => {
        var o;
        return (o = n.hostUpdate) == null ? void 0 : o.call(n);
      }), this.update(e)) : this._$EU();
    } catch (n) {
      throw t = false, this._$EU(), n;
    }
    t && this._$AE(e);
  }
  willUpdate(t) {
  }
  _$AE(t) {
    var e;
    (e = this._$EO) == null || e.forEach((s) => {
      var n;
      return (n = s.hostUpdated) == null ? void 0 : n.call(s);
    }), this.hasUpdated || (this.hasUpdated = true, this.firstUpdated(t)), this.updated(t);
  }
  _$EU() {
    this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = false;
  }
  get updateComplete() {
    return this.getUpdateComplete();
  }
  getUpdateComplete() {
    return this._$ES;
  }
  shouldUpdate(t) {
    return true;
  }
  update(t) {
    this._$Ej && (this._$Ej = this._$Ej.forEach((e) => this._$EC(e, this[e]))), this._$EU();
  }
  updated(t) {
  }
  firstUpdated(t) {
  }
};
Ut.elementStyles = [], Ut.shadowRootOptions = { mode: "open" }, Ut[_e("elementProperties")] = /* @__PURE__ */ new Map(), Ut[_e("finalized")] = /* @__PURE__ */ new Map(), Ri2 == null || Ri2({ ReactiveElement: Ut }), (vt.reactiveElementVersions ?? (vt.reactiveElementVersions = [])).push("2.0.4");
var xe = globalThis;
var hi = xe.trustedTypes;
var fs = hi ? hi.createPolicy("lit-html", { createHTML: (i) => i }) : void 0;
var Zo = "$lit$";
var bt = `lit$${Math.random().toFixed(9).slice(2)}$`;
var tr = "?" + bt;
var uu = `<${tr}>`;
var zt = document;
var Oe = () => zt.createComment("");
var Te = (i) => i === null || typeof i != "object" && typeof i != "function";
var er = Array.isArray;
var hu = (i) => er(i) || typeof (i == null ? void 0 : i[Symbol.iterator]) == "function";
var Ni = `[ 	
\f\r]`;
var me = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
var ms = /-->/g;
var bs = />/g;
var It = RegExp(`>|${Ni}(?:([^\\s"'>=/]+)(${Ni}*=${Ni}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
var gs = /'/g;
var vs = /"/g;
var ir = /^(?:script|style|textarea|title)$/i;
var du = (i) => (t, ...e) => ({ _$litType$: i, strings: t, values: e });
var nr = du(1);
var Zt = Symbol.for("lit-noChange");
var D = Symbol.for("lit-nothing");
var ys = /* @__PURE__ */ new WeakMap();
var Rt = zt.createTreeWalker(zt, 129);
function sr(i, t) {
  if (!Array.isArray(i) || !i.hasOwnProperty("raw"))
    throw Error("invalid template strings array");
  return fs !== void 0 ? fs.createHTML(t) : t;
}
var pu = (i, t) => {
  const e = i.length - 1, s = [];
  let n, o = t === 2 ? "<svg>" : "", r = me;
  for (let a = 0; a < e; a++) {
    const l = i[a];
    let u, h, d = -1, c = 0;
    for (; c < l.length && (r.lastIndex = c, h = r.exec(l), h !== null); )
      c = r.lastIndex, r === me ? h[1] === "!--" ? r = ms : h[1] !== void 0 ? r = bs : h[2] !== void 0 ? (ir.test(h[2]) && (n = RegExp("</" + h[2], "g")), r = It) : h[3] !== void 0 && (r = It) : r === It ? h[0] === ">" ? (r = n ?? me, d = -1) : h[1] === void 0 ? d = -2 : (d = r.lastIndex - h[2].length, u = h[1], r = h[3] === void 0 ? It : h[3] === '"' ? vs : gs) : r === vs || r === gs ? r = It : r === ms || r === bs ? r = me : (r = It, n = void 0);
    const p = r === It && i[a + 1].startsWith("/>") ? " " : "";
    o += r === me ? l + uu : d >= 0 ? (s.push(u), l.slice(0, d) + Zo + l.slice(d) + bt + p) : l + bt + (d === -2 ? a : p);
  }
  return [sr(i, o + (i[e] || "<?>") + (t === 2 ? "</svg>" : "")), s];
};
var Ie = class _Ie {
  constructor({ strings: t, _$litType$: e }, s) {
    let n;
    this.parts = [];
    let o = 0, r = 0;
    const a = t.length - 1, l = this.parts, [u, h] = pu(t, e);
    if (this.el = _Ie.createElement(u, s), Rt.currentNode = this.el.content, e === 2) {
      const d = this.el.content.firstChild;
      d.replaceWith(...d.childNodes);
    }
    for (; (n = Rt.nextNode()) !== null && l.length < a; ) {
      if (n.nodeType === 1) {
        if (n.hasAttributes())
          for (const d of n.getAttributeNames())
            if (d.endsWith(Zo)) {
              const c = h[r++], p = n.getAttribute(d).split(bt), g = /([.?@])?(.*)/.exec(c);
              l.push({ type: 1, index: o, name: g[2], strings: p, ctor: g[1] === "." ? mu : g[1] === "?" ? bu : g[1] === "@" ? gu : Ci2 }), n.removeAttribute(d);
            } else
              d.startsWith(bt) && (l.push({ type: 6, index: o }), n.removeAttribute(d));
        if (ir.test(n.tagName)) {
          const d = n.textContent.split(bt), c = d.length - 1;
          if (c > 0) {
            n.textContent = hi ? hi.emptyScript : "";
            for (let p = 0; p < c; p++)
              n.append(d[p], Oe()), Rt.nextNode(), l.push({ type: 2, index: ++o });
            n.append(d[c], Oe());
          }
        }
      } else if (n.nodeType === 8)
        if (n.data === tr)
          l.push({ type: 2, index: o });
        else {
          let d = -1;
          for (; (d = n.data.indexOf(bt, d + 1)) !== -1; )
            l.push({ type: 7, index: o }), d += bt.length - 1;
        }
      o++;
    }
  }
  static createElement(t, e) {
    const s = zt.createElement("template");
    return s.innerHTML = t, s;
  }
};
function te(i, t, e = i, s) {
  var r, a;
  if (t === Zt)
    return t;
  let n = s !== void 0 ? (r = e._$Co) == null ? void 0 : r[s] : e._$Cl;
  const o = Te(t) ? void 0 : t._$litDirective$;
  return (n == null ? void 0 : n.constructor) !== o && ((a = n == null ? void 0 : n._$AO) == null || a.call(n, false), o === void 0 ? n = void 0 : (n = new o(i), n._$AT(i, e, s)), s !== void 0 ? (e._$Co ?? (e._$Co = []))[s] = n : e._$Cl = n), n !== void 0 && (t = te(i, n._$AS(i, t.values), n, s)), t;
}
var fu = class {
  constructor(t, e) {
    this._$AV = [], this._$AN = void 0, this._$AD = t, this._$AM = e;
  }
  get parentNode() {
    return this._$AM.parentNode;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  u(t) {
    const { el: { content: e }, parts: s } = this._$AD, n = ((t == null ? void 0 : t.creationScope) ?? zt).importNode(e, true);
    Rt.currentNode = n;
    let o = Rt.nextNode(), r = 0, a = 0, l = s[0];
    for (; l !== void 0; ) {
      if (r === l.index) {
        let u;
        l.type === 2 ? u = new We(o, o.nextSibling, this, t) : l.type === 1 ? u = new l.ctor(o, l.name, l.strings, this, t) : l.type === 6 && (u = new vu(o, this, t)), this._$AV.push(u), l = s[++a];
      }
      r !== (l == null ? void 0 : l.index) && (o = Rt.nextNode(), r++);
    }
    return Rt.currentNode = zt, n;
  }
  p(t) {
    let e = 0;
    for (const s of this._$AV)
      s !== void 0 && (s.strings !== void 0 ? (s._$AI(t, s, e), e += s.strings.length - 2) : s._$AI(t[e])), e++;
  }
};
var We = class _We {
  get _$AU() {
    var t;
    return ((t = this._$AM) == null ? void 0 : t._$AU) ?? this._$Cv;
  }
  constructor(t, e, s, n) {
    this.type = 2, this._$AH = D, this._$AN = void 0, this._$AA = t, this._$AB = e, this._$AM = s, this.options = n, this._$Cv = (n == null ? void 0 : n.isConnected) ?? true;
  }
  get parentNode() {
    let t = this._$AA.parentNode;
    const e = this._$AM;
    return e !== void 0 && (t == null ? void 0 : t.nodeType) === 11 && (t = e.parentNode), t;
  }
  get startNode() {
    return this._$AA;
  }
  get endNode() {
    return this._$AB;
  }
  _$AI(t, e = this) {
    t = te(this, t, e), Te(t) ? t === D || t == null || t === "" ? (this._$AH !== D && this._$AR(), this._$AH = D) : t !== this._$AH && t !== Zt && this._(t) : t._$litType$ !== void 0 ? this.$(t) : t.nodeType !== void 0 ? this.T(t) : hu(t) ? this.k(t) : this._(t);
  }
  S(t) {
    return this._$AA.parentNode.insertBefore(t, this._$AB);
  }
  T(t) {
    this._$AH !== t && (this._$AR(), this._$AH = this.S(t));
  }
  _(t) {
    this._$AH !== D && Te(this._$AH) ? this._$AA.nextSibling.data = t : this.T(zt.createTextNode(t)), this._$AH = t;
  }
  $(t) {
    var o;
    const { values: e, _$litType$: s } = t, n = typeof s == "number" ? this._$AC(t) : (s.el === void 0 && (s.el = Ie.createElement(sr(s.h, s.h[0]), this.options)), s);
    if (((o = this._$AH) == null ? void 0 : o._$AD) === n)
      this._$AH.p(e);
    else {
      const r = new fu(n, this), a = r.u(this.options);
      r.p(e), this.T(a), this._$AH = r;
    }
  }
  _$AC(t) {
    let e = ys.get(t.strings);
    return e === void 0 && ys.set(t.strings, e = new Ie(t)), e;
  }
  k(t) {
    er(this._$AH) || (this._$AH = [], this._$AR());
    const e = this._$AH;
    let s, n = 0;
    for (const o of t)
      n === e.length ? e.push(s = new _We(this.S(Oe()), this.S(Oe()), this, this.options)) : s = e[n], s._$AI(o), n++;
    n < e.length && (this._$AR(s && s._$AB.nextSibling, n), e.length = n);
  }
  _$AR(t = this._$AA.nextSibling, e) {
    var s;
    for ((s = this._$AP) == null ? void 0 : s.call(this, false, true, e); t && t !== this._$AB; ) {
      const n = t.nextSibling;
      t.remove(), t = n;
    }
  }
  setConnected(t) {
    var e;
    this._$AM === void 0 && (this._$Cv = t, (e = this._$AP) == null || e.call(this, t));
  }
};
var Ci2 = class {
  get tagName() {
    return this.element.tagName;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  constructor(t, e, s, n, o) {
    this.type = 1, this._$AH = D, this._$AN = void 0, this.element = t, this.name = e, this._$AM = n, this.options = o, s.length > 2 || s[0] !== "" || s[1] !== "" ? (this._$AH = Array(s.length - 1).fill(new String()), this.strings = s) : this._$AH = D;
  }
  _$AI(t, e = this, s, n) {
    const o = this.strings;
    let r = false;
    if (o === void 0)
      t = te(this, t, e, 0), r = !Te(t) || t !== this._$AH && t !== Zt, r && (this._$AH = t);
    else {
      const a = t;
      let l, u;
      for (t = o[0], l = 0; l < o.length - 1; l++)
        u = te(this, a[s + l], e, l), u === Zt && (u = this._$AH[l]), r || (r = !Te(u) || u !== this._$AH[l]), u === D ? t = D : t !== D && (t += (u ?? "") + o[l + 1]), this._$AH[l] = u;
    }
    r && !n && this.j(t);
  }
  j(t) {
    t === D ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t ?? "");
  }
};
var mu = class extends Ci2 {
  constructor() {
    super(...arguments), this.type = 3;
  }
  j(t) {
    this.element[this.name] = t === D ? void 0 : t;
  }
};
var bu = class extends Ci2 {
  constructor() {
    super(...arguments), this.type = 4;
  }
  j(t) {
    this.element.toggleAttribute(this.name, !!t && t !== D);
  }
};
var gu = class extends Ci2 {
  constructor(t, e, s, n, o) {
    super(t, e, s, n, o), this.type = 5;
  }
  _$AI(t, e = this) {
    if ((t = te(this, t, e, 0) ?? D) === Zt)
      return;
    const s = this._$AH, n = t === D && s !== D || t.capture !== s.capture || t.once !== s.once || t.passive !== s.passive, o = t !== D && (s === D || n);
    n && this.element.removeEventListener(this.name, this, s), o && this.element.addEventListener(this.name, this, t), this._$AH = t;
  }
  handleEvent(t) {
    var e;
    typeof this._$AH == "function" ? this._$AH.call(((e = this.options) == null ? void 0 : e.host) ?? this.element, t) : this._$AH.handleEvent(t);
  }
};
var vu = class {
  constructor(t, e, s) {
    this.element = t, this.type = 6, this._$AN = void 0, this._$AM = e, this.options = s;
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AI(t) {
    te(this, t);
  }
};
var Mi = xe.litHtmlPolyfillSupport;
Mi == null || Mi(Ie, We), (xe.litHtmlVersions ?? (xe.litHtmlVersions = [])).push("3.1.4");
var yu = (i, t, e) => {
  const s = (e == null ? void 0 : e.renderBefore) ?? t;
  let n = s._$litPart$;
  if (n === void 0) {
    const o = (e == null ? void 0 : e.renderBefore) ?? null;
    s._$litPart$ = n = new We(t.insertBefore(Oe(), o), o, void 0, e ?? {});
  }
  return n._$AI(i), n;
};
var Wt = class extends Ut {
  constructor() {
    super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
  }
  createRenderRoot() {
    var e;
    const t = super.createRenderRoot();
    return (e = this.renderOptions).renderBefore ?? (e.renderBefore = t.firstChild), t;
  }
  update(t) {
    const e = this.render();
    this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(t), this._$Do = yu(e, this.renderRoot, this.renderOptions);
  }
  connectedCallback() {
    var t;
    super.connectedCallback(), (t = this._$Do) == null || t.setConnected(true);
  }
  disconnectedCallback() {
    var t;
    super.disconnectedCallback(), (t = this._$Do) == null || t.setConnected(false);
  }
  render() {
    return Zt;
  }
};
var _s;
Wt._$litElement$ = true, Wt.finalized = true, (_s = globalThis.litElementHydrateSupport) == null || _s.call(globalThis, { LitElement: Wt });
var Li = globalThis.litElementPolyfillSupport;
Li == null || Li({ LitElement: Wt });
(globalThis.litElementVersions ?? (globalThis.litElementVersions = [])).push("4.0.6");
var _u = { attribute: true, type: String, converter: ui, reflect: false, hasChanged: gn };
var xu = (i = _u, t, e) => {
  const { kind: s, metadata: n } = e;
  let o = globalThis.litPropertyMetadata.get(n);
  if (o === void 0 && globalThis.litPropertyMetadata.set(n, o = /* @__PURE__ */ new Map()), o.set(e.name, i), s === "accessor") {
    const { name: r } = e;
    return { set(a) {
      const l = t.get.call(this);
      t.set.call(this, a), this.requestUpdate(r, l, i);
    }, init(a) {
      return a !== void 0 && this.P(r, void 0, i), a;
    } };
  }
  if (s === "setter") {
    const { name: r } = e;
    return function(a) {
      const l = this[r];
      t.call(this, a), this.requestUpdate(r, l, i);
    };
  }
  throw Error("Unsupported decorator location: " + s);
};
function tt(i) {
  return (t, e) => typeof e == "object" ? xu(i, t, e) : ((s, n, o) => {
    const r = n.hasOwnProperty(o);
    return n.constructor.createProperty(o, r ? { ...s, wrapped: true } : s), r ? Object.getOwnPropertyDescriptor(n, o) : void 0;
  })(i, t, e);
}
function wu(i) {
  return tt({ ...i, state: true, attribute: false });
}
var $u = class extends Object3D {
  constructor(t = document.createElement("div")) {
    super(), this.isCSS2DObject = true, this.element = t, this.element.style.position = "absolute", this.element.style.userSelect = "none", this.element.setAttribute("draggable", false), this.center = new Vector2(0.5, 0.5), this.addEventListener("removed", function() {
      this.traverse(function(e) {
        e.element instanceof Element && e.element.parentNode !== null && e.element.parentNode.removeChild(e.element);
      });
    });
  }
  copy(t, e) {
    return super.copy(t, e), this.element = t.element.cloneNode(true), this.center = t.center, this;
  }
};
new Vector3();
new Matrix4();
new Matrix4();
new Vector3();
new Vector3();
var Cu = class {
  constructor(t, e) {
    this._group = new Group(), this._frustum = new Frustum(), this._frustumMat = new Matrix4(), this._regenerateDelay = 200, this._regenerateCounter = 0, this.material = new LineBasicMaterial({ color: "#2e3338" }), this.numbers = new Group(), this.maxRegenerateRetrys = 4, this.gridsFactor = 5, this._scaleX = 1, this._scaleY = 1, this._camera = t, this._container = e;
    const s = this.newGrid(-1), n = this.newGrid(-2);
    this.grids = { main: s, secondary: n }, this._group.add(n, s, this.numbers);
  }
  set scaleX(t) {
    this._scaleX = t, this.regenerate();
  }
  get scaleX() {
    return this._scaleX;
  }
  set scaleY(t) {
    this._scaleY = t, this.regenerate();
  }
  get scaleY() {
    return this._scaleY;
  }
  get() {
    return this._group;
  }
  dispose() {
    const { main: t, secondary: e } = this.grids;
    t.removeFromParent(), e.removeFromParent(), t.geometry.dispose(), t.material.dispose(), e.geometry.dispose(), e.material.dispose();
  }
  regenerate() {
    if (!this.isGridReady()) {
      if (this._regenerateCounter++, this._regenerateCounter > this.maxRegenerateRetrys)
        throw new Error("Grid could not be regenerated");
      setTimeout(() => this.regenerate, this._regenerateDelay);
      return;
    }
    this._regenerateCounter = 0, this._camera.updateMatrix(), this._camera.updateMatrixWorld();
    const e = this._frustumMat.multiplyMatrices(
      this._camera.projectionMatrix,
      this._camera.matrixWorldInverse
    );
    this._frustum.setFromProjectionMatrix(e);
    const { planes: s } = this._frustum, n = s[0].constant * -s[0].normal.x, o = s[1].constant * -s[1].normal.x, r = s[2].constant * -s[2].normal.y, a = s[3].constant * -s[3].normal.y, l = Math.abs(n - o), u = Math.abs(a - r), { clientWidth: h, clientHeight: d } = this._container, c = Math.max(h, d), g = Math.max(l, u) / c, w = Math.ceil(Math.log10(l / this.scaleX)), _ = Math.ceil(Math.log10(u / this.scaleY)), x = 10 ** (w - 2) * this.scaleX, m = 10 ** (_ - 2) * this.scaleY, b = x * this.gridsFactor, v = m * this.gridsFactor, $ = Math.ceil(u / v), C = Math.ceil(l / b), E = Math.ceil(u / m), A = Math.ceil(l / x), j = x * Math.ceil(o / x), O = m * Math.ceil(r / m), G = b * Math.ceil(o / b), kt = v * Math.ceil(r / v), ht = [...this.numbers.children];
    for (const F of ht)
      F.removeFromParent();
    this.numbers.children = [];
    const U = [], dt = 9 * g, H = 1e4, et = Math.round(Math.abs(G / this.scaleX) * H) / H, q = (C - 1) * b, it = Math.round(Math.abs((G + q) / this.scaleX) * H) / H, or = Math.max(et, it).toString().length * dt;
    let Ei = Math.ceil(or / b) * b;
    for (let F = 0; F < C; F++) {
      let z = G + F * b;
      U.push(z, a, 0, z, r, 0);
      const de = z / this.scaleX;
      z = Math.round(z * H) / H, Ei = Math.round(Ei * H) / H;
      const Ge = z % Ei;
      if (!(b < 1 || v < 1) && Math.abs(Ge) > 0.01)
        continue;
      const ur = this.newNumber(de), hr = 12 * g;
      ur.position.set(z, r + hr, 0);
    }
    for (let F = 0; F < $; F++) {
      const z = kt + F * v;
      U.push(o, z, 0, n, z, 0);
      const de = this.newNumber(z / this.scaleY);
      let Ge = 12;
      de.element.textContent && (Ge += 4 * de.element.textContent.length);
      const wn = Ge * g;
      de.position.set(o + wn, z, 0);
    }
    const Ai2 = [];
    for (let F = 0; F < A; F++) {
      const z = j + F * x;
      Ai2.push(z, a, 0, z, r, 0);
    }
    for (let F = 0; F < E; F++) {
      const z = O + F * m;
      Ai2.push(o, z, 0, n, z, 0);
    }
    const rr = new BufferAttribute(new Float32Array(U), 3), ar = new BufferAttribute(new Float32Array(Ai2), 3), { main: lr, secondary: cr } = this.grids;
    lr.geometry.setAttribute("position", rr), cr.geometry.setAttribute("position", ar);
  }
  newNumber(t) {
    const e = document.createElement("bim-label");
    e.textContent = String(Math.round(t * 100) / 100);
    const s = new $u(e);
    return this.numbers.add(s), s;
  }
  newGrid(t) {
    const e = new BufferGeometry(), s = new LineSegments(e, this.material);
    return s.frustumCulled = false, s.renderOrder = t, s;
  }
  isGridReady() {
    const t = this._camera.projectionMatrix.elements;
    for (let e = 0; e < t.length; e++) {
      const s = t[e];
      if (Number.isNaN(s))
        return false;
    }
    return true;
  }
};
var Eu = Object.defineProperty;
var Au = Object.getOwnPropertyDescriptor;
var vn = (i, t, e, s) => {
  for (var n = Au(t, e), o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Eu(t, e, n), n;
};
var yn = class yn2 extends Wt {
  constructor() {
    super(...arguments), this._grid = null, this._world = null, this.resize = () => {
      this._world && this._grid && this._grid.regenerate();
    };
  }
  set gridColor(t) {
    if (this._gridColor = t, !(t && this._grid))
      return;
    const e = Number(t.replace("#", "0x"));
    Number.isNaN(e) || this._grid.material.color.setHex(e);
  }
  get gridColor() {
    return this._gridColor;
  }
  set gridScaleX(t) {
    this._gridScaleX = t, t && this._grid && (this._grid.scaleX = t);
  }
  get gridScaleX() {
    return this._gridScaleX;
  }
  set gridScaleY(t) {
    this._gridScaleY = t, t && this._grid && (this._grid.scaleY = t);
  }
  get gridScaleY() {
    return this._gridScaleY;
  }
  set components(t) {
    this.dispose();
    const s = t.get(Ci).create();
    this._world = s, s.scene = new sa(t), s.scene.setup(), s.renderer = new Uf(t, this);
    const n = new na(t);
    s.camera = n;
    const o = new Cu(n.threeOrtho, this);
    this._grid = o, s.scene.three.add(o.get()), n.controls.addEventListener(
      "update",
      () => o.regenerate()
    ), setTimeout(async () => {
      s.camera.updateAspect(), n.set("Plan"), await n.controls.setLookAt(0, 0, 100, 0, 0, 0), await n.projection.set("Orthographic"), n.controls.dollySpeed = 3, n.controls.draggingSmoothTime = 0.085, n.controls.maxZoom = 1e3, n.controls.zoom(4);
    });
  }
  get world() {
    return this._world;
  }
  dispose() {
    var t;
    (t = this.world) == null || t.dispose(), this._world = null, this._grid = null;
  }
  connectedCallback() {
    super.connectedCallback(), new ResizeObserver(this.resize).observe(this);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.dispose();
  }
  render() {
    return nr`<slot></slot>`;
  }
};
yn.styles = Ko`
    :host {
      position: relative;
      display: flex;
      min-width: 0px;
      height: 100%;
      background-color: var(--bim-ui_bg-base);
    }
  `;
var ee = yn;
vn([
  tt({ type: String, attribute: "grid-color", reflect: true })
], ee.prototype, "gridColor");
vn([
  tt({ type: Number, attribute: "grid-scale-x", reflect: true })
], ee.prototype, "gridScaleX");
vn([
  tt({ type: Number, attribute: "grid-scale-y", reflect: true })
], ee.prototype, "gridScaleY");
var Su = Object.defineProperty;
var St = (i, t, e, s) => {
  for (var n = void 0, o = i.length - 1, r; o >= 0; o--)
    (r = i[o]) && (n = r(t, e, n) || n);
  return n && Su(t, e, n), n;
};
var _n2 = class _n3 extends Wt {
  constructor() {
    super(...arguments), this._defaults = {
      size: 60
    }, this._cssMatrix3D = "", this._matrix = new Matrix4(), this._onRightClick = new Event("rightclick"), this._onLeftClick = new Event("leftclick"), this._onTopClick = new Event("topclick"), this._onBottomClick = new Event("bottomclick"), this._onFrontClick = new Event("frontclick"), this._onBackClick = new Event("backclick"), this._camera = null, this._epsilon = (t) => Math.abs(t) < 1e-10 ? 0 : t;
  }
  set camera(t) {
    this._camera = t, this.updateOrientation();
  }
  get camera() {
    return this._camera;
  }
  updateOrientation() {
    if (!this.camera)
      return;
    this._matrix.extractRotation(this.camera.matrixWorldInverse);
    const { elements: t } = this._matrix;
    this._cssMatrix3D = `matrix3d(
      ${this._epsilon(t[0])},
      ${this._epsilon(-t[1])},
      ${this._epsilon(t[2])},
      ${this._epsilon(t[3])},
      ${this._epsilon(t[4])},
      ${this._epsilon(-t[5])},
      ${this._epsilon(t[6])},
      ${this._epsilon(t[7])},
      ${this._epsilon(t[8])},
      ${this._epsilon(-t[9])},
      ${this._epsilon(t[10])},
      ${this._epsilon(t[11])},
      ${this._epsilon(t[12])},
      ${this._epsilon(-t[13])},
      ${this._epsilon(t[14])},
      ${this._epsilon(t[15])})
    `;
  }
  render() {
    const t = this.size ?? this._defaults.size;
    return nr`
      <style>
        .face,
        .cube {
          width: ${t}px;
          height: ${t}px;
          transform: translateZ(-300px) ${this._cssMatrix3D};
        }

        .face-right {
          translate: ${t / 2}px 0 0;
        }

        .face-left {
          translate: ${-t / 2}px 0 0;
        }

        .face-top {
          translate: 0 ${t / 2}px 0;
        }

        .face-bottom {
          translate: 0 ${-t / 2}px 0;
        }

        .face-front {
          translate: 0 0 ${t / 2}px;
        }

        .face-back {
          translate: 0 0 ${-t / 2}px;
        }
      </style>
      <div class="parent">
        <div class="cube">
          <div
            class="face x-direction face-right"
            @click=${() => this.dispatchEvent(this._onRightClick)}
          >
            ${this.rightText}
          </div>
          <div
            class="face x-direction face-left"
            @click=${() => this.dispatchEvent(this._onLeftClick)}
          >
            ${this.leftText}
          </div>
          <div
            class="face y-direction face-top"
            @click=${() => this.dispatchEvent(this._onTopClick)}
          >
            ${this.topText}
          </div>
          <div
            class="face y-direction face-bottom"
            @click=${() => this.dispatchEvent(this._onBottomClick)}
          >
            ${this.bottomText}
          </div>
          <div
            class="face z-direction face-front"
            @click=${() => this.dispatchEvent(this._onFrontClick)}
          >
            ${this.frontText}
          </div>
          <div
            class="face z-direction face-back"
            @click=${() => this.dispatchEvent(this._onBackClick)}
          >
            ${this.backText}
          </div>
        </div>
      </div>
    `;
  }
};
_n2.styles = Ko`
    :host {
      position: absolute;
      z-index: 999;
      bottom: 1rem;
      right: 1rem;
    }

    .parent {
      perspective: 400px;
    }

    .cube {
      position: relative;
      transform-style: preserve-3d;
    }

    .face {
      position: absolute;
      display: flex;
      justify-content: center;
      user-select: none;
      align-items: center;
      cursor: pointer;
      text-align: center;
      transition: all 0.2s;
      color: var(--bim-view-cube--c, white);
      font-size: var(--bim-view-cube--fz, --bim-ui_size-2xl);
    }

    .x-direction {
      // background-color: var(--bim-view-cube_x--bgc, #c93830DD);
      background-color: var(--bim-view-cube_x--bgc, #01a6bcde);
    }

    .x-direction:hover {
      background-color: var(--bim-ui_accent-base, white);
    }

    .y-direction {
      // background-color: var(--bim-view-cube_y--bgc, #54ff19DD);
      background-color: var(--bim-view-cube_y--bgc, #8d0ec8de);
    }

    .y-direction:hover {
      background-color: var(--bim-ui_accent-base, white);
    }

    .z-direction {
      // background-color: var(--bim-view-cube_z--bgc, #3041c9DD);
      background-color: var(--bim-view-cube_z--bgc, #2718afde);
    }

    .z-direction:hover {
      background-color: var(--bim-ui_accent-base, white);
    }

    .face-front {
      transform: rotateX(180deg);
    }

    .face-back {
      transform: rotateZ(180deg);
    }

    .face-top {
      transform: rotateX(90deg);
    }

    .face-bottom {
      transform: rotateX(270deg);
    }

    .face-right {
      transform: rotateY(-270deg) rotateX(180deg);
    }

    .face-left {
      transform: rotateY(-90deg) rotateX(180deg);
    }
  `;
var K = _n2;
St([
  tt({ type: Number, reflect: true })
], K.prototype, "size");
St([
  tt({ type: String, attribute: "right-text", reflect: true })
], K.prototype, "rightText");
St([
  tt({ type: String, attribute: "left-text", reflect: true })
], K.prototype, "leftText");
St([
  tt({ type: String, attribute: "top-text", reflect: true })
], K.prototype, "topText");
St([
  tt({ type: String, attribute: "bottom-text", reflect: true })
], K.prototype, "bottomText");
St([
  tt({ type: String, attribute: "front-text", reflect: true })
], K.prototype, "frontText");
St([
  tt({ type: String, attribute: "back-text", reflect: true })
], K.prototype, "backText");
St([
  wu()
], K.prototype, "_cssMatrix3D");
var rh = class {
  /**
   * Initializes the custom elements for the BIM application.
   *
   * @remarks
   * This method should be called once during the application's initialization.
   *
   */
  static init() {
    Qi.defineCustomElement("bim-view-cube", K), Qi.defineCustomElement("bim-world-2d", ee);
  }
};
export {
  rh as Manager,
  K as ViewCube,
  ee as World2D,
  nh as buttons,
  sh as tables
};
/*! Bundled license information:

@thatopen/ui-obc/dist/index.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
  (**
   * @license
   * Copyright 2020 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
  (**
  * (c) Iconify
  *
  * For the full copyright and license information, please view the license.txt
  * files at https://github.com/iconify/iconify
  *
  * Licensed under MIT.
  *
  * @license MIT
  * @version 2.0.0
  *)
  (**
   * @license
   * Copyright 2018 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
*/
//# sourceMappingURL=@thatopen_ui-obc.js.map
